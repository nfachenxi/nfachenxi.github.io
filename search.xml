<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>序章</title>
      <link href="/posts/81s2cdc0/"/>
      <url>/posts/81s2cdc0/</url>
      
        <content type="html"><![CDATA[<div class="hbe hbe-container" id="hexo-blog-encrypt" data-wpm="密码错误，不要偷看哦。" data-whm="内容认证失败，但仍可尝试查看。">  <script id="hbeData" type="hbeData" data-hmacdigest="7bf7167deaa3c255d62ca62d49a161d910b4746d253ac6305d80352ec041a27b"></script>  <div class="hbe hbe-content">    <div class="hbe hbe-input hbe-input-default">      <input class="hbe hbe-input-field hbe-input-field-default" type="password" id="hbePass">      <label class="hbe hbe-input-label hbe-input-label-default" for="hbePass">        <span class="hbe hbe-input-label-content hbe-input-label-content-default">请输入密码查看本文。</span>      </label>    </div>  </div></div><script data-pjax src="/lib/hbe.js"></script><link href="/css/hbe.style.css" rel="stylesheet" type="text/css">]]></content>
      
      
      <categories>
          
          <category> 小说 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 小说 </tag>
            
            <tag> 终末之旅 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>力扣刷题日常(17-18)</title>
      <link href="/posts/6491732a/"/>
      <url>/posts/6491732a/</url>
      
        <content type="html"><![CDATA[<h1>力扣刷题日常(17-18)</h1><h2 id="第17题-电话号码的字母组合-难度L">第17题: 电话号码的字母组合 (难度L)</h2><h3 id="原题">原题:</h3><p>给定一个仅包含数字 <code>2-9</code> 的字符串，返回所有它能表示的字母组合。答案可以按 <strong>任意顺序</strong> 返回。</p><p>给出数字到字母的映射如下（与电话按键相同）。注意 1 不对应任何字母。</p><p><img src="https://image.nfasystem.top/img-nfa/1752723054-mfIHZs-image.png" alt="img"></p><p><strong>示例 1：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入：digits = &quot;23&quot;</span><br><span class="line">输出：[&quot;ad&quot;,&quot;ae&quot;,&quot;af&quot;,&quot;bd&quot;,&quot;be&quot;,&quot;bf&quot;,&quot;cd&quot;,&quot;ce&quot;,&quot;cf&quot;]</span><br></pre></td></tr></table></figure><p><strong>示例 2：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入：digits = &quot;&quot;</span><br><span class="line">输出：[]</span><br></pre></td></tr></table></figure><p><strong>示例 3：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入：digits = &quot;2&quot;</span><br><span class="line">输出：[&quot;a&quot;,&quot;b&quot;,&quot;c&quot;]</span><br></pre></td></tr></table></figure><p><strong>提示：</strong></p><ul><li><code>0 &lt;= digits.length &lt;= 4</code></li><li><code>digits[i]</code> 是范围 <code>['2', '9']</code> 的一个数字。</li></ul><hr><h3 id="开始解题">开始解题:</h3>]]></content>
      
      
      <categories>
          
          <category> 力扣刷题 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 算法 </tag>
            
            <tag> 力扣 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>力扣刷题日常(15-16)</title>
      <link href="/posts/80d20dc4/"/>
      <url>/posts/80d20dc4/</url>
      
        <content type="html"><![CDATA[<h1>力扣刷题日常(15-16)</h1><h2 id="第15题-三数之和-难度-中等">第15题: 三数之和 (难度:中等)</h2><h3 id="原题">原题:</h3><p>给我们一个整数数组 <code>nums</code> ，判断是否存在三元组 <code>[nums[i], nums[j], nums[k]]</code> 满足 <code>i != j</code>、<code>i != k</code> 且 <code>j != k</code> ，同时还满足 <code>nums[i] + nums[j] + nums[k] == 0</code> 。请我们返回所有和为 <code>0</code> 且不重复的三元组。</p><p><strong>注意：</strong> 答案中不可以包含重复的三元组。</p><p><strong>示例 1：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">输入：nums = [-1,0,1,2,-1,-4]</span><br><span class="line">输出：[[-1,-1,2],[-1,0,1]]</span><br><span class="line">解释：</span><br><span class="line">nums[0] + nums[1] + nums[2] = (-1) + 0 + 1 = 0 。</span><br><span class="line">nums[1] + nums[2] + nums[4] = 0 + 1 + (-1) = 0 。</span><br><span class="line">nums[0] + nums[3] + nums[4] = (-1) + 2 + (-1) = 0 。</span><br><span class="line">不同的三元组是 [-1,0,1] 和 [-1,-1,2] 。</span><br><span class="line">注意，输出的顺序和三元组的顺序并不重要。</span><br></pre></td></tr></table></figure><p><strong>示例 2：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入：nums = [0,1,1]</span><br><span class="line">输出：[]</span><br><span class="line">解释：唯一可能的三元组和不为 0 。</span><br></pre></td></tr></table></figure><p><strong>示例 3：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入：nums = [0,0,0]</span><br><span class="line">输出：[[0,0,0]]</span><br><span class="line">解释：唯一可能的三元组和为 0 。</span><br></pre></td></tr></table></figure><p><strong>提示：</strong></p><ul><li><code>3 &lt;= nums.length &lt;= 3000</code></li><li><code>-105 &lt;= nums[i] &lt;= 105</code></li></ul><hr><h3 id="原题提示：">原题提示：</h3><ol><li>So, we essentially need to find three numbers x, y, and z such that they add up to the given value. If we fix one of the numbers say x, we are left with the two-sum problem at hand!(所以，我们实际上需要找出三个数 x、y 和 z，使得它们的和等于给定的值。如果我们固定其中一个数，比如 x，那么就只剩下我们当前要解决的两个数相加的问题了！)</li><li>For the two-sum problem, if we fix one of the numbers, say x, we have to scan the entire array to find the next number y, which is value - x where value is the input parameter. Can we change our array somehow so that this search becomes faster?(对于“两数之和”问题，如果我们固定其中一个数字，比如 x，那么我们就必须遍历整个数组来找到下一个数字 y，其值为输入参数 value 减去 x。我们能否以某种方式改变数组，从而使这个搜索过程变得更加快捷呢？)</li><li>The second train of thought for two-sum is, without changing the array, can we use additional space somehow? Like maybe a hash map to speed up the search?(对于两数之和的第二种思路是，在不改变数组的情况下，我们能否以某种方式使用额外的空间？比如，或许可以使用哈希表来加快搜索速度？)</li></ol><hr><h3 id="开始解题：">开始解题：</h3><p>那么这是一道十分经典的算法问题, 它考察的不仅仅是暴力求解, 更是如何通过巧妙的思路优化时间和空间效率.</p><p>解决这个问题, 如果我们直接使用三层循环来遍历所有可能的三数组合, 时间复杂度会达到 O(n³), 这在 <code>n</code> 达到3000时是绝对无法接受的. 因此, 我们需要更优化的方法. 结合题目的提示, 核心思想是 <strong>“降维”</strong>, 也就是将一个 “三数之和” 的问题, 转化为我们更熟悉的 “两数之和” 问题.</p><p>那么我们这里选择的解法为 <strong>“排序 + 双指针”</strong>.</p><p>详细步骤如下所示:</p><ol><li><p><strong>特殊情况处理:</strong> 如果数组的长度小于3, 那么不可能凑成三元组, 直接返回一个空列表.</p></li><li><p><strong>排序:</strong> 这是整个算法的关键第一步. 我们首先对整个数组进行升序排序. 排序有两个至关重要的作用:</p><ul><li>它为后续使用 “双指针” 技巧创造了前提.</li><li>它能极大地简化 “去重” 的逻辑. 相同的元素会聚集在一起, 便于我们跳过.</li><li>我们这里使用数组自带的排序方法, 时间复杂度记为 <code>O(n log n)</code></li></ul></li><li><p><strong>固定一个数, 转化问题:</strong> 我们使用一个 <code>for</code> 循环来遍历排序后的数组, 假设当前遍历到的元素是 <code>nums[i]</code>. 我们将这个 <code>nums[i]</code> 作为三元组中的第一个固定的数. 那么, 我们的任务就变成了在数组剩下的部分 (从索引 <code>i+1</code> 到末尾) 中, 寻找另外两个数, 使它们的和等于 <code>-nums[i]</code>. 这就成功地将问题从 “三数之和” 变成了 “两数之和”.</p></li><li><p><strong>双指针登场:</strong> 对于每一个固定的 <code>nums[i]</code>, 我们在它后面的区间 <code>[i+1, nums.length-1]</code> 中寻找目标两数. 我们使用两个指针:</p><ul><li>左指针 <code>left</code>, 初始化为 <code>i + 1</code>.</li><li>右指针 <code>right</code>, 初始化为数组的最后一个元素的索引 <code>nums.length - 1</code>.</li></ul></li><li><p><strong>移动指针并判断:</strong> 我们在 <code>left &lt; right</code> 的条件下进行循环:</p><ul><li>计算三个数的当前和 <code>sum = nums[i] + nums[left] + nums[right]</code>.</li><li><strong>如果 <code>sum == 0</code>:</strong> 恭喜, 我们找到了一个符合条件的三元组 <code>[nums[i], nums[left], nums[right]]</code>. 我们将它添加到结果列表中.</li><li><strong>如果 <code>sum &lt; 0</code>:</strong> 说明总和太小了. 因为数组是排好序的, 为了让 <code>sum</code> 变大, 我们需要一个更大的数. 此时, 左指针 <code>left</code> 向右移动 (<code>left++</code>), 指向一个可能更大的值.</li><li><strong>如果 <code>sum &gt; 0</code>:</strong> 说明总和太大了. 为了让 <code>sum</code> 变小, 我们需要一个更小的数. 此时, 右指针 <code>right</code> 向左移动 (<code>right--</code>), 指向一个可能更小的值.</li></ul></li><li><p><strong>处理重复三元组:</strong> 这是本题的另一个核心难点.</p><ul><li><strong>对固定的数 <code>nums[i]</code> 去重:</strong> 在 <code>for</code> 循环中, 如果当前的 <code>nums[i]</code> 和它前一个数 <code>nums[i-1]</code> 相等, 说明以这个数开头的所有可能组合我们都已经找过了, 为了避免重复, 我们应该直接跳过, 继续下一次循环.</li><li><strong>对找到的结果 <code>[nums[left], nums[right]]</code> 去重:</strong> 当我们找到一个 <code>sum == 0</code> 的三元组后, 我们不能立刻停止. 比如数组是 <code>[-2, 0, 0, 2, 2]</code>, <code>i</code> 指向 <code>-2</code>. <code>left</code> 指向第一个 <code>0</code>, <code>right</code> 指向最后一个 <code>2</code>, 和为0. 此时, 我们需要继续移动 <code>left</code> 和 <code>right</code> 来寻找新的组合. 但如果只是简单地 <code>left++</code> 和 <code>right--</code>, <code>left</code> 会指向第二个 <code>0</code>, <code>right</code> 会指向第一个 <code>2</code>, 它们依然能组成和为0的三元组, 但这和我们刚找到的是重复的. 所以, 在找到一个解后, 我们需要持续移动 <code>left</code> 和 <code>right</code> 指针, 跳过所有与当前 <code>nums[left]</code> 和 <code>nums[right]</code> 相等的后续元素.</li></ul></li></ol><p>通过以上步骤, 我们就能在 O(n²) 的时间复杂度内, 找出所有不重复的三元组.</p><hr><h3 id="搭配代码理解">搭配代码理解:</h3><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Solution</span> </span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">public</span> IList&lt;IList&lt;<span class="built_in">int</span>&gt;&gt; ThreeSum(<span class="built_in">int</span>[] nums) </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 1. 初始化结果列表</span></span><br><span class="line">        <span class="keyword">var</span> result = <span class="keyword">new</span> List&lt;IList&lt;<span class="built_in">int</span>&gt;&gt;();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 处理边界情况</span></span><br><span class="line">        <span class="keyword">if</span> (nums == <span class="literal">null</span> || nums.Length &lt; <span class="number">3</span>) </span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">return</span> result;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 对数组进行排序</span></span><br><span class="line">        Array.Sort(nums);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 4. 主循环, 固定第一个数 nums[i]</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">0</span>; i &lt; nums.Length - <span class="number">2</span>; i++) </span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">// 如果固定的数大于0, 因为数组已排序, 后面的数都大于0, 和不可能为0</span></span><br><span class="line">            <span class="keyword">if</span> (nums[i] &gt; <span class="number">0</span>) </span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 5. 对固定的数进行去重</span></span><br><span class="line">            <span class="comment">// 如果当前数字和前一个数字相同, 则跳过, 避免重复计算</span></span><br><span class="line">            <span class="comment">// 这里因为已经排序过了。所以直接和前面一个进行比较即可</span></span><br><span class="line">            <span class="keyword">if</span> (i &gt; <span class="number">0</span> &amp;&amp; nums[i] == nums[i - <span class="number">1</span>]) </span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 6. 初始化双指针</span></span><br><span class="line">            <span class="built_in">int</span> left = i + <span class="number">1</span>;</span><br><span class="line">            <span class="built_in">int</span> right = nums.Length - <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 7. 双指针向中间移动</span></span><br><span class="line">            <span class="keyword">while</span> (left &lt; right) </span><br><span class="line">            &#123;</span><br><span class="line">                <span class="built_in">int</span> sum = nums[i] + nums[left] + nums[right];</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (sum == <span class="number">0</span>) </span><br><span class="line">                &#123;</span><br><span class="line">                    <span class="comment">// 找到了一个解</span></span><br><span class="line">                    result.Add(<span class="keyword">new</span> List&lt;<span class="built_in">int</span>&gt; &#123; nums[i], nums[left], nums[right] &#125;);</span><br><span class="line"></span><br><span class="line">                    <span class="comment">// 8. 对左右指针指向的数进行去重</span></span><br><span class="line">                    <span class="keyword">while</span> (left &lt; right &amp;&amp; nums[left] == nums[left + <span class="number">1</span>]) </span><br><span class="line">                    &#123;</span><br><span class="line">                        left++;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">while</span> (left &lt; right &amp;&amp; nums[right] == nums[right - <span class="number">1</span>]) </span><br><span class="line">                    &#123;</span><br><span class="line">                        right--;</span><br><span class="line">                    &#125;</span><br><span class="line"></span><br><span class="line">                    <span class="comment">// 移动指针, 寻找新的可能性</span></span><br><span class="line">                    left++;</span><br><span class="line">                    right--;</span><br><span class="line">                &#125; </span><br><span class="line">                <span class="keyword">else</span> <span class="keyword">if</span> (sum &lt; <span class="number">0</span>) </span><br><span class="line">                &#123;</span><br><span class="line">                    <span class="comment">// 和太小, 左指针右移</span></span><br><span class="line">                    left++;</span><br><span class="line">                &#125; </span><br><span class="line">                <span class="keyword">else</span> <span class="comment">// sum &gt; 0</span></span><br><span class="line">                &#123;</span><br><span class="line">                    <span class="comment">// 和太大, 右指针左移</span></span><br><span class="line">                    right--;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="简单补充：">简单补充：</h4><ol><li><strong><code>IList&lt;IList&lt;int&gt;&gt;</code> (返回类型)</strong></li></ol><ul><li><strong><code>IList&lt;T&gt;</code></strong>: 这是一个接口 (Interface), <code>List&lt;T&gt;</code> 是它的一个常见实现. 在Unity中我们可能直接用 <code>List&lt;GameObject&gt;</code>, 这里的 <code>IList</code> 是一种更通用的&quot;契约&quot;. 它规定了一个集合应该有哪些基本功能 (如 <code>Add</code>, <code>Remove</code>, <code>Count</code>), 但不关心具体是怎么实现的. 在函数签名中使用接口 (<code>IList</code>) 而不是具体类 (<code>List</code>) 是一个很好的编程习惯, 这让代码更灵活, 更具扩展性.</li><li><strong><code>IList&lt;IList&lt;int&gt;&gt;</code></strong>: 这是一个 “列表的列表”. 外层的 <code>IList</code> 表示最终结果是一个列表, 而这个列表中的每一个元素 (<code>IList&lt;int&gt;</code>) 本身也是一个整数列表, 用来存放我们找到的每个三元组, 例如 <code>[-1, 0, 1]</code>.</li></ul><ol start="2"><li><strong><code>Array.Sort(nums);</code></strong></li></ol><ul><li>这是.NET标准库中 <code>System.Array</code> 类提供的一个静态方法. 它会对传入的数组 <code>nums</code> 进行 <strong>原地排序</strong> (in-place sort), 也就是说它会直接修改 <code>nums</code> 数组本身, 而不是返回一个排好序的新数组. 默认是升序排序.</li><li>C#的 <code>Array.Sort</code> 内部实现很复杂, 是一种名为 IntroSort 的混合排序算法, 但其平均和最坏情况下的时间复杂度都可以视为 O(n log n).</li></ul><hr><h3 id="知识点总结">知识点总结:</h3><ol><li><strong>降维思想:</strong> 这是解决复杂问题的一个核心策略. 当我们面对一个看似需要多层循环 (如三层, 四层) 的问题时, 优先思考是否能通过 “固定” 其中一个或多个变量, 将问题转化为一个更低维度, 我们更熟悉的问题. “三数之和” 通过固定一个数, 巧妙地变成了 “两数之和”. 这个思想是算法优化的金钥匙.</li><li><strong>预处理的重要性:</strong> <code>Array.Sort()</code> 就是一个典型的预处理步骤. 有时, 在正式解决问题前, 花费一些时间对数据进行排序, 建立索引 (如哈希表), 或者进行其他形式的规整, 会为后续的算法执行创造极大的便利, 从而获得数量级上的性能提升. “先花一点时间磨刀, 可以大大缩短砍柴的时间”.</li><li><strong>双指针算法模式:</strong> 对于一个 <strong>有序数组</strong>, 使用一头一尾两个指针相向而行, 是一个极其高效的模式. 它能将嵌套循环的 O(n²) 复杂度降低到线性 O(n). 这个模式广泛应用于查找特定和的数对, 反转数组, 去重等场景.</li><li><strong>时间复杂度权衡:</strong> 优秀的工程师需要具备分析和权衡不同方案成本的能力. 在本例中, 我们接受了排序带来的 O(n log n) 成本, 是因为它帮助我们将查找成本从 O(n²) 降到了 O(n), 最终使得总复杂度从 O(n³) 优化到了 O(n²). 在评估一个多步骤算法的性能时, 要抓住其中的 <strong>主导项 (dominant term)</strong>.</li></ol><hr><h2 id="第16题-最接近的三数之和-难度-中等">第16题: 最接近的三数之和 (难度:中等)</h2><h3 id="原题-2">原题:</h3><p>给你一个长度为 <code>n</code> 的整数数组 <code>nums</code> 和 一个目标值 <code>target</code>。请你从 <code>nums</code> 中选出三个整数，使它们的和与 <code>target</code> 最接近。</p><p>返回这三个数的和。</p><p>假定每组输入只存在恰好一个解。</p><p><strong>示例 1：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入：nums = [-1,2,1,-4], target = 1</span><br><span class="line">输出：2</span><br><span class="line">解释：与 target 最接近的和是 2 (-1 + 2 + 1 = 2)。</span><br></pre></td></tr></table></figure><p><strong>示例 2：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入：nums = [0,0,0], target = 1</span><br><span class="line">输出：0</span><br><span class="line">解释：与 target 最接近的和是 0（0 + 0 + 0 = 0）。</span><br></pre></td></tr></table></figure><p><strong>提示：</strong></p><ul><li><code>3 &lt;= nums.length &lt;= 1000</code></li><li><code>-1000 &lt;= nums[i] &lt;= 1000</code></li><li><code>-104 &lt;= target &lt;= 104</code></li></ul><hr><h3 id="开始解题">开始解题:</h3><p>那么这道题和上一道是十分相似, 唯一有变化的就是我们要判断的内容了, 在双指针移动的每一步, 我们都需要检查当前的 <code>sum</code> 是否比我们之前记录的&quot;最接近的和&quot;(我们用 <code>result</code> 来存储)更接近 <code>target</code>.</p><ul><li><strong>比较距离:</strong> 我们不直接比较 <code>sum</code> 和 <code>result</code>, 而是比较它们与 <code>target</code> 的&quot;距离&quot;. 这个距离就是差值的绝对值: <code>Math.Abs(sum - target)</code>.</li><li><strong>更新:</strong> 如果 <code>Math.Abs(sum - target)</code> 小于 <code>Math.Abs(result- target)</code>, 说明我们找到了一个更好的答案, 此时就更新 <code>result = sum</code>.</li></ul><hr><h3 id="直接上代码">直接上代码:</h3><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Solution</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">int</span> <span class="title">ThreeSumClosest</span>(<span class="params"><span class="built_in">int</span>[] nums, <span class="built_in">int</span> target</span>)</span> &#123;</span><br><span class="line">        <span class="comment">// 1. 对数组进行排序, 这是双指针算法的前提.</span></span><br><span class="line">        Array.Sort(nums);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 初始化一个结果变量. 使用数组前三个数的和作为初始值,</span></span><br><span class="line">        <span class="built_in">int</span> result = nums[<span class="number">0</span>] + nums[<span class="number">1</span>] + nums[<span class="number">2</span>];</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 遍历数组, 固定第一个数 nums[i].</span></span><br><span class="line">        <span class="comment">// 循环到倒数第三个数即可, 因为我们需要为 left 和 right 指针留出位置.</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">0</span>; i &lt; nums.Length - <span class="number">2</span>; i++) &#123;</span><br><span class="line">            <span class="comment">// 4. 跳过重复的起始元素.</span></span><br><span class="line">            <span class="comment">// 如果当前的元素和前一个相同, 那么以它为起点的搜索会和之前的重复, 直接跳过.</span></span><br><span class="line">            <span class="keyword">if</span> (i &gt; <span class="number">0</span> &amp;&amp; nums[i] == nums[i - <span class="number">1</span>]) &#123;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 5. 初始化左右指针.</span></span><br><span class="line">            <span class="built_in">int</span> left = i + <span class="number">1</span>;</span><br><span class="line">            <span class="built_in">int</span> right = nums.Length - <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 6. 开始双指针循环.</span></span><br><span class="line">            <span class="keyword">while</span> (left &lt; right) &#123;</span><br><span class="line">                <span class="built_in">int</span> sum = nums[i] + nums[left] + nums[right];</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 7. 检查当前的和是否比已记录的结果更接近目标.</span></span><br><span class="line">                <span class="keyword">if</span> (Math.Abs(sum - target) &lt; Math.Abs(result - target)) &#123;</span><br><span class="line">                    result = sum;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 8. 根据 sum 和 target 的大小关系移动指针.</span></span><br><span class="line">                <span class="keyword">if</span> (sum &lt; target) &#123;</span><br><span class="line">                    <span class="comment">// 和太小, 需要增大, 所以移动左指针.</span></span><br><span class="line">                    left++;</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (sum &gt; target) &#123;</span><br><span class="line">                    <span class="comment">// 和太大, 需要减小, 所以移动右指针.</span></span><br><span class="line">                    right--;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// 和与目标完全相等, 已经是&quot;最接近&quot;(距离为0), 直接返回.</span></span><br><span class="line">                    <span class="keyword">return</span> sum;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 9. 循环结束后, 返回记录的最接近的和.</span></span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>但是这个代码在力扣上的表现我并不满意</p><p><img src="https://image.nfasystem.top/img-nfa/2025%2F08%2Fe0acf0abc733f20d4cefc818c81d2f62.ext.png" alt="image-20250805190637931"></p><p>所以我们要继续进行优化.</p><p>我们当前的 <code>O(n²)</code> 方案在理论上已经是该问题的最优解法. 所以, 优化的空间在于<strong>剪枝 (Pruning)</strong>, 即在循环过程中, 提前识别出那些不可能产生更优解的分支, 并直接跳过它们.</p><h4 id="优化的核心-剪枝策略">优化的核心: 剪枝策略</h4><h5 id="优化点-1-对外层循环进行剪枝-最重要">优化点 1: 对外层循环进行剪枝 (最重要)</h5><p><strong>逻辑:</strong><br>当我们固定了 <code>nums[i]</code> 后, 我们能构成的最小的和是 <code>nums[i] + nums[i+1] + nums[i+2]</code>.(因为我们已经排序)<br>如果这个<strong>可能的最小和</strong>都已经比我们当前找到的 <code>result</code> 更远离 <code>target</code> (并且是在 <code>target</code> 的右侧), 那么后续的 <code>i</code> (只会让这个最小和变得更大) 就完全没有必要再检查了. 我们可以直接 <code>break</code> 掉整个外层循环.</p><h5 id="优化点-2-对内层循环进行剪枝-处理重复元素">优化点 2: 对内层循环进行剪枝 (处理重复元素)</h5><p><strong>逻辑:</strong><br>在 <code>while</code> 循环中, 当我们移动了 <code>left</code> 或 <code>right</code> 指针后, 如果新位置的元素和旧位置的元素相同, 那么计算出的 <code>sum</code> 也将是相同的, 这是一次无效的重复计算. 我们应该跳过所有这些重复的元素.</p><p><strong>补充:</strong></p><p>那么这个其实在上一道题里用到了, 不过为什么我在这道题里没用到呢, 原因很简单, 我放错位置, 然后报错了, 然后我就直接删除了.</p><p><strong>那么我们最后的代码就是这样的了:</strong></p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Solution</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">int</span> <span class="title">ThreeSumClosest</span>(<span class="params"><span class="built_in">int</span>[] nums, <span class="built_in">int</span> target</span>)</span> &#123;</span><br><span class="line">        Array.Sort(nums);</span><br><span class="line">        <span class="built_in">int</span> result = nums[<span class="number">0</span>] + nums[<span class="number">1</span>] + nums[<span class="number">2</span>];</span><br><span class="line">        <span class="built_in">int</span> minDiff = Math.Abs(result - target);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">0</span>; i &lt; nums.Length - <span class="number">2</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (i &gt; <span class="number">0</span> &amp;&amp; nums[i] == nums[i - <span class="number">1</span>]) &#123;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 【优化1】: 剪枝, 如果最小可能和都比当前最优解差, 直接退出</span></span><br><span class="line">            <span class="built_in">int</span> minPossibleSum = nums[i] + nums[i + <span class="number">1</span>] + nums[i + <span class="number">2</span>];</span><br><span class="line">            <span class="keyword">if</span> (minPossibleSum &gt; target &amp;&amp; minPossibleSum - target &gt;= minDiff) &#123;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">          </span><br><span class="line">            <span class="comment">// 【优化1.1】: 剪枝, 如果最大可能和都比当前最优解差, 跳过本次循环</span></span><br><span class="line">            <span class="built_in">int</span> maxPossibleSum = nums[i] + nums[nums.Length - <span class="number">2</span>] + nums[nums.Length - <span class="number">1</span>];</span><br><span class="line">            <span class="keyword">if</span> (maxPossibleSum &lt; target &amp;&amp; target - maxPossibleSum &gt;= minDiff) &#123;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="built_in">int</span> left = i + <span class="number">1</span>;</span><br><span class="line">            <span class="built_in">int</span> right = nums.Length - <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">while</span> (left &lt; right) &#123;</span><br><span class="line">                <span class="built_in">int</span> sum = nums[i] + nums[left] + nums[right];</span><br><span class="line">                <span class="built_in">int</span> diff = Math.Abs(sum - target);</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (diff &lt; minDiff) &#123;</span><br><span class="line">                    minDiff = diff;</span><br><span class="line">                    result = sum;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (sum &lt; target) &#123;</span><br><span class="line">                    left++;</span><br><span class="line">                    <span class="comment">// 【优化2】: 跳过内部重复</span></span><br><span class="line">                    <span class="keyword">while</span> (left &lt; right &amp;&amp; nums[left] == nums[left - <span class="number">1</span>]) left++;</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (sum &gt; target) &#123;</span><br><span class="line">                    right--;</span><br><span class="line">                    <span class="comment">// 【优化2】: 跳过内部重复</span></span><br><span class="line">                    <span class="keyword">while</span> (left &lt; right &amp;&amp; nums[right] == nums[right + <span class="number">1</span>]) right--;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="keyword">return</span> sum; <span class="comment">// 距离为0, 最优解</span></span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><img src="https://image.nfasystem.top/img-nfa/2025%2F08%2F0efa8d7542347963b20c441064b1c98a.ext.png" alt="image-20250805191249985"></p><p>现在的质量已经很好了, 我们不再进行研究了, 因为我们不为了算法竞赛, 而是为了实际应用.</p><hr><h3 id="知识点总结-2">知识点总结:</h3><p><strong>剪枝优化</strong></p><ul><li><strong>核心思想:</strong> 在搜索或遍历的过程中, 利用已知信息(如数组的有序性), 提前判断某些分支或路径不可能产生最优解, 从而&quot;剪掉&quot;这些分支, 避免无效的计算.</li><li><strong>启示:</strong> 优化不仅仅是降低算法的时间复杂度, 也包括在同等复杂度下减少实际的运算量. &quot;剪枝&quot;是性能优化的关键技巧, 特别是在处理大数据集或复杂搜索问题(如AI, 游戏寻路)时.</li></ul>]]></content>
      
      
      <categories>
          
          <category> 力扣刷题 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 算法 </tag>
            
            <tag> 力扣 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>力扣刷题日常(13-14)</title>
      <link href="/posts/3da49ae6/"/>
      <url>/posts/3da49ae6/</url>
      
        <content type="html"><![CDATA[<h1>力扣刷题日常(13-14)</h1><h2 id="第13题-罗马数字转整数-难度-简单">第13题: 罗马数字转整数 (难度:简单)</h2><h3 id="原题">原题:</h3><p>罗马数字包含以下七种字符: <code>I</code>， <code>V</code>， <code>X</code>， <code>L</code>，<code>C</code>，<code>D</code> 和 <code>M</code>。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">字符          数值</span><br><span class="line">I             1</span><br><span class="line">V             5</span><br><span class="line">X             10</span><br><span class="line">L             50</span><br><span class="line">C             100</span><br><span class="line">D             500</span><br><span class="line">M             1000</span><br></pre></td></tr></table></figure><p>例如， 罗马数字 <code>2</code> 写做 <code>II</code> ，即为两个并列的 1 。<code>12</code> 写做 <code>XII</code> ，即为 <code>X</code> + <code>II</code> 。 <code>27</code> 写做 <code>XXVII</code>, 即为 <code>XX</code> + <code>V</code> + <code>II</code> 。</p><p>通常情况下，罗马数字中小的数字在大的数字的右边。但也存在特例，例如 4 不写做 <code>IIII</code>，而是 <code>IV</code>。数字 1 在数字 5 的左边，所表示的数等于大数 5 减小数 1 得到的数值 4 。同样地，数字 9 表示为 <code>IX</code>。这个特殊的规则只适用于以下六种情况：</p><ul><li><code>I</code> 可以放在 <code>V</code> (5) 和 <code>X</code> (10) 的左边，来表示 4 和 9。</li><li><code>X</code> 可以放在 <code>L</code> (50) 和 <code>C</code> (100) 的左边，来表示 40 和 90。</li><li><code>C</code> 可以放在 <code>D</code> (500) 和 <code>M</code> (1000) 的左边，来表示 400 和 900。</li></ul><p>给定一个罗马数字，将其转换成整数。</p><p><strong>示例 1:</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入: s = &quot;III&quot;</span><br><span class="line">输出: 3</span><br></pre></td></tr></table></figure><p><strong>示例 2:</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入: s = &quot;IV&quot;</span><br><span class="line">输出: 4</span><br></pre></td></tr></table></figure><p><strong>示例 3:</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入: s = &quot;IX&quot;</span><br><span class="line">输出: 9</span><br></pre></td></tr></table></figure><p><strong>示例 4:</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入: s = &quot;LVIII&quot;</span><br><span class="line">输出: 58</span><br><span class="line">解释: L = 50, V= 5, III = 3.</span><br></pre></td></tr></table></figure><p><strong>示例 5:</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入: s = &quot;MCMXCIV&quot;</span><br><span class="line">输出: 1994</span><br><span class="line">解释: M = 1000, CM = 900, XC = 90, IV = 4.</span><br></pre></td></tr></table></figure><p><strong>提示：</strong></p><ul><li><code>1 &lt;= s.length &lt;= 15</code></li><li><code>s</code> 仅含字符 <code>('I', 'V', 'X', 'L', 'C', 'D', 'M')</code></li><li>题目数据保证 <code>s</code> 是一个有效的罗马数字，且表示整数在范围 <code>[1, 3999]</code> 内</li><li>题目所给测试用例皆符合罗马数字书写规则，不会出现跨位等情况。</li><li>IL 和 IM 这样的例子并不符合题目要求，49 应该写作 XLIX，999 应该写作 CMXCIX 。</li><li>关于罗马数字的详尽书写规则，可以参考 <a href="https://baike.baidu.com/item/%E7%BD%97%E9%A9%AC%E6%95%B0%E5%AD%97/772296">罗马数字 - 百度百科</a>。</li></ul><hr><h3 id="开始解题">开始解题</h3><p>那么这个题与昨天的12题十分相似,就是逆向操作</p><p>那么将罗马数字转换为整数,核心在于如何处理那六种特殊的&quot;减法&quot;情况 (如 <code>IV</code> 是 4, <code>CM</code> 是 900). 如果没有这些特殊情况, 我们只需要把每个罗马字符代表的数字加起来就行了.</p><p>一个非常巧妙且直观的思路是<strong>从右向左</strong>遍历这个罗马数字字符串. 为什么从右向左呢? 因为一个字符是应该被加还是被减, 取决于它<strong>右边</strong>的字符.</p><p>让我们来构思一下这个从右到左的逻辑:</p><ol><li><p><strong>建立映射</strong>: 首先, 我们需要一个能快速查询到每个罗马字符对应整数值的方法. 比如, 输入 ‘V’, 就能立刻得到 5. 在编程中, 这通常用一个<strong>哈希表 (Dictionary)</strong> 或一个 <code>switch</code> 语句来实现.</p></li><li><p><strong>处理最后一个字符</strong>: 罗马数字最右边的那个字符, 它的右边没有其他字符了, 所以它永远代表的是<strong>加法</strong>. 我们可以用它的值作为我们计算的初始值.</p></li><li><p><strong>从右向左遍历</strong>: 我们从字符串的<strong>倒数第二个</strong>字符开始, 一直遍历到第一个字符. 在每一步, 我们都将当前字符的值与它右边一个字符的值进行比较.</p></li><li><p><strong>判断与操作</strong>: 假设我们当前遍历到的字符是 <code>current</code>, 它右边的字符是 <code>right</code>.</p><ul><li><strong>如果 <code>current</code> 的值大于或等于 <code>right</code> 的值</strong>: 这说明是一个正常的, 从大到小的排列 (例如 <code>VI</code> 中的 ‘V’, <code>XI</code> 中的 ‘X’). 这种情况下, 我们就将 <code>current</code> 的值<strong>加上</strong>去.</li><li><strong>如果 <code>current</code> 的值小于 <code>right</code> 的值</strong>: 这就命中了我们的特殊&quot;减法&quot;规则 (例如 <code>IV</code> 中的 ‘I’, <code>IX</code> 中的 ‘I’). 这种情况下, 我们就将 <code>current</code> 的值<strong>减掉</strong>.</li></ul></li></ol><p>让我们用 “MCMXCIV” 这个例子来走一遍这个流程:</p><ul><li><strong>输入</strong>: “MCMXCIV”</li><li><strong>1. 初始化</strong>: 先看最右边的 ‘V’. 它的值是 5. 我们的总和 <code>result</code> 初始化为 5.</li><li><strong>2. 向左移动</strong>: 看 ‘V’ 左边的 ‘I’.<ul><li>‘I’ 的值是 1. 它右边 ‘V’ 的值是 5.</li><li>因为 <code>1 &lt; 5</code>, 所以执行减法. <code>result = 5 - 1 = 4</code>.</li></ul></li><li><strong>3. 向左移动</strong>: 看 ‘I’ 左边的 ‘C’.<ul><li>‘C’ 的值是 100. 它右边 ‘I’ 的值是 1.</li><li>因为 <code>100 &gt; 1</code>, 所以执行加法. <code>result = 4 + 100 = 104</code>.</li></ul></li><li><strong>4. 向左移动</strong>: 看 ‘C’ 左边的 ‘X’.<ul><li>‘X’ 的值是 10. 它右边 ‘C’ 的值是 100.</li><li>因为 <code>10 &lt; 100</code>, 所以执行减法. <code>result = 104 - 10 = 94</code>.</li></ul></li><li><strong>5. 向左移动</strong>: 看 ‘X’ 左边的 ‘M’.<ul><li>‘M’ 的值是 1000. 它右边 ‘X’ 的值是 10.</li><li>因为 <code>1000 &gt; 10</code>, 所以执行加法. <code>result = 94 + 1000 = 1094</code>.</li></ul></li><li><strong>6. 向左移动</strong>: 看 ‘M’ 左边的 ‘C’.<ul><li>‘C’ 的值是 100. 它右边 ‘M’ 的值是 1000.</li><li>因为 <code>100 &lt; 1000</code>, 所以执行减法. <code>result = 1094 - 100 = 994</code>.</li></ul></li><li><strong>7. 向左移动</strong>: 看 ‘C’ 左边的 ‘M’.<ul><li>‘M’ 的值是 1000. 它右边 ‘C’ 的值是 100.</li><li>因为 <code>1000 &gt; 100</code>, 所以执行加法. <code>result = 994 + 1000 = 1994</code>.</li></ul></li><li><strong>8. 结束</strong>: 遍历完成, 最终结果是 1994.</li></ul><hr><h3 id="代码">代码:</h3><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Solution</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">int</span> <span class="title">RomanToInt</span>(<span class="params"><span class="built_in">string</span> s</span>)</span> &#123;</span><br><span class="line">        <span class="comment">// 1. 使用 Dictionary 建立字符到数值的映射</span></span><br><span class="line">        Dictionary&lt;<span class="built_in">char</span>,<span class="built_in">int</span>&gt; rome = <span class="keyword">new</span> Dictionary&lt;<span class="built_in">char</span>,<span class="built_in">int</span>&gt;</span><br><span class="line">        &#123;</span><br><span class="line">            &#123;<span class="string">&#x27;I&#x27;</span>, <span class="number">1</span>&#125;,</span><br><span class="line">            &#123;<span class="string">&#x27;V&#x27;</span>, <span class="number">5</span>&#125;,</span><br><span class="line">            &#123;<span class="string">&#x27;X&#x27;</span>, <span class="number">10</span>&#125;,</span><br><span class="line">            &#123;<span class="string">&#x27;L&#x27;</span>, <span class="number">50</span>&#125;,</span><br><span class="line">            &#123;<span class="string">&#x27;C&#x27;</span>, <span class="number">100</span>&#125;,</span><br><span class="line">            &#123;<span class="string">&#x27;D&#x27;</span>, <span class="number">500</span>&#125;,</span><br><span class="line">            &#123;<span class="string">&#x27;M&#x27;</span>, <span class="number">1000</span>&#125;</span><br><span class="line">        &#125;;</span><br><span class="line">      </span><br><span class="line">        <span class="built_in">int</span> result = <span class="number">0</span>;</span><br><span class="line">        <span class="comment">// 2. 从右向左遍历, 先处理最右边的值</span></span><br><span class="line">        <span class="built_in">char</span> rightChar = s[s.Length - <span class="number">1</span>];</span><br><span class="line">        <span class="built_in">int</span> rightValue = rome[rightChar];</span><br><span class="line">        result = rightValue;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 从倒数第二个字符开始向左循环</span></span><br><span class="line">        <span class="keyword">for</span>(<span class="built_in">int</span> i = s.Length - <span class="number">2</span>; i &gt;= <span class="number">0</span>; i --)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">char</span> currentChar = s[i];</span><br><span class="line">            <span class="built_in">int</span> currentValue = rome[currentChar];</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 4. 核心逻辑: 与右边的值比较</span></span><br><span class="line">            <span class="keyword">if</span>(currentValue &lt; rightValue)</span><br><span class="line">            &#123;</span><br><span class="line">                result -= currentValue; <span class="comment">// 小于则减</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">            &#123;</span><br><span class="line">                result += currentValue; <span class="comment">// 大于或等于则加</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 5. 更新 rightValue, 为下一次循环做准备</span></span><br><span class="line">            rightValue = currentValue;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h3 id="可能的实际应用">可能的实际应用:</h3><ol><li><strong><code>Dictionary</code> / <code>switch</code> (键值映射):</strong><ul><li><strong>输入系统:</strong> 在Unity中, 我们可以使用 <code>Dictionary&lt;KeyCode, System.Action&gt;</code> 来构建一个灵活的按键绑定系统, 按下某个键时, 执行对应的委托(Action).</li><li><strong>本地化:</strong> 游戏需要支持多国语言时, 可以用 <code>Dictionary&lt;string, string&gt;</code> 来存储文本的键(如 “MENU_START”)和对应语言的值(如 “开始游戏”).</li><li><strong>对象池:</strong> 使用 <code>Dictionary&lt;string, Queue&lt;GameObject&gt;&gt;</code> 来管理不同种类的预制体对象池, 通过预制体的名字(string)来获取对应的对象队列.</li></ul></li><li><strong>循环逻辑 (从右到左 vs. 向前看):</strong><ul><li><strong>UI布局:</strong> 在实现一个从右向左排列的UI元素列表(比如阿拉伯语国家的UI)时, 我们就需要使用反向循环来计算每个元素的位置.</li><li><strong>状态效果结算:</strong> 在一个回合制游戏中, 结算玩家身上的Buff/Debuff时, 某些效果的计算可能依赖于它后面的效果(比如一个&quot;伤害加深&quot;的debuff需要先于伤害技能结算). 这时就需要精心设计遍历和结算的顺序.</li></ul></li><li><strong>代码权衡 (可读性 vs. 效率):</strong><ul><li><strong>Shader编写:</strong> 在写Shader时, 这是一个非常经典的权衡. 我们可以使用一些数学技巧让光照计算更快, 但这会让Shader代码非常难懂.</li><li><strong>ECS (Entity Component System):</strong> 在Unity的DOTS中, 我们写的System代码非常注重性能, 因为它会处理成千上万个实体. 在这种场景下, 性能的优先级会高于常规代码的可读性. 但即便如此, 良好的命名和注释依然至关重要.</li></ul></li></ol><hr><h2 id="第14题-最长公共前缀-难度-简单">第14题: 最长公共前缀 (难度:简单)</h2><h3 id="原题-2">原题:</h3><p>编写一个函数来查找字符串数组中的最长公共前缀。</p><p>如果不存在公共前缀，返回空字符串 <code>&quot;&quot;</code>。</p><p><strong>示例 1：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入：strs = [&quot;flower&quot;,&quot;flow&quot;,&quot;flight&quot;]</span><br><span class="line">输出：&quot;fl&quot;</span><br></pre></td></tr></table></figure><p><strong>示例 2：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入：strs = [&quot;dog&quot;,&quot;racecar&quot;,&quot;car&quot;]</span><br><span class="line">输出：&quot;&quot;</span><br><span class="line">解释：输入不存在公共前缀。</span><br></pre></td></tr></table></figure><p><strong>提示：</strong></p><ul><li><code>1 &lt;= strs.length &lt;= 200</code></li><li><code>0 &lt;= strs[i].length &lt;= 200</code></li><li><code>strs[i]</code> 如果非空，则仅由小写英文字母组成</li></ul><hr><h3 id="开始解题-2">开始解题:</h3><h4 id="算法实现逻辑-纵向扫描">算法实现逻辑: 纵向扫描</h4><p>这个方法非常直观, 就像我们用眼睛逐列对比文本一样. 它的核心思想是:</p><ol><li><p><strong>选定一个基准:</strong> 我们不需要两两比较所有字符串, 这样太复杂了. 我们可以直接选择数组中的第一个字符串 (例如 <code>strs[0]</code>) 作为我们的 “基准” 或者 “模板”. 最终的最长公共前缀, 长度绝对不可能超过这个基准字符串的长度.</p></li><li><p><strong>逐个字符进行纵向对比:</strong> 我们从基准字符串的第一个字符开始 (索引为0), 然后拿着这个字符去和数组中 <em>所有其他</em> 字符串在 <em>相同位置</em> 的字符做比较.</p><ul><li>如果所有字符串在当前位置的字符都和基准字符相同, 那么说明这个字符是公共前缀的一部分. 我们就继续去检查下一个位置的字符.</li><li>如果在这个过程中, 遇到了任何一个字符串在当前位置的字符与基准字符不同, 或者某个字符串的长度已经不够长了(比如我们要检查第5个字符, 但某个字符串总共只有4个字符), 那么比较就此终止.</li></ul></li><li><p><strong>确定最终结果:</strong></p><ul><li>一旦比较终止, 那么最长公共前缀就是基准字符串从开头到 <em>上一个</em> 成功比较的位置的子串.</li><li>如果整个基准字符串的所有字符都被成功比较完了, 那么说明整个基准字符串本身就是最长公共前缀.</li></ul></li></ol><h4 id="让我们用示例-flower-flow-flight-来走一遍流程">让我们用示例 <code>[&quot;flower&quot;, &quot;flow&quot;, &quot;flight&quot;]</code> 来走一遍流程:</h4><ol><li><p><strong>选择基准:</strong> 我们选择 <code>&quot;flower&quot;</code> 作为基准.</p></li><li><p><strong>开始纵向扫描:</strong></p><ul><li><p><strong>第1列 (索引 0):</strong></p><ul><li>基准字符是 <code>'f'</code>.</li><li>检查 <code>&quot;flow&quot;</code> 的第1个字符, 是 <code>'f'</code>. 匹配.</li><li>检查 <code>&quot;flight&quot;</code> 的第1个字符, 是 <code>'f'</code>. 匹配.</li><li>结论: 所有字符串的第一列都是 <code>'f'</code>, <code>'f'</code> 是公共前缀的一部分. 继续.</li></ul></li><li><p><strong>第2列 (索引 1):</strong></p><ul><li>基准字符是 <code>'l'</code>.</li><li>检查 <code>&quot;flow&quot;</code> 的第2个字符, 是 <code>'l'</code>. 匹配.</li><li>检查 <code>&quot;flight&quot;</code> 的第2个字符, 是 <code>'l'</code>. 匹配.</li><li>结论: 所有字符串的第二列都是 <code>'l'</code>, <code>'l'</code> 是公共前缀的一部分. 继续.</li></ul></li><li><p><strong>第3列 (索引 2):</strong></p><ul><li>基准字符是 <code>'o'</code>.</li><li>检查 <code>&quot;flow&quot;</code> 的第3个字符, 是 <code>'o'</code>. 匹配.</li><li>检查 <code>&quot;flight&quot;</code> 的第3个字符, 是 <code>'i'</code>. <strong>不匹配!</strong></li></ul></li></ul></li><li><p><strong>得出结果:</strong></p><ul><li>我们在索引为 <code>2</code> 的位置发现了不匹配. 这意味着最长公共前缀的长度就是 <code>2</code>.</li><li>我们截取基准字符串 <code>&quot;flower&quot;</code> 从索引 <code>0</code> 开始, 长度为 <code>2</code> 的部分.</li><li>结果就是 <code>&quot;fl&quot;</code>.</li></ul></li></ol><p>这个方法的优点是, 一旦发现不匹配, 算法会立刻终止并返回结果, 在公共前缀很短的情况下效率非常高.</p><hr><h3 id="代码-2">代码:</h3><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Solution</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">string</span> <span class="title">LongestCommonPrefix</span>(<span class="params"><span class="built_in">string</span>[] strs</span>)</span> &#123;</span><br><span class="line">        <span class="comment">// 1. 处理边界情况: 如果数组为null或为空, 直接返回空字符串.实际上题目的条件已经给出范围了.</span></span><br><span class="line">        <span class="keyword">if</span> (strs == <span class="literal">null</span> || strs.Length == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">&quot;&quot;</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 2. 外层循环遍历第一个字符串(基准)的每一个字符.</span></span><br><span class="line">        <span class="comment">// i 代表当前正在比较的字符列的索引.</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">0</span>; i &lt; strs[<span class="number">0</span>].Length; i++) &#123;</span><br><span class="line">            <span class="comment">// 3. 取出基准字符串在当前列的字符.</span></span><br><span class="line">            <span class="built_in">char</span> c = strs[<span class="number">0</span>][i];</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 4. 内层循环遍历数组中剩余的字符串(从第二个开始).</span></span><br><span class="line">            <span class="comment">// j 代表当前正在比较的字符串的索引.</span></span><br><span class="line">            <span class="keyword">for</span> (<span class="built_in">int</span> j = <span class="number">1</span>; j &lt; strs.Length; j++) &#123;</span><br><span class="line">                <span class="comment">// 5. 检查两种失败情况:</span></span><br><span class="line">                <span class="comment">// a) 当前比较的字符串长度已经不够了 (i == strs[j].Length).</span></span><br><span class="line">                <span class="comment">// b) 当前列的字符不匹配 (strs[j][i] != c).</span></span><br><span class="line">                <span class="keyword">if</span> (i == strs[j].Length || strs[j][i] != c) &#123;</span><br><span class="line">                    <span class="comment">// 6. 一旦失败, 说明公共前缀到此为止.</span></span><br><span class="line">                    <span class="comment">// 截取基准字符串从0到i的部分并返回.</span></span><br><span class="line">                    <span class="keyword">return</span> strs[<span class="number">0</span>].Substring(<span class="number">0</span>, i);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 7. 如果所有循环都正常结束, 说明第一个字符串本身就是最长公共前缀.</span></span><br><span class="line">        <span class="keyword">return</span> strs[<span class="number">0</span>];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr>]]></content>
      
      
      <categories>
          
          <category> 力扣刷题 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 算法 </tag>
            
            <tag> 力扣 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>力扣刷题日常(11-12)</title>
      <link href="/posts/113e6e00/"/>
      <url>/posts/113e6e00/</url>
      
        <content type="html"><![CDATA[<h1>力扣刷题日常(11-12)</h1><h2 id="第11题-盛最多水的容器-难度-中等">第11题:盛最多水的容器 (难度:中等)</h2><h3 id="原题">原题:</h3><p>给定一个长度为 <code>n</code> 的整数数组 <code>height</code> 。有 <code>n</code> 条垂线，第 <code>i</code> 条线的两个端点是 <code>(i, 0)</code> 和 <code>(i, height[i])</code> 。</p><p>找出其中的两条线，使得它们与 <code>x</code> 轴共同构成的容器可以容纳最多的水。</p><p>返回容器可以储存的最大水量。</p><p>**说明：**我们不能倾斜容器。</p><p><strong>示例 1：</strong></p><p><img src="https://image.nfasystem.top/img-nfa/2025%2F08%2F9daebb6ebbdb925763fbd31e9a7aa329.ext.jpeg" alt="img"></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入：[1,8,6,2,5,4,8,3,7]</span><br><span class="line">输出：49 </span><br><span class="line">解释：图中垂直线代表输入数组 [1,8,6,2,5,4,8,3,7]。在此情况下，容器能够容纳水（表示为蓝色部分）的最大值为 49。</span><br></pre></td></tr></table></figure><p><strong>示例 2：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入：height = [1,1]</span><br><span class="line">输出：1</span><br></pre></td></tr></table></figure><p><strong>提示：</strong></p><ul><li><code>n == height.length</code></li><li><code>2 &lt;= n &lt;= 105</code></li><li><code>0 &lt;= height[i] &lt;= 104</code></li></ul><h3 id="原题提示：">原题提示：</h3><ol><li>If you simulate the problem, it will be O(n^2) which is not efficient.(如果我们模拟这个问题，其时间复杂度将是 O(n²)，这效率不高。)</li><li>Try to use two-pointers. Set one pointer to the left and one to the right of the array. Always move the pointer that points to the lower line.(尽量使用双指针。将一个指针置于数组的左侧，另一个置于右侧。始终移动指向较小值的指针。)</li><li>How can you calculate the amount of water at each step?(如何计算每一步的水量？)</li></ol><hr><h3 id="开始解题">开始解题:</h3><p>这道题的目标是找到两条垂直线,使其与x轴构成的容器能装最多的水.水的容量由两条线中较短的那条(木桶效应)和它们之间的距离共同决定.</p><ol><li><strong>暴力算法的思考</strong></li></ol><p>最直观的想法就是尝试所有可能的线段组合.我们可以用一个循环来固定第一条线<code>i</code>,然后用另一个嵌套的循环来遍历所有在<code>i</code>右边的线<code>j</code>.对于每一对<code>(i, j)</code>,我们都计算它们能构成的容器容量,然后用一个变量来记录遇到的最大容量.</p><ul><li>容量计算公式: <code>area = (j - i) * min(height[i], height[j])</code></li><li><code>j - i</code> 是容器的宽度.</li><li><code>min(height[i], height[j])</code> 是容器的高度,由较短的板决定.</li></ul><p>这种方法需要两层循环,时间复杂度是 O(n²).根据题目提示,当<code>n</code>达到10^5^时,n²大约是10^10^,这个计算量太大了,会导致超时.所以我们需要一个更高效的方法.</p><ol start="2"><li><strong>双指针法的优化思路</strong></li></ol><p>这正是题目提示我们要使用的核心技巧.双指针法是一种非常高效的线性扫描技巧,它能将时间复杂度降低到 O(n).</p><p>我们的核心思路如下:</p><ol><li><p><strong>初始化:</strong> 我们设置两个指针,一个<code>left</code>指向数组的起始位置(索引0),另一个<code>right</code>指向数组的末尾位置(索引<code>n-1</code>).这构成了一个初始的容器,这个容器的宽度是所有可能组合中最大的.</p></li><li><p><strong>迭代与移动:</strong> 我们在一个循环中持续计算当前<code>left</code>和<code>right</code>指针所构成容器的容量,并更新我们记录的最大容量.然后,关键问题来了: <strong>我们应该移动哪个指针? <code>left</code>向右移,还是<code>right</code>向左移?</strong></p><ul><li>让我们再次审视容量公式: <code>容量 = 宽度 * 高度</code>.</li><li>当前容器的宽度是 <code>right - left</code>, 高度是 <code>min(height[left], height[right])</code>.</li><li>当我们移动任何一个指针时,无论是<code>left++</code>还是<code>right--</code>,容器的<strong>宽度都必然会减小</strong>.</li><li>因此,要想找到一个比当前容量更大的新容器,我们<strong>唯一的希望就是让它的高度增加</strong>.</li></ul><p>现在我们来分析移动哪个指针:</p><ul><li>假设 <code>height[left]</code> &lt; <code>height[right]</code>. 此时容器的高度由 <code>height[left]</code> (较短的板)决定.</li><li>如果我们移动<code>right</code>指针(<code>right--</code>),新的宽度变小了,而新的高度 <code>min(height[left], height[right-1])</code> 不可能大于 <code>height[left]</code> (因为<code>height[left]</code>仍然是其中一个可能的限制).所以,新的容量<code>新宽度 * 新高度</code>必然小于当前的容量.移动<code>right</code>指针没有任何收益.</li><li>反之,如果我们移动<code>left</code>指针(<code>left++</code>),虽然宽度也变小了,但新的高度 <code>min(height[left+1], height[right])</code> <strong>有可能</strong>会大于旧的高度 <code>height[left]</code>.这就为我们找到了一个更大容量的容器提供了可能性.</li></ul></li></ol><p><strong>结论:</strong> 在每一步,我们都应该移动指向<strong>较短</strong>垂直线的那一个指针.</p><p><strong>算法步骤总结:</strong></p><ol><li>初始化 <code>left = 0</code>, <code>right = height.length - 1</code>, <code>maxArea = 0</code>.</li><li>当 <code>left &lt; right</code> 时,执行循环:<br>a. 计算当前宽度 <code>width = right - left</code>.<br>b. 确定当前高度 <code>h = min(height[left], height[right])</code>.<br>c. 计算当前容量 <code>currentArea = width * h</code>.<br>d. 更新最大容量 <code>maxArea = max(maxArea, currentArea)</code>.<br>e. 比较 <code>height[left]</code> 和 <code>height[right]</code>:<br>*   如果 <code>height[left] &lt; height[right]</code>, 则 <code>left++</code>.<br>*   否则, <code>right--</code>.</li><li>循环结束后,<code>maxArea</code> 就是最终结果.</li></ol><p>这种方法,<code>left</code>和<code>right</code>指针总共只会遍历整个数组一次,所以时间复杂度是 O(n),空间复杂度是 O(1).</p><hr><h3 id="代码">代码:</h3><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Solution</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">int</span> <span class="title">MaxArea</span>(<span class="params"><span class="built_in">int</span>[] height</span>)</span> &#123;</span><br><span class="line">        <span class="comment">// 1. 变量声明与初始化</span></span><br><span class="line">        <span class="built_in">int</span> left = <span class="number">0</span>;</span><br><span class="line">        <span class="built_in">int</span> right = height.Length - <span class="number">1</span>; <span class="comment">// 2. 数组属性</span></span><br><span class="line">        <span class="built_in">int</span> maxArea = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. while 循环</span></span><br><span class="line">        <span class="keyword">while</span>(left &lt; right)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">// 4. Math 静态类</span></span><br><span class="line">            <span class="built_in">int</span> h = Math.Min(height[left], height[right]);</span><br><span class="line">            <span class="built_in">int</span> width = right - left;</span><br><span class="line">            <span class="built_in">int</span> currentArea = width * h;</span><br><span class="line">            maxArea = Math.Max(maxArea, currentArea);</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 5. if-else 条件判断</span></span><br><span class="line">            <span class="keyword">if</span>(height[left] &lt; height[right])</span><br><span class="line">                left++; <span class="comment">// 6. 自增/自减运算符</span></span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                right--;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> maxArea;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h3 id="知识点">知识点:</h3><ol><li><p><strong>双指针技巧 (Two-Pointers Technique)</strong></p><ul><li>这是一种极其常用且高效的算法技巧,通常用于处理数组或序列问题.它通过维护两个指针在数据结构中移动来减少不必要的计算.</li><li><strong>主要类型:</strong><ul><li><strong>对撞指针(Colliding Pointers):</strong> 正如本题所用,一个指针在头,一个在尾,互相靠近.常用于查找满足特定条件的数对.</li><li><strong>快慢指针(Fast-Slow Pointers):</strong> 两个指针同向移动,但速度不同.常用于判断链表是否有环,或查找链表的中点.</li></ul></li><li>掌握双指针技巧能让我们用 O(n) 的时间解决很多看似需要 O(n²) 才能解决的问题.</li></ul></li><li><p><strong>贪心算法思想 (Greedy Algorithm Mindset)</strong></p><ul><li>贪心算法的核心是在每一步都做出当前看起来最优的选择,并期望通过一系列局部最优解,最终得到全局最优解.</li><li>在本题中,我们的&quot;贪心&quot;选择是: <strong>移动较短的那块板</strong>. 我们之所以敢这么做,是因为我们通过推理证明了保留短板并移动长板,绝不可能得到比当前更大的面积.因此,舍弃短板是当前唯一可能找到更优解的策略.</li><li><strong>重要提示:</strong> 贪心算法并不总是能得到全局最优解,它只适用于具有&quot;贪心选择性质&quot;和&quot;最优子结构&quot;的问题.但它是一种强大的问题解决思路.</li></ul></li></ol><hr><h3 id="练习题">练习题:</h3><h4 id="选择题">选择题</h4><p>对于本题的双指针解法,在 <code>while(left &lt; right)</code> 循环中,为什么我们总是选择移动指向较短垂直线的那个指针?</p><p>A. 因为这样可以保证容器的宽度(width)每次都只减少1.<br>B. 因为移动较长的指针可能会错过最优解,而移动较短的指针则不会.<br>C. 因为移动较短的指针,下一步才有可能找到一个更高的板来增高容器的高度,从而才有可能得到更大的面积.<br>D. 因为数组是从小到大排序的.</p><h4 id="简答题">简答题</h4><p>请简述暴力解法(O(n²))和双指针解法(O(n))在解决此问题时的主要区别,以及它们各自的优缺点.</p><hr><h3 id="参考答案">参考答案</h3><h4 id="选择题答案-C">选择题答案: C</h4><ul><li><strong>A 错误:</strong> 无论移动哪个指针,宽度 <code>right - left</code> 每次都减少1.</li><li><strong>B 错误:</strong> 这个说法是结论,但没有解释原因.为什么移动较长的指针会错过最优解?因为它无法让作为瓶颈的短板高度增加.</li><li><strong>C 正确:</strong> 当前容器的面积由 <code>宽度 * 短板高度</code> 决定.当移动指针时,宽度必然减小.要想让新面积有可能超过旧面积,唯一的方法就是期望容器的<strong>高度</strong>能增加.移动长板,新高度最多和旧短板一样高,面积必然减小.只有移动短板,才<strong>有可能</strong>遇到一个更高的板,使得新的高度增加,从而弥补宽度的损失,得到更大的面积.</li><li><strong>D 错误:</strong> 题目并未说明数组是排序的.</li></ul><h4 id="简答题答案">简答题答案</h4><ul><li><p><strong>主要区别:</strong></p><ul><li><strong>搜索方式:</strong> 暴力解法通过嵌套循环,检查了所有可能的线段组合,是一种无差别的穷举搜索.双指针解法则通过一个巧妙的策略(移动短板),在每一步都排除了大量不可能成为最优解的组合,是一种智能的、收缩范围的搜索.</li></ul></li><li><p><strong>优缺点:</strong></p><ul><li><strong>暴力解法:</strong><ul><li><strong>优点:</strong> 逻辑简单直观,容易理解和实现.</li><li><strong>缺点:</strong> 时间复杂度为 O(n²),效率极低,当数据规模<code>n</code>很大时,会导致程序超时或卡死.</li></ul></li><li><strong>双指针解法:</strong><ul><li><strong>优点:</strong> 时间复杂度为 O(n),空间复杂度为 O(1),非常高效,能够轻松处理大规模数据.</li><li><strong>缺点:</strong> 需要对问题进行更深入的分析才能发现其正确性,逻辑上比暴力解法稍复杂.</li></ul></li></ul></li></ul><hr><h2 id="第12题-整数转罗马数字-难度-中等">第12题: 整数转罗马数字 (难度: 中等)</h2><h3 id="原题：">原题：</h3><p>七个不同的符号代表罗马数字，其值如下：</p><table><thead><tr><th style="text-align:left">符号</th><th>值</th></tr></thead><tbody><tr><td style="text-align:left">I</td><td>1</td></tr><tr><td style="text-align:left">V</td><td>5</td></tr><tr><td style="text-align:left">X</td><td>10</td></tr><tr><td style="text-align:left">L</td><td>50</td></tr><tr><td style="text-align:left">C</td><td>100</td></tr><tr><td style="text-align:left">D</td><td>500</td></tr><tr><td style="text-align:left">M</td><td>1000</td></tr></tbody></table><p>罗马数字是通过添加从最高到最低的小数位值的转换而形成的。将小数位值转换为罗马数字有以下规则：</p><ul><li>如果该值不是以 4 或 9 开头，请选择可以从输入中减去的最大值的符号，将该符号附加到结果，减去其值，然后将其余部分转换为罗马数字。</li><li>如果该值以 4 或 9 开头，使用 <strong>减法形式</strong>，表示从以下符号中减去一个符号，例如 4 是 5 (<code>V</code>) 减 1 (<code>I</code>): <code>IV</code> ，9 是 10 (<code>X</code>) 减 1 (<code>I</code>)：<code>IX</code>。仅使用以下减法形式：4 (<code>IV</code>)，9 (<code>IX</code>)，40 (<code>XL</code>)，90 (<code>XC</code>)，400 (<code>CD</code>) 和 900 (<code>CM</code>)。</li><li>只有 10 的次方（<code>I</code>, <code>X</code>, <code>C</code>, <code>M</code>）最多可以连续附加 3 次以代表 10 的倍数。我们不能多次附加 5 (<code>V</code>)，50 (<code>L</code>) 或 500 (<code>D</code>)。如果需要将符号附加4次，请使用 <strong>减法形式</strong>。</li></ul><p>给定一个整数，将其转换为罗马数字。</p><p><strong>示例 1：</strong></p><p>**输入：**num = 3749</p><p><strong>输出：</strong> “MMMDCCXLIX”</p><p><strong>解释：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">3000 = MMM 由于 1000 (M) + 1000 (M) + 1000 (M)</span><br><span class="line"> 700 = DCC 由于 500 (D) + 100 (C) + 100 (C)</span><br><span class="line">  40 = XL 由于 50 (L) 减 10 (X)</span><br><span class="line">   9 = IX 由于 10 (X) 减 1 (I)</span><br><span class="line">注意：49 不是 50 (L) 减 1 (I) 因为转换是基于小数位</span><br></pre></td></tr></table></figure><p><strong>示例 2：</strong></p><p>**输入：**num = 58</p><p><strong>输出：</strong>“LVIII”</p><p><strong>解释：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">50 = L</span><br><span class="line"> 8 = VIII</span><br></pre></td></tr></table></figure><p><strong>示例 3：</strong></p><p>**输入：**num = 1994</p><p><strong>输出：</strong>“MCMXCIV”</p><p><strong>解释：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">1000 = M</span><br><span class="line"> 900 = CM</span><br><span class="line">  90 = XC</span><br><span class="line">   4 = IV</span><br></pre></td></tr></table></figure><p><strong>提示：</strong></p><ul><li><code>1 &lt;= num &lt;= 3999</code></li></ul><hr><h3 id="开始解题-2">开始解题:</h3><p>这个问题的核心思想是一种非常直观且强大的算法策略,叫做<strong>贪心算法</strong>.</p><p>想象一下我们在兑换零钱,比如要兑换37元,我们手头有20元,10元,5元,2元,1元的纸币.为了用最少的纸币,我们的直觉会告诉我们:</p><ol><li>先看看能不能用20元的?可以,用一张.还剩17元.</li><li>再看看能不能用20元的?不行了.试试10元的?可以,用一张.还剩7元.</li><li>再看看10元的?不行.试试5元的?可以,用一张.还剩2元.</li><li>再看看5元的?不行.试试2元的?可以,用一张.还剩0元.</li><li>兑换完成.</li></ol><p>这个过程就是贪心,即在每一步都做出当前看起来最好的选择.</p><p>对于整数转罗马数字,我们也可以采用完全相同的策略.关键在于,我们的&quot;钱币&quot;是什么?</p><p>题目给了我们基础的对应关系 (I=1, V=5, X=10等),但更重要的是,它指出了特殊的&quot;减法形式&quot;,比如 4 (IV), 9 (IX), 40 (XL), 90 (XC), 400 (CD), 900 (CM).</p><p>如果我们把这些特殊形式也看作是&quot;一体&quot;的符号,我们就可以得到一个从大到小的&quot;面值&quot;列表:</p><table><thead><tr><th style="text-align:left">值</th><th style="text-align:left">符号</th></tr></thead><tbody><tr><td style="text-align:left">1000</td><td style="text-align:left">M</td></tr><tr><td style="text-align:left">900</td><td style="text-align:left">CM</td></tr><tr><td style="text-align:left">500</td><td style="text-align:left">D</td></tr><tr><td style="text-align:left">400</td><td style="text-align:left">CD</td></tr><tr><td style="text-align:left">100</td><td style="text-align:left">C</td></tr><tr><td style="text-align:left">90</td><td style="text-align:left">XC</td></tr><tr><td style="text-align:left">50</td><td style="text-align:left">L</td></tr><tr><td style="text-align:left">40</td><td style="text-align:left">XL</td></tr><tr><td style="text-align:left">10</td><td style="text-align:left">X</td></tr><tr><td style="text-align:left">9</td><td style="text-align:left">IX</td></tr><tr><td style="text-align:left">5</td><td style="text-align:left">V</td></tr><tr><td style="text-align:left">4</td><td style="text-align:left">IV</td></tr><tr><td style="text-align:left">1</td><td style="text-align:left">I</td></tr></tbody></table><p>有了这个列表,我们的贪心策略就非常清晰了:</p><ol><li><strong>初始化</strong>: 准备一个空字符串用来拼接结果.</li><li><strong>循环处理</strong>: 从列表最大的值(1000)开始,依次尝试从输入的整数 <code>num</code> 中减去它.</li><li><strong>判断与操作</strong>:<ul><li>如果当前的 <code>num</code> 大于或等于列表中的某个值 (比如1000),我们就把这个值对应的罗马符号 (“M”) 添加到结果字符串中,然后从 <code>num</code> 中减去这个值 (1000).</li><li>然后,我们<strong>继续用当前的 <code>num</code></strong> 和 <strong>同一个值 (1000)</strong> 进行比较,直到 <code>num</code> 不再大于或等于它.</li><li>例如,对于 <code>num = 3749</code>:<ul><li><code>3749 &gt;= 1000</code>? 是. 结果: “M”, <code>num</code> 变为 2749.</li><li><code>2749 &gt;= 1000</code>? 是. 结果: “MM”, <code>num</code> 变为 1749.</li><li><code>1749 &gt;= 1000</code>? 是. 结果: “MMM”, <code>num</code> 变为 749.</li><li><code>749 &gt;= 1000</code>? 否. 移动到下一个值 (900).</li></ul></li></ul></li><li><strong>迭代</strong>: 对列表中的下一个值 (900) 重复第3步.<ul><li><code>749 &gt;= 900</code>? 否. 移动到下一个值 (500).</li><li><code>749 &gt;= 500</code>? 是. 结果: “MMMD”, <code>num</code> 变为 249.</li><li>…以此类推,直到 <code>num</code> 变为 0.</li></ul></li></ol><p>这个过程保证了我们总是优先使用能表示最大数值的符号组合,完美地解决了这个问题.</p><hr><h3 id="代码-2">代码:</h3><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Solution</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">string</span> <span class="title">IntToRoman</span>(<span class="params"><span class="built_in">int</span> num</span>)</span> &#123;</span><br><span class="line">        <span class="comment">// 1. 定义数值和符号的对应关系, 从大到小排列.</span></span><br><span class="line">        <span class="comment">// 这种并行的数组结构非常高效, 通过相同的索引(index)来关联值和符号.</span></span><br><span class="line">        <span class="built_in">int</span>[] values = &#123; <span class="number">1000</span>, <span class="number">900</span>, <span class="number">500</span>, <span class="number">400</span>, <span class="number">100</span>, <span class="number">90</span>, <span class="number">50</span>, <span class="number">40</span>, <span class="number">10</span>, <span class="number">9</span>, <span class="number">5</span>, <span class="number">4</span>, <span class="number">1</span> &#125;;</span><br><span class="line">        <span class="built_in">string</span>[] symbols = &#123; <span class="string">&quot;M&quot;</span>, <span class="string">&quot;CM&quot;</span>, <span class="string">&quot;D&quot;</span>, <span class="string">&quot;CD&quot;</span>, <span class="string">&quot;C&quot;</span>, <span class="string">&quot;XC&quot;</span>, <span class="string">&quot;L&quot;</span>, <span class="string">&quot;XL&quot;</span>, <span class="string">&quot;X&quot;</span>, <span class="string">&quot;IX&quot;</span>, <span class="string">&quot;V&quot;</span>, <span class="string">&quot;IV&quot;</span>, <span class="string">&quot;I&quot;</span> &#125;;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 使用 StringBuilder 来高效地拼接字符串.</span></span><br><span class="line">        StringBuilder result = <span class="keyword">new</span> StringBuilder();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 遍历所有的数值. 循环条件 `num &gt; 0` 是一个优化, 一旦数字减到0就提前退出循环.</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">0</span>; i &lt; values.Length &amp;&amp; num &gt; <span class="number">0</span>; i++) &#123;</span><br><span class="line">            <span class="comment">// 4. 贪心选择: 只要当前数字还能减去这个值, 就一直减.</span></span><br><span class="line">            <span class="keyword">while</span> (num &gt;= values[i]) &#123;</span><br><span class="line">                <span class="comment">// 减去数值</span></span><br><span class="line">                num -= values[i];</span><br><span class="line">                <span class="comment">// 同时拼接上对应的罗马符号</span></span><br><span class="line">                result.Append(symbols[i]);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 5. 将 StringBuilder 对象转换为最终的 string 并返回.</span></span><br><span class="line">        <span class="keyword">return</span> result.ToString();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="部分讲解">部分讲解</h4><p><strong><code>StringBuilder</code> 类</strong></p><ul><li><strong>是什么</strong>: <code>StringBuilder</code> 是一个专门用来处理&quot;可变&quot;字符串的类.</li><li><strong>为什么用它</strong>: 在C#中, <code>string</code> 类型是<strong>不可变的 (immutable)</strong>. 这意味着每次我们执行 <code>myString = myString + &quot;a&quot;;</code> 这样的操作时, 系统并不是在原来的字符串后面添加字符, 而是创建了一个<strong>全新的字符串对象</strong>, 然后把旧字符串和 “a” 的内容拷贝进去. 如果在循环中大量执行这种操作(就像本题), 会频繁地创建对象和回收内存, 导致性能下降.</li><li><strong><code>StringBuilder</code> 的优势</strong>: 它内部维护一个可变的字符缓冲区. 当我们调用 <code>result.Append(symbols[i]);</code> 时, 它只是把新字符添加到缓冲区的末尾, 而不是每次都创建新对象. 这在循环拼接字符串的场景下效率极高.</li><li><strong><code>result.ToString()</code></strong>: 当所有拼接操作完成后, 调用 <code>ToString()</code> 方法可以从 <code>StringBuilder</code> 内部的缓冲区生成一个最终的, 不可变的 <code>string</code> 对象.</li></ul><hr><h3 id="可能的实际应用">可能的实际应用:</h3><ol><li><p><strong>贪心算法的应用:</strong></p><ul><li><strong>游戏AI:</strong> 怪物或NPC的行为决策. 比如一个怪物总是优先攻击离它最近或者血量最低的玩家, 这就是一种贪心策略.</li><li><strong>寻路算法:</strong> 像A*这样的寻路算法中, &quot;启发函数(heuristic)&quot;部分就带有贪心的思想, 它会估算一个到达终点的&quot;最优&quot;距离, 优先探索看起来离终点更近的节点.</li><li><strong>程序化内容生成:</strong> 在生成地牢或地图时, 可能会有一个算法贪心地选择下一个房间放置的位置, 以求最快填满空间或连接所有关键点.</li></ul></li><li><p><strong>查找表 (Lookup Table) 的应用 (类似本题的<code>values</code>和<code>symbols</code>数组):</strong></p><ul><li><strong>游戏数据管理:</strong> 在Unity中, 我们可以使用 <code>ScriptableObject</code> 或 <code>Dictionary</code> 创建一个物品ID到物品属性(如名称, 伤害, 图标)的查找表. 当玩家获得ID为101的物品时, 我们可以快速从中查出它的所有信息, 而不是遍历一个巨大的列表.</li><li><strong>性能优化:</strong> 对于一些计算复杂的函数(比如三角函数), 如果输入值的范围有限, 可以预先计算好一批结果存入数组或哈希表. 在运行时直接查表取值, 用空间换时间, 避免重复的昂贵计算.</li></ul></li><li><p><strong><code>StringBuilder</code> 的应用:</strong></p><ul><li><strong>动态UI文本:</strong> 构建复杂的UI显示内容, 比如一个角色的状态面板, 需要拼接玩家姓名, 等级, 生命值, 魔法值, 各种状态效果等多个信息.</li><li><strong>日志系统:</strong> 在开发过程中, 我们可能需要一个日志系统来记录游戏运行时的详细信息. 将多条日志信息拼接成一个完整的日志文件时, 使用 <code>StringBuilder</code> 会比用 <code>+</code> 高效得多.</li><li><strong>数据序列化:</strong> 当我们需要将游戏存档数据转换成JSON或XML格式的字符串时, 这个过程涉及到大量的字符串拼接, <code>StringBuilder</code> 是不二之选.</li></ul></li></ol>]]></content>
      
      
      <categories>
          
          <category> 力扣刷题 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 算法 </tag>
            
            <tag> 力扣 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>力扣刷题日常(9-10)</title>
      <link href="/posts/9cf22d8b/"/>
      <url>/posts/9cf22d8b/</url>
      
        <content type="html"><![CDATA[<h1>力扣刷题日常(9-10)</h1><h2 id="第9题-回文数-难度-简单">第9题: 回文数 (难度: 简单)</h2><h3 id="原题">原题:</h3><p>给你一个整数 <code>x</code> ，如果 <code>x</code> 是一个回文整数，返回 <code>true</code> ；否则，返回 <code>false</code> 。</p><p>回文数是指正序（从左向右）和倒序（从右向左）读都是一样的整数。</p><ul><li>例如，<code>121</code> 是回文，而 <code>123</code> 不是。</li></ul><p><strong>示例 1：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入：x = 121</span><br><span class="line">输出：true</span><br></pre></td></tr></table></figure><p><strong>示例 2：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入：x = -121</span><br><span class="line">输出：false</span><br><span class="line">解释：从左向右读, 为 -121 。 从右向左读, 为 121- 。因此它不是一个回文数。</span><br></pre></td></tr></table></figure><p><strong>示例 3：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入：x = 10</span><br><span class="line">输出：false</span><br><span class="line">解释：从右向左读, 为 01 。因此它不是一个回文数。</span><br></pre></td></tr></table></figure><p><strong>提示：</strong></p><ul><li><code>-2^31 &lt;= x &lt;= 2^31 - 1</code></li></ul><p>**进阶：**你能不将整数转为字符串来解决这个问题吗？</p><h3 id="原题提示-附翻译">原题提示(附翻译):</h3><p>Beware of overflow when you reverse the integer.(反转整数时要小心溢出。)</p><hr><h3 id="开始解题">开始解题:</h3><h4 id="思路1-转换为字符串">思路1: 转换为字符串</h4><p>这是很容易就能想到的方法.我们把数字变成一串字符, 然后检查这串字符是不是一个回文串.那回文串在第5题里也有过涉及.</p><ol><li><strong>转换:</strong> 将整数 x 转换为字符串</li><li><strong>反转:</strong> 将这个字符串反转</li><li><strong>比较:</strong> 比较原始字符串与反转字符串是否相等. 如果相等, 这就是回文数.</li></ol><p>这个方法很简单直接,但看题目的进阶部分: “你能不将整数转为字符串来解决这个问题吗？”.这通常是面试官更想看到的解法,因为它更能考察我们对数字本身的操作能力</p><h4 id="思路2-数学解法-反转一半数字">思路2: 数学解法 - 反转一半数字</h4><p>这个办法完全通过数学运算来处理,避免了字符串转换带来的额外内存开销,效率更高. 核心思想是: 我们只反转数字的后半部分,然后把它和前半部分进行比较.</p><p>那为什么我们只反转一半呢? 题目的提示中提到了&quot;反转时当心溢出&quot;. 如果我们尝试反转一个完整的非常大的整数, 反转后的整数可能会超过 int 的显示范围,导致程序出错. 只反转一半可以完美地规避这个问题.</p><p>我们用 <code>1221</code> 这个栗子来分解步骤:</p><ol><li><p><strong>处理特殊情况(边缘处理):</strong></p><ul><li>负数: 负数绝对不是回文数,从原题给的例子里我们就能看出来. 因为负号只在最左边. 反转之后负号在最右边. 所以只要输入 x 为负数,直接返回 <code>false</code>.</li><li>以0结尾的非0数: 如过一个数的最后一位是 0 (例如 10, 120),那么它想成为回文数,第一位就必须是 0. 但除了 0 本身,其他任何数字都不能以 0 开头. 所以,任何以 0 结尾且不为 0 的数都不是回文数,直接返回 <code>false</code>.</li></ul></li><li><p><strong>核心反转逻辑:</strong></p><ul><li>我们创建一个新变量 <code>revertedNumber</code> 来存储反转后的后半部分数字,初始值为 0.</li><li>我们用一个循环,每次循环都从原数字 x 中取出最后一位,加到 <code>revertedNumber</code> 的末尾,同时将 x 的最后一位移除.</li><li>循环的终止条件是 <code>x&lt;=revertedNumber</code>. 当 x 不再大于 <code>revertedNumber</code> 时, 说明我们已经处理到数字的中心部分了.</li></ul><p>以 <code>1221</code> 为栗子:</p><ul><li><strong>初始:</strong> <code>x=1221</code>, <code>revertedNumber=0</code></li><li><strong>循环1:</strong><ul><li>取出 <code>x</code> 的末位: <code>1221 % 10</code> -&gt; <code>1</code></li><li>更新 <code>revertedNumber</code>: <code>revertedNumber = revertedNumber * 10 + 1</code> -&gt; <code>1</code></li><li>更新 <code>x</code>: <code>x = 1221 / 10</code> -&gt; <code>122</code></li><li>此时 <code>x(122) &gt; revertedNumber(1)</code>,循环继续</li></ul></li><li><strong>循环2:</strong><ul><li>取出 <code>x</code> 的末位: <code>122 % 10</code> -&gt; <code>2</code></li><li>更新 <code>revertedNumber</code>: <code>revertedNumber = revertedNumber * 10 + 2</code> -&gt; <code>12</code></li><li>更新 <code>x</code>: <code>x = 122 / 10</code> -&gt; <code>12</code></li><li>此时 <code>x(12) == revertedNumber (12)</code>,循环停止.</li></ul></li></ul></li><li><p><strong>最后比较:</strong></p><ul><li>循环结束后,我们需要比较 <code>x</code> (原数字的前半部分) 与 <code>revertedNumber</code> (原数字反转后的后半部分).</li><li>对于偶数位数的数字(如 <code>1221</code>): 循环结束后, <code>x</code> 会等于 <code>revertedNumber</code> , 所以判断 <code>x== revertedNumber</code></li><li>对于奇数位数的数字(如 <code>121</code>):<ul><li>初始: <code>x = 121</code>, <code>revertedNumber = 0</code></li><li>循环1: <code>x = 12</code>, <code>revertedNumber = 1</code></li><li>循环2: <code>x = 1</code>, <code>revertedNumber = 12</code>. 此时 <code>x(1) &lt;revertedNumber(12)</code>, 循环停止.</li><li>在这种情况下, <code>x = 1</code>, <code>revertedNumber = 12</code>. 中间的那个 <code>2</code> 被包含在了 <code>revertedNumber</code> 里. 为了公平比较, 我们需要把这个中间位去掉, 即 <code>revertedNumber / 10</code> (<code>12 / 10</code> -&gt; <code>1</code>). 然后再比较 <code>x</code> 和 <code>revertedNumber / 10</code>.</li></ul></li><li>所以, 最终的判断条件是: <code>x == revertedNumber || x == revertedNumber / 10</code>. 这个条件同时覆盖了偶数位数和奇数位数的情况.</li></ul></li></ol><hr><h3 id="代码实现">代码实现:</h3><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Solution</span> </span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">bool</span> <span class="title">IsPalindrome</span>(<span class="params"><span class="built_in">int</span> x</span>)</span> </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 1: 特殊情况处理</span></span><br><span class="line">        <span class="keyword">if</span> (x &lt; <span class="number">0</span> || (x % <span class="number">10</span> == <span class="number">0</span> &amp;&amp; x != <span class="number">0</span>)) </span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2: 变量声明与循环</span></span><br><span class="line">        <span class="built_in">int</span> revertedNumber = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">while</span> (x &gt; revertedNumber) </span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">// 3: 核心数学运算</span></span><br><span class="line">            revertedNumber = revertedNumber * <span class="number">10</span> + x % <span class="number">10</span>;</span><br><span class="line">            x /= <span class="number">10</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 4: 最终的返回与比较</span></span><br><span class="line">        <span class="keyword">return</span> x == revertedNumber || x == revertedNumber / <span class="number">10</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h3 id="练习题">练习题</h3><h4 id="选择题">选择题</h4><p><strong>1. 对于一个整数 <code>int num = 12345;</code>, 以下哪个C#表达式可以得到数字 <code>4</code>?</strong></p><p>A. <code>num / 100 % 10</code><br>B. <code>num % 100 / 10</code><br>C. <code>num / 10 % 100</code><br>D. <code>(num / 10) % 10</code></p><h4 id="简答题">简答题</h4><p><strong>2. 给你一个32位有符号整数 <code>x</code>, 编写一个函数 <code>Reverse(int x)</code> 来反转 <code>x</code> 的每一位. 例如, 输入 <code>123</code>, 输出 <code>321</code>; 输入 <code>-123</code>, 输出 <code>-321</code>. 但你需要考虑一个重要问题: 反转后的数字可能会超出 <code>int</code> 的表示范围 (即溢出). 你将如何处理这个溢出问题? (提示: 不需要写完整的代码, 描述你的检查逻辑即可).</strong></p><hr><h4 id="参考答案">参考答案:</h4><h5 id="选择题答案">选择题答案</h5><p><strong>D</strong>.</p><ul><li><code>num / 10</code> 结果是 <code>1234</code>.</li><li><code>1234 % 10</code> 结果是 <code>4</code>.</li><li>选项A: <code>12345 / 100</code> -&gt; <code>123</code>. <code>123 % 10</code> -&gt; <code>3</code>.</li><li>选项B: <code>12345 % 100</code> -&gt; <code>45</code>. <code>45 / 10</code> -&gt; <code>4</code>. (这个也可以, 但D更符合逐位处理的常规思路).</li><li>选项C: <code>12345 / 10</code> -&gt; <code>1234</code>. <code>1234 % 100</code> -&gt; <code>34</code>.</li></ul><p>(注: 选项B也能得到正确结果, 但D选项的操作 <code>(num / 10) % 10</code> 更能体现 “先移除个位, 再取新个位” 的逻辑, 具有更好的通用性. 两个都对, 但D的思路更具代表性.)</p><h5 id="简答题答案">简答题答案</h5><p><strong>处理整数反转溢出问题的逻辑:</strong></p><p>核心思想是在将新的数字添加到结果 <code>result</code> <strong>之前</strong>, 检查这个操作是否会导致溢出.</p><p>假设我们正在构建反转后的数字 <code>result</code>. 在每一步循环中, 我们会执行 <code>result = result * 10 + digit</code>.</p><p>溢出可能发生在 <code>result * 10</code> 这一步.</p><ol><li><p><strong>检查正溢出</strong>: 在执行 <code>result = result * 10 + digit</code> 之前, 我们需要检查 <code>result * 10</code> 是否会大于 <code>int.MaxValue</code>.</p><ul><li>一个安全的检查方法是: <code>if (result &gt; int.MaxValue / 10) return 0;</code> (0通常作为错误/溢出标志).</li><li>还需要考虑一个临界情况: 如果 <code>result == int.MaxValue / 10</code>, 那么只有当 <code>digit</code> 大于 <code>int.MaxValue % 10</code> (即7) 时才会溢出. 所以完整的检查是: <code>if (result &gt; int.MaxValue / 10 || (result == int.MaxValue / 10 &amp;&amp; digit &gt; 7)) return 0;</code></li></ul></li><li><p><strong>检查负溢出</strong>: 逻辑类似, 但使用 <code>int.MinValue</code>.</p><ul><li>安全检查: <code>if (result &lt; int.MinValue / 10 || (result == int.MinValue / 10 &amp;&amp; digit &lt; -8)) return 0;</code> (因为 <code>int.MinValue</code> 是 <code>-2147483648</code>).</li></ul></li></ol><p>通过在每一步乘法和加法之前进行这样的 “预检查”, 我们可以有效地防止实际的溢出发生, 从而保证算法的正确性.</p><hr><h3 id="可能的实际用途">可能的实际用途:</h3><ul><li><strong>底层数据处理</strong>: 在一些二进制数据处理, 或者需要对数字位进行操作的底层算法中, 这种直接操作数字位的技巧非常有用.</li><li><strong>面试和技术评估</strong>: 这是最直接的应用. 这类问题是科技公司面试中评估候选人基本功和思维严谨性的经典题目. 掌握它能让我们在面试中表现得更出色.</li></ul><hr><h2 id="第10题-正则表达式匹配-难度-困难">第10题: 正则表达式匹配(难度: 困难)</h2><h3 id="原题-2">原题:</h3><p>给你一个字符串 <code>s</code> 和一个字符规律 <code>p</code>，请你来实现一个支持 <code>'.'</code> 和 <code>'*'</code> 的正则表达式匹配。</p><ul><li><code>'.'</code> 匹配任意单个字符</li><li><code>'*'</code> 匹配零个或多个前面的那一个元素</li></ul><p>所谓匹配，是要涵盖 <strong>整个</strong> 字符串 <code>s</code> 的，而不是部分字符串。</p><p><strong>示例 1：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入：s = &quot;aa&quot;, p = &quot;a&quot;</span><br><span class="line">输出：false</span><br><span class="line">解释：&quot;a&quot; 无法匹配 &quot;aa&quot; 整个字符串。</span><br></pre></td></tr></table></figure><p><strong>示例 2:</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入：s = &quot;aa&quot;, p = &quot;a*&quot;</span><br><span class="line">输出：true</span><br><span class="line">解释：因为 &#x27;*&#x27; 代表可以匹配零个或多个前面的那一个元素, 在这里前面的元素就是 &#x27;a&#x27;。因此，字符串 &quot;aa&quot; 可被视为 &#x27;a&#x27; 重复了一次。</span><br></pre></td></tr></table></figure><p><strong>示例 3：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入：s = &quot;ab&quot;, p = &quot;.*&quot;</span><br><span class="line">输出：true</span><br><span class="line">解释：&quot;.*&quot; 表示可匹配零个或多个（&#x27;*&#x27;）任意字符（&#x27;.&#x27;）。</span><br></pre></td></tr></table></figure><p><strong>提示：</strong></p><ul><li><code>1 &lt;= s.length &lt;= 20</code></li><li><code>1 &lt;= p.length &lt;= 20</code></li><li><code>s</code> 只包含从 <code>a-z</code> 的小写字母。</li><li><code>p</code> 只包含从 <code>a-z</code> 的小写字母，以及字符 <code>.</code> 和 <code>*</code>。</li><li>保证每次出现字符 <code>*</code> 时，前面都匹配到有效的字符</li></ul><hr><h3 id="开始解题-2">开始解题:</h3><p>此题需要用到动态规划(DP)，技术能力暂时达不到，待我进修归来再进行讲解。</p>]]></content>
      
      
      <categories>
          
          <category> 力扣刷题 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 算法 </tag>
            
            <tag> 力扣 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>力扣刷题日常(7-8)</title>
      <link href="/posts/a1373671/"/>
      <url>/posts/a1373671/</url>
      
        <content type="html"><![CDATA[<h1>力扣刷题日常(7-8)</h1><h2 id="第7题-整数反转-难度-中等">第7题: 整数反转(难度: 中等)</h2><h3 id="原题">原题:</h3><p>给你一个 32 位的有符号整数 <code>x</code> ,返回将 <code>x</code> 中的数字部分反转后的结果.</p><p>如果反转后整数超过 32 位的有符号整数的范围 <code>[−231, 231 − 1]</code> ,就返回 0.</p><p><strong>假设环境不允许存储 64 位整数（有符号或无符号）.</strong></p><p><strong>示例 1:</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入:x = 123</span><br><span class="line">输出:321</span><br></pre></td></tr></table></figure><p><strong>示例 2:</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入:x = -123</span><br><span class="line">输出:-321</span><br></pre></td></tr></table></figure><p><strong>示例 3:</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入:x = 120</span><br><span class="line">输出:21</span><br></pre></td></tr></table></figure><p><strong>示例 4:</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入:x = 0</span><br><span class="line">输出:0</span><br></pre></td></tr></table></figure><p><strong>提示:</strong></p><ul><li><code>-231 &lt;= x &lt;= 231 - 1</code></li></ul><hr><h3 id="开始解题">开始解题:</h3><p>首先,这道题虽然是中等难度,但它考察的重点——整数溢出处理,是编程中一个非常重要且实用的概念.</p><p>我们先来分析这道题的实现逻辑</p><h4 id="算法实现逻辑">算法实现逻辑</h4><p>我们先从数学的角度思考,如何把一个数字 <code>123</code> ,颠倒过来变成 <code>321</code>?</p><p>核心思想是<strong>数学方法</strong>,通过循环不断&quot;<strong>剥离</strong>&quot;原数字的末尾,然后&quot;<strong>构建</strong>&quot;新数字的开头</p><p>我们以 <code>123</code> 为栗子,来走一步这个流程</p><ol><li><p><strong>初始化:</strong> 我们需要一个变量来存储反转后的结果,命名为 <code>reversed_x</code> ,初始时 <code>reversed_x = 0</code></p></li><li><p><strong>第一轮循环:</strong></p><ul><li>取末位: 如何得到最后一位呢----取余(%)运算. <code>123 % 10</code> 的结果就是 <code>3</code> .我们将这个数字定为 <code>dight</code>.</li><li>构建新数: 把这个 <code>dight</code> 添加到 <code>reversed_x</code> 中. <code>reversed_x = reversed_x * 10 + dight</code>. 经过计算,现在 <code>reversed_x</code> 的值为 <code>0 * 10 + 3 = 3</code></li><li>&quot;砍掉&quot;原数的末位: 如何砍掉原数的末位呢----整除(/)运算. <code>123 / 10</code> 的结果就是 <code>12</code> .现在 <code>x</code> 变成了 <code>12</code>.</li></ul></li><li><p><strong>第二轮循环</strong>:</p><ul><li><p>此时 <code>x = 12</code>,<code>reversed_x = 3</code>.</p></li><li><p><strong>取末位</strong>: <code>12 % 10</code> 得到 <code>2</code>.(<code>digit = 2</code>)</p></li><li><p><strong>构建新数</strong>: <code>reversed_x = 3 * 10 + 2</code>,结果为 <code>32</code>.</p></li><li><p><strong>“砍掉”原数的末位</strong>: <code>12 / 10</code> 得到 <code>1</code>.现在 <code>x</code> 变成了 <code>1</code>.</p></li></ul></li><li><p><strong>第三轮循环</strong>:</p><ul><li><p>此时 <code>x = 1</code>,<code>reversed_x = 32</code>.</p></li><li><p><strong>取末位</strong>: <code>1 % 10</code> 得到 <code>1</code>.(<code>digit = 1</code>)</p></li><li><p><strong>构建新数</strong>: <code>reversed_x = 32 * 10 + 1</code>,结果为 <code>321</code>.</p></li><li><p><strong>“砍掉”原数的末位</strong>: <code>1 / 10</code> 得到 <code>0</code>.现在 <code>x</code> 变成了 <code>0</code>.</p></li></ul></li><li><p><strong>循环结束</strong>: 我们的循环条件是 <code>x != 0</code>.现在 <code>x</code> 已经是 <code>0</code> 了,所以循环停止.最终的结果 <code>reversed_x</code> 就是 <code>321</code>.</p></li></ol><h4 id="如何处理特殊情况">如何处理特殊情况?</h4><ul><li><strong>负数 (如 -123)</strong>: 这个数学方法同样适用<ul><li><code>-123 % 10</code> 结果是 <code>-3</code>.</li><li><code>-123 / 10</code> 结果是 <code>-12</code>.</li><li>整个过程下来,符号会被自然而然地保留,最终得到 <code>-321</code>.</li></ul></li><li><strong>末尾是0 (如 120)</strong>:<ul><li>第一轮,<code>120 % 10</code> 得到 <code>0</code>,<code>reversed_x</code> 变成 <code>0</code>.<code>x</code> 变成 <code>12</code>.</li><li>后续步骤和 <code>12</code> 的反转一样,最终得到 <code>21</code>.前导零被自动消除了.</li></ul></li></ul><h4 id="本题最大的难点-处理整数溢出">本题最大的难点: 处理整数溢出</h4><p>题目要求环境是32位有符号整数,范围是 <code>[-2^31, 2^31 - 1]</code>,也就是 <code>[-2147483648, 2147483647]</code>.</p><p>在我们的核心步骤 <code>reversed_x = reversed_x * 10 + digit</code> 中,<code>reversed_x * 10</code> 的结果可能会超出这个范围.由于题目<strong>不允许使用64位整数（long）<strong>来临时存储,我们不能等溢出发生了再去判断.我们必须在它发生</strong>之前</strong>就预判到.</p><p><strong>如何进行预判:</strong></p><p>我们来分析一下正数溢出的情况.溢出即将发生在 <code>reversed_x</code> 乘以 10 的过程中</p><p><code>int</code> 的最大值为 <code>int.MaxValue</code> (即 <code>2147483647</code>)</p><ol><li><strong>一个粗略的检查:</strong> 如果 <code>reversed_x</code> 大于 <code>int.MaxValue / 10</code> (即 <code>214748364</code>),那么它再乘以10,无论 <code>digit</code> 是多少,都<strong>必然</strong>会溢出.</li><li><strong>一个精确的检查:</strong> 如果 <code>reversed_x</code> 正好等于 <code>int.MaxValue / 10</code> (即 <code>214748364</code>),那么能否溢出就取决于 <code>digit</code> 了.<ul><li><code>int.MaxValue</code> 的末尾是 <code>7</code>.</li><li>如果 <code>digit</code> 大于 <code>7</code>,那么 <code>214748364 * 10 + digit</code> 就会大于 <code>2147483647</code>,导致溢出.</li><li>如果 <code>digit</code> 小于等于 <code>7</code>,就不会溢出.</li></ul></li></ol><p>所以,正数溢出的完整判断条件是:</p><p><code>if (reversed_x &gt; int.MaxValue / 10 || (reversed_x == int.MaxValue / 10 &amp;&amp; digit &gt; 7))</code></p><p>同理,负数溢出的判断条件（<code>int.MinValue</code> 是 <code>-2147483648</code>）:<br><code>if (reversed_x &lt; int.MinValue / 10 || (reversed_x == int.MinValue / 10 &amp;&amp; digit &lt; -8))</code></p><p>一旦检测到即将溢出,我们就按题目要求返回 <code>0</code>.</p><h4 id="简单总结">简单总结:</h4><ol><li>初始化 <code>reversed_x = 0</code>.</li><li>使用 <code>while</code> 循环,条件是 <code>x != 0</code>.</li><li>在循环内部:<br>a.  通过 <code>x % 10</code> 取出 <code>x</code> 的末位数字 <code>digit</code>.<br>b.  <strong>在执行乘法前,进行溢出检查</strong>.如果检查到即将溢出,立即 <code>return 0</code>.<br>c.  如果安全,则更新 <code>reversed_x = reversed_x * 10 + digit</code>.<br>d.  通过 <code>x / 10</code> “砍掉” <code>x</code> 的末位.</li><li>循环结束后,返回 <code>reversed_x</code>.</li></ol><hr><h3 id="算法代码">算法代码:</h3><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Solution</span> </span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">int</span> <span class="title">Reverse</span>(<span class="params"><span class="built_in">int</span> x</span>)</span> </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 用于存储反转后的结果</span></span><br><span class="line">        <span class="built_in">int</span> reversed = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 循环直到x的所有位都被处理</span></span><br><span class="line">        <span class="keyword">while</span> (x != <span class="number">0</span>) </span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">// 1. 取出x的末尾数字</span></span><br><span class="line">            <span class="comment">// C#的取余(%)运算会自动处理符号,例如 -123 % 10 的结果是 -3</span></span><br><span class="line">            <span class="built_in">int</span> digit = x % <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 2. “砍掉”x的末尾数字</span></span><br><span class="line">            <span class="comment">// C#的整除(/)运算同样会自动处理符号,例如 -123 / 10 的结果是 -12</span></span><br><span class="line">            x /= <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 3. 【核心】在构建新数之前,检查是否会溢出</span></span><br><span class="line">            <span class="comment">// 检查正数溢出:</span></span><br><span class="line">            <span class="comment">// a) reversed &gt; 214748364 (int.MaxValue / 10)</span></span><br><span class="line">            <span class="comment">//    如果成立,那么 reversed * 10 必然大于 int.MaxValue,溢出.</span></span><br><span class="line">            <span class="comment">// b) reversed == 214748364 &amp;&amp; digit &gt; 7</span></span><br><span class="line">            <span class="comment">//    如果成立,那么 reversed * 10 + digit 必然大于 2147483647,溢出.</span></span><br><span class="line">            <span class="keyword">if</span> (reversed &gt; <span class="built_in">int</span>.MaxValue / <span class="number">10</span> || (reversed == <span class="built_in">int</span>.MaxValue / <span class="number">10</span> &amp;&amp; digit &gt; <span class="number">7</span>)) </span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="number">0</span>; <span class="comment">// 发生正向溢出,返回0</span></span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 检查负数溢出:</span></span><br><span class="line">            <span class="comment">// a) reversed &lt; -214748364 (int.MinValue / 10)</span></span><br><span class="line">            <span class="comment">//    如果成立,那么 reversed * 10 必然小于 int.MinValue,溢出.</span></span><br><span class="line">            <span class="comment">// b) reversed == -214748364 &amp;&amp; digit &lt; -8</span></span><br><span class="line">            <span class="comment">//    如果成立,那么 reversed * 10 + digit 必然小于 -2147483648,溢出.</span></span><br><span class="line">            <span class="keyword">if</span> (reversed &lt; <span class="built_in">int</span>.MinValue / <span class="number">10</span> || (reversed == <span class="built_in">int</span>.MinValue / <span class="number">10</span> &amp;&amp; digit &lt; <span class="number">-8</span>)) </span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="number">0</span>; <span class="comment">// 发生负向溢出,返回0</span></span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 4. 安全,构建新数</span></span><br><span class="line">            <span class="comment">// 将取出的末位数字拼接到reversed的末尾</span></span><br><span class="line">            reversed = reversed * <span class="number">10</span> + digit;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 循环结束,返回最终结果</span></span><br><span class="line">        <span class="keyword">return</span> reversed;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h3 id="知识点总结">知识点总结:</h3><ol><li><p><strong>数值溢出:一个“沉默的杀手”</strong></p><ul><li><strong>核心思想</strong>:任何有固定大小的数据类型（如 <code>byte</code>, <code>short</code>, <code>int</code>, <code>long</code>）都有其表示范围.当计算结果超出这个范围时,就会发生“溢出”.在C#中,整数溢出默认是<strong>不抛出异常</strong>的,它会“环绕”（wrap around）,例如 <code>int.MaxValue + 1</code> 会变成 <code>int.MinValue</code>.这种不报错的错误行为非常隐蔽,是很多逻辑bug的根源.</li><li><strong>通用原则</strong>:在进行任何可能导致结果急剧增大的运算（尤其是乘法和累加）时,都要有意识地思考:“我的结果会超出数据类型的边界吗？”</li></ul></li><li><p><strong>防御性编程:在错误发生前预判</strong></p><ul><li><strong>核心思想</strong>:不要等到溢出发生后再去处理（因为你可能根本察觉不到）,而是在执行运算<strong>之前</strong>,就检查这次运算是否安全.</li><li><strong>本题范例</strong>:<code>if (reversed &gt; int.MaxValue / 10 ...)</code> 就是一个完美的防御性编程范例.我们通过逆向思维（从 <code>int.MaxValue</code> 反推）,为即将执行的 <code>reversed * 10</code> 操作建立了一个“安全护栏”.</li><li><strong>通用原则</strong>:对于关键运算,先检查其操作数是否在安全范围内,再执行运算.这比“事后补救”要健壮得多.</li></ul></li><li><p><strong>数学方法处理数字:高效且独立</strong></p><ul><li><strong>核心思想</strong>:使用取余 (<code>%</code>) 和整除 (<code>/</code>) 是一套“组合拳”,可以逐位拆解任何整数,而无需将其转换为字符串.</li><li><strong>优点</strong>:这种纯数学处理通常比字符串转换和操作（如 <code>ToString()</code>, <code>ToCharArray()</code>, <code>int.Parse()</code>）的性能要高得多,并且不产生额外的内存分配（GC）,这在对性能敏感的Unity <code>Update</code> 循环中尤其重要.</li><li><strong>通用原则</strong>:当需要处理数字的各个位时,优先考虑 <code>%</code> 和 <code>/</code> 的数学方法.</li></ul></li></ol><hr><h3 id="练习题">练习题:</h3><h4 id="选择题"><strong>选择题</strong></h4><p><strong>1. 在本题的溢出判断 <code>if (reversed &gt; int.MaxValue / 10 || ...)</code> 中,为什么我们用 <code>int.MaxValue / 10</code> 进行比较,而不是直接写 <code>if (reversed * 10 &gt; int.MaxValue)</code>？</strong></p><p>A. 因为 <code>reversed * 10</code> 可能会先于比较操作发生溢出,导致 <code>if</code> 判断的条件本身就是基于一个错误（已环绕）的值,从而判断失效.<br>B. 这是一种性能优化,除法比乘法快.<br>C. 因为 <code>int.MaxValue</code> 不能被10整除,所以必须先做除法.<br>D. 为了让代码更易读.</p><p><strong>2. 如果你需要获取一个正整数 <code>n</code> (例如 <code>n = 54321</code>) 的百位数（也就是 <code>3</code>）,以下哪个表达式是正确的？</strong></p><p>A. <code>n % 100</code><br>B. <code>n / 100</code><br>C. <code>(n / 100) % 10</code><br>D. <code>n % 1000 / 100</code></p><hr><h4 id="简答题"><strong>简答题</strong></h4><p><strong>3. 假设你需要反转一个 <code>short</code> 类型的整数（范围: -32768 到 32767）.请参照本题的思路,写出针对 <code>short</code> 类型的正数溢出检查的 <code>if</code> 条件语句.</strong></p><hr><h4 id="参考答案">参考答案:</h4><h5 id="选择题答案"><strong>选择题答案</strong></h5><p><strong>1. 在本题的溢出判断 <code>if (reversed &gt; int.MaxValue / 10 || ...)</code> 中,为什么我们用 <code>int.MaxValue / 10</code> 进行比较,而不是直接写 <code>if (reversed * 10 &gt; int.MaxValue)</code>？</strong></p><p><strong>正确答案:A</strong></p><p><strong>解析:</strong></p><ul><li><strong>A. 因为 <code>reversed * 10</code> 可能会先于比较操作发生溢出,导致 <code>if</code> 判断的条件本身就是基于一个错误（已环绕）的值,从而判断失效.</strong><ul><li>这是最关键的原因.在C#中,<code>if (A &gt; B)</code> 这个表达式会先计算 <code>A</code> 的值,再计算 <code>B</code> 的值,最后进行比较.如果 <code>reversed</code> 的值已经很大（例如 <code>300000000</code>）,那么在执行 <code>reversed * 10</code> 时,结果就已经超出了 <code>int</code> 的最大值,发生了溢出并“环绕”成了一个负数.此时,<code>if</code> 语句就变成了 <code>if (一个负数 &gt; int.MaxValue)</code>,这个条件永远是 <code>false</code>,导致我们错过了真正的溢出,程序会继续使用这个错误的负数进行计算.而 <code>reversed &gt; int.MaxValue / 10</code> 这种写法,将运算转移到了不易溢出的一侧,从而安全地预判了风险.</li></ul></li><li>B. 这是一种性能优化,除法比乘法快.<ul><li>这个说法是错误的.在大多数现代处理器上,整数乘法通常比整数除法要快.</li></ul></li><li>C. 因为 <code>int.MaxValue</code> 不能被10整除,所以必须先做除法.<ul><li><code>int.MaxValue</code> 能否被10整除与判断逻辑的正确性无关.</li></ul></li><li>D. 为了让代码更易读.<ul><li>虽然代码可读性很重要,但在这里,最主要的原因是<strong>为了保证逻辑的正确性</strong>,防止因运算顺序导致溢出,从而使判断失效.</li></ul></li></ul><p><strong>2. 如果你需要获取一个正整数 <code>n</code> (例如 <code>n = 54321</code>) 的百位数（也就是 <code>3</code>）,以下哪个表达式是正确的？</strong></p><p><strong>正确答案:C</strong> (选项 D 在数学上也是正确的,但 C 是更通用和标准的方法)</p><p><strong>解析:</strong></p><ul><li>我们的目标是隔离出百位上的 <code>3</code>.</li><li><strong>第一步:去掉百位右边的所有数字.</strong> 我们可以通过整除 <code>100</code> 来实现.<br><code>54321 / 100</code> 的结果是 <code>543</code>.现在,我们想要的目标数字 <code>3</code> 成为了结果的个位数.</li><li><strong>第二步:从新结果中取出个位数.</strong> 我们可以通过对 <code>10</code> 取余来实现.<br><code>543 % 10</code> 的结果是 <code>3</code>.</li><li>所以,组合起来就是 <code>(n / 100) % 10</code>.</li><li><strong>为什么 D <code>n % 1000 / 100</code> 也可以？</strong><ul><li><code>54321 % 1000</code> 的结果是 <code>321</code>（取出了后三位）.</li><li><code>321 / 100</code> 的结果是 <code>3</code>（对后三位数取百位）.</li><li>虽然结果正确,但选项 C 的思路（“移位”再“取末位”）更具有普适性.例如,要取任意第 <code>k</code> 位的数字,通用的公式就是 <code>(n / 10^k) % 10</code>.</li></ul></li></ul><h5 id="简答题答案">简答题答案:</h5><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// short.MaxValue 是 32767</span></span><br><span class="line"><span class="comment">// short.MaxValue / 10 是 3276</span></span><br><span class="line"><span class="comment">// short.MaxValue % 10 是 7</span></span><br><span class="line"> </span><br><span class="line"><span class="comment">// 假设 digit 是当前取出的末位数,rev 是 short 类型的反转结果</span></span><br><span class="line"><span class="keyword">if</span> (rev &gt; <span class="built_in">short</span>.MaxValue / <span class="number">10</span> || (rev == <span class="built_in">short</span>.MaxValue / <span class="number">10</span> &amp;&amp; digit &gt; <span class="number">7</span>))</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 即将发生正向溢出</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h3 id="实际应用场景">实际应用场景:</h3><ol><li><p><strong>游戏经济系统（金币、资源）</strong></p><ul><li><strong>场景</strong>:玩家的货币数量通常用 <code>int</code> 或 <code>long</code> 存储.如果一个玩家拥有接近 <code>int.MaxValue</code> 的金币,此时他完成一个任务获得了少量金币,若不检查溢出,他的金币数可能会瞬间从一个巨大的正数变成负数,导致经济系统崩溃.</li><li><strong>应用</strong>:在增加玩家货币的函数中,必须加入溢出检查.例如 <code>if (playerGold &gt; int.MaxValue - rewardAmount)</code>,如果成立,则直接将玩家金币设为 <code>int.MaxValue</code>,而不是执行加法.</li></ul></li><li><p><strong>计分与伤害计算</strong></p><ul><li><strong>场景</strong>:在一个伤害可以无限叠加的“割草”游戏中,或者一个分数可以滚得极高的街机游戏中,总伤害或总分数很容易超过 <code>int</code> 的上限.</li><li><strong>应用</strong>:使用 <code>long</code> 类型来存储分数/伤害值.或者,在每次累加伤害前,进行防御性检查,防止数值环绕.</li></ul></li><li><p><strong>UI显示和格式化</strong></p><ul><li><strong>场景</strong>:你需要将一个分数 <code>12345</code> 显示成带有闪烁效果的单个数字 <code>1</code>, <code>2</code>, <code>3</code>, <code>4</code>, <code>5</code>.</li><li><strong>应用</strong>:可以使用 <code>%</code> 和 <code>/</code> 的技巧,循环取出每个数字,然后为每个数字实例化一个UI组件并应用动画,而无需进行字符串操作.</li></ul></li><li><p><strong>程序化生成（Procedural Generation）</strong></p><ul><li><strong>场景</strong>:使用一个整数种子（Seed）来生成关卡.你可以通过拆解这个种子的各位数字来决定不同的生成规则.</li><li><strong>应用</strong>:例如,<code>seed % 10</code> 的结果决定地图主题（0=森林, 1=沙漠…）,<code>(seed / 10) % 10</code> 的结果决定敌人密度,以此类推.这让一个简单的整数种子可以控制多个生成维度.</li></ul></li></ol><hr><h2 id="第8题-字符串转换整数-atoi-难度-中等">第8题: 字符串转换整数 (atoi) (难度: 中等)</h2><h3 id="原题-这部分粘贴存在问题-可自行去力扣查看">原题(这部分粘贴存在问题,可自行去力扣查看):</h3><p>请你来实现一个 <code>myAtoi(string s)</code> 函数,使其能将字符串转换成一个 32 位有符号整数.</p><p>函数 <code>myAtoi(string s)</code> 的算法如下:</p><ol><li>**空格:**读入字符串并丢弃无用的前导空格（<code>&quot; &quot;</code>）</li><li>**符号:**检查下一个字符（假设还未到字符末尾）为 <code>'-'</code> 还是 <code>'+'</code>.如果两者都不存在,则假定结果为正.</li><li>**转换:**通过跳过前置零来读取该整数,直到遇到非数字字符或到达字符串的结尾.如果没有读取数字,则结果为0.</li><li>**舍入:**如果整数数超过 32 位有符号整数范围 <code>[−231, 231 − 1]</code> ,需要截断这个整数,使其保持在这个范围内.具体来说,小于 <code>−231</code> 的整数应该被舍入为 <code>−231</code> ,大于 <code>231 − 1</code> 的整数应该被舍入为 <code>231 − 1</code> .</li></ol><p>返回整数作为最终结果.</p><p><strong>示例 1:</strong></p><p>**输入:**s = “42”</p><p>**输出:**42</p><p>**解释:**加粗的字符串为已经读入的字符,插入符号是当前读取的字符.</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">带下划线线的字符是所读的内容,插入符号是当前读入位置.</span><br><span class="line">第 1 步:&quot;42&quot;（当前没有读入字符,因为没有前导空格）</span><br><span class="line">         ^</span><br><span class="line">第 2 步:&quot;42&quot;（当前没有读入字符,因为这里不存在 &#x27;-&#x27; 或者 &#x27;+&#x27;）</span><br><span class="line">         ^</span><br><span class="line">第 3 步:&quot;42&quot;（读入 &quot;42&quot;）</span><br><span class="line">           ^</span><br></pre></td></tr></table></figure><p><strong>示例 2:</strong></p><p>**输入:**s = &quot; -042&quot;</p><p><strong>输出:</strong>-42</p><p><strong>解释:</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">第 1 步:&quot;   -042&quot;（读入前导空格,但忽视掉）</span><br><span class="line">            ^</span><br><span class="line">第 2 步:&quot;   -042&quot;（读入 &#x27;-&#x27; 字符,所以结果应该是负数）</span><br><span class="line">             ^</span><br><span class="line">第 3 步:&quot;   -042&quot;（读入 &quot;042&quot;,在结果中忽略前导零）</span><br><span class="line">               ^</span><br></pre></td></tr></table></figure><p><strong>示例 3:</strong></p><p>**输入:**s = “1337c0d3”</p><p>**输出:**1337</p><p><strong>解释:</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">第 1 步:&quot;1337c0d3&quot;（当前没有读入字符,因为没有前导空格）</span><br><span class="line">         ^</span><br><span class="line">第 2 步:&quot;1337c0d3&quot;（当前没有读入字符,因为这里不存在 &#x27;-&#x27; 或者 &#x27;+&#x27;）</span><br><span class="line">         ^</span><br><span class="line">第 3 步:&quot;1337c0d3&quot;（读入 &quot;1337&quot;；由于下一个字符不是一个数字,所以读入停止）</span><br><span class="line">             ^</span><br></pre></td></tr></table></figure><p><strong>示例 4:</strong></p><p>**输入:**s = “0-1”</p><p>**输出:**0</p><p><strong>解释:</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">第 1 步:&quot;0-1&quot; (当前没有读入字符,因为没有前导空格)</span><br><span class="line">         ^</span><br><span class="line">第 2 步:&quot;0-1&quot; (当前没有读入字符,因为这里不存在 &#x27;-&#x27; 或者 &#x27;+&#x27;)</span><br><span class="line">         ^</span><br><span class="line">第 3 步:&quot;0-1&quot; (读入 &quot;0&quot;；由于下一个字符不是一个数字,所以读入停止)</span><br><span class="line">          ^</span><br></pre></td></tr></table></figure><p><strong>示例 5:</strong></p><p>**输入:**s = “words and 987”</p><p>**输出:**0</p><p><strong>解释:</strong></p><p>读取在第一个非数字字符“w”处停止.</p><p><strong>提示:</strong></p><ul><li><code>0 &lt;= s.length &lt;= 200</code></li><li><code>s</code> 由英文字母（大写和小写）、数字（<code>0-9</code>）、<code>' '</code>、<code>'+'</code>、<code>'-'</code> 和 <code>'.'</code> 组成</li></ul><hr><h3 id="开始解题-2">开始解题:</h3><p>那么这道题的本质是<strong>模拟一个字符串解析器</strong>. 我们可以把它想象成一个机器人,它会从左到右扫描一个字符串,并根据一套严格的规定来决定自己该做什么.如果我们将这个过程分解,就会发现它非常符合逻辑,就像在Unity中编写一个角色的AI状态机一样.</p><p>我们可以把整个过程分为几个连续的<strong>状态</strong>或步骤:</p><h4 id="第一步-初始状态-寻找数字-状态">第一步: 初始状态 -&gt; &quot;寻找数字&quot;状态</h4><p>目标: 跳过所有无关紧要的前导空格.</p><ul><li><strong>机器人行为:</strong> 我们的机器人从字符串的第一个字符(索引 <code>0</code>)开始.它会问自己:“这个字符是空格吗?”<ul><li>如果是,它会简单的向前走一步(索引 <code>+1</code>),然后继续问同样的问题.</li><li>如果不是,或者已经走到了字符串的尽头,这个阶段就结束了.</li></ul></li><li><strong>关键点:</strong> 这个阶段是&quot;清理&quot;阶段,为后续真正的解析做准备.如果整个字符串都是空格,那么机器人走到头也找不到任何有用的东西,最终就应该返回 <code>0</code>.</li></ul><h4 id="第二步-寻找数字-状态-确认符号-状态">第二步: &quot;寻找数字&quot;状态 -&gt; &quot;确认符号&quot;状态</h4><p><strong>目标:</strong> 在找到第一个非空格字符后,判断数字的正负.</p><ul><li><p><strong>机器人行为:</strong> 机器人停在了第一个非空格字符上.它会检查这个字符:</p><ul><li>是 <code>'-'</code> 吗?如果是,它就在自己的小本本上记下“这是个负数”,然后向前走一步,准备读取数字.</li><li>是 <code>'+'</code> 吗?如果是,它记下“这是个正数”（或者什么都不记,因为默认就是正数）,然后也向前走一步.</li><li>如果既不是 <code>'-'</code> 也不是 <code>'+'</code>,机器人就认为这是一个正数,并且<strong>停在原地不动</strong>,因为这个字符可能就是数字本身.</li></ul></li><li><p><strong>关键点:</strong> 符号只在数字的最前面出现才有效.例如,对于 <code>&quot;  -42&quot;</code>,这个阶段会记录负号；但对于 <code>&quot;  4-2&quot;</code>,这个阶段会认为数字是正数,因为第一个非空格字符是 <code>'4'</code>.</p></li></ul><h4 id="第三步-确认符号-状态-转换数字-状态">第三步: &quot;确认符号&quot;状态 -&gt; &quot;转换数字&quot;状态</h4><p><strong>目标:</strong> 连续读取所有数字字符,并将它们组合成一个整数.</p><ul><li><p><strong>机器人行为:</strong> 现在机器人已经准备好读取核心数字了.它会继续向前扫描:</p><ul><li>“当前字符是数字（<code>'0'</code> 到 <code>'9'</code>）吗?”</li><li>如果是,它就执行一个核心操作:<strong><code>当前结果 = 当前结果 * 10 + 这个数字的值</code></strong>.<ul><li>例如,如果当前结果是 <code>4</code>,读到了新数字 <code>'2'</code>,那么新结果就是 <code>4 * 10 + 2 = 42</code>.</li><li>如果当前结果是 <code>42</code>,读到了新数字 <code>'3'</code>,那么新结果就是 <code>42 * 10 + 3 = 423</code>.</li></ul></li><li>如果当前字符<strong>不是</strong>数字,或者走到了字符串的尽头,这个阶段就立刻结束.</li></ul></li><li><p><strong>一个巨大的陷阱（核心难点）:整数溢出</strong>(第7题)</p><ul><li>C# 的 <code>int</code> 类型有范围限制,即 <code>[-2147483648, 2147483647]</code>.</li><li>在执行 <code>当前结果 = 当前结果 * 10 + 这个数字的值</code> 之前,我们必须<strong>预判</strong>这次计算是否会导致结果超出范围.</li><li><strong>如何预判?</strong><ul><li>假设我们正在构建一个正数.<code>int</code> 的最大值是 <code>2147483647</code>.</li><li>在乘以10之前,如果 <code>当前结果</code> 已经大于 <code>int.MaxValue / 10</code> (即 <code>214748364</code>),那么再乘以10必然溢出.</li><li>或者,如果 <code>当前结果</code> 正好等于 <code>int.MaxValue / 10</code> (即 <code>214748364</code>),那么我们就要看下一个数字了.如果这个数字大于 <code>int.MaxValue % 10</code> (即 <code>7</code>),那么加法操作就会导致溢出.</li></ul></li><li>如果预判到会溢出,就不能再继续计算了,必须立即停止并返回该方向的极值（正数返回 <code>int.MaxValue</code>,负数返回 <code>int.MinValue</code>）.</li></ul></li></ul><h4 id="第四步-转换数字-状态-最终返回-状态">第四步: &quot;转换数字&quot;状态 -&gt; &quot;最终返回&quot;状态</h4><p><strong>目标:</strong> 结合符号和计算出的数值,得到最终结果.</p><ul><li><p><strong>机器人行为:</strong> 数字读取循环结束后,我们得到了一个数值（在计算过程中我们一直把它当作正数处理）.</p><ul><li>现在,机器人拿出它的小本本,看看在第二步记录的符号是什么.</li><li>如果是“负数”,就把计算出的数值乘以 <code>-1</code>.</li><li>如果是“正数”,就保持原样.</li><li>最后,返回这个最终的整数.</li></ul></li><li><p><strong>特殊情况:</strong> 如果从头到尾都没有读到任何一个数字（例如 <code>&quot;words and 987&quot;</code> 或者 <code>&quot;  -abc&quot;</code>）,那么“转换数字”这个阶段根本就不会执行,我们计算出的数值会保持初始值 <code>0</code>.最终返回 <code>0</code>,完全符合题意.</p></li></ul><h4 id="总结一下逻辑">总结一下逻辑:</h4><p><code>开始</code> -&gt; <code>循环跳过空格</code> -&gt; <code>判断并记录符号</code> -&gt; <code>循环读取数字并处理溢出</code> -&gt; <code>结合符号返回结果</code></p><hr><h3 id="代码与讲解">代码与讲解:</h3><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Solution</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">int</span> <span class="title">MyAtoi</span>(<span class="params"><span class="built_in">string</span> s</span>)</span> &#123;</span><br><span class="line">        <span class="comment">// 1. 初始化变量</span></span><br><span class="line">        <span class="built_in">int</span> i = <span class="number">0</span>; <span class="comment">// 我们的“机器人”或指针,从字符串开头出发</span></span><br><span class="line">        <span class="built_in">int</span> sign = <span class="number">1</span>; <span class="comment">// 符号,默认为正</span></span><br><span class="line">        <span class="built_in">long</span> result = <span class="number">0</span>; <span class="comment">// 使用 long 类型来存储中间结果,方便检测溢出</span></span><br><span class="line">        <span class="built_in">int</span> n = s.Length;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 步骤一:丢弃前导空格</span></span><br><span class="line">        <span class="keyword">while</span> (i &lt; n &amp;&amp; s[i] == <span class="string">&#x27; &#x27;</span>) &#123;</span><br><span class="line">            i++;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 步骤二:检查符号</span></span><br><span class="line">        <span class="keyword">if</span> (i &lt; n &amp;&amp; (s[i] == <span class="string">&#x27;+&#x27;</span> || s[i] == <span class="string">&#x27;-&#x27;</span>)) &#123;</span><br><span class="line">            sign = (s[i] == <span class="string">&#x27;-&#x27;</span>) ? <span class="number">-1</span> : <span class="number">1</span>;</span><br><span class="line">            i++;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 4. 步骤三:转换数字并处理溢出</span></span><br><span class="line">        <span class="keyword">while</span> (i &lt; n &amp;&amp; <span class="built_in">char</span>.IsDigit(s[i])) &#123;</span><br><span class="line">            <span class="comment">// 将字符转换为数字</span></span><br><span class="line">            <span class="built_in">int</span> digit = s[i] - <span class="string">&#x27;0&#x27;</span>;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 核心:构建数字</span></span><br><span class="line">            result = result * <span class="number">10</span> + digit;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 核心:在构建过程中检查是否溢出</span></span><br><span class="line">            <span class="keyword">if</span> (sign * result &gt; <span class="built_in">int</span>.MaxValue) &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="built_in">int</span>.MaxValue;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (sign * result &lt; <span class="built_in">int</span>.MinValue) &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="built_in">int</span>.MinValue;</span><br><span class="line">            &#125;</span><br><span class="line">          </span><br><span class="line">            i++;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 5. 步骤四:结合符号并返回最终结果</span></span><br><span class="line">        <span class="comment">// 将 long 类型的结果乘以符号,并强制转换为 int</span></span><br><span class="line">        <span class="keyword">return</span> (<span class="built_in">int</span>)(sign * result);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="代码讲解">代码讲解:</h4><ol><li><p><strong><code>long</code> 数据类型</strong></p><ul><li><strong>是什么:</strong> <code>long</code> 是一个64位有符号整数类型,它的范围比32位的 <code>int</code> 大得多（大约是 -9 x 10^18 到 9 x 10^18）.</li><li><strong>为什么用在这里:</strong> 这是处理整数溢出问题的一个绝佳技巧.题目要求我们检测结果是否会超出 <code>int</code> 的范围.如果我们直接用 <code>int</code> 来累加,一旦溢出,它就会变成一个意想不到的负数（或正数）,我们就丢失了溢出前的信息.通过使用 <code>long</code> 这个“更大的容器”来存放计算结果,我们可以安全地进行 <code>result = result * 10 + digit</code> 操作.然后,在每一步都检查这个 <code>long</code> 类型的结果是否已经超出了 <code>int</code> 的范围.这让溢出判断变得非常简单和清晰.</li></ul></li><li><p>*<strong><code>int.MaxValue</code> 和 <code>int.MinValue</code></strong></p><ul><li><strong>是什么:</strong> 它们是 <code>System.Int32</code> 结构体中定义的两个公共静态常量（<code>public static const</code>）.<code>int.MaxValue</code> 的值是 <code>2,147,483,647</code>,<code>int.MinValue</code> 的值是 <code>-2,147,483,648</code>.</li><li><strong>如何使用:</strong> 它们为我们提供了 <code>int</code> 类型的精确边界,是进行范围判断和“截断”（Clamping）操作的权威标准.在代码中,我们用 <code>if (sign * result &gt; int.MaxValue)</code> 来判断是否超过了正数边界.这比自己手写 <code>2147483647</code> 要更具可读性,也更不容易出错.在Unity中,我们常用 <code>Mathf.Infinity</code>,这与 <code>int.MaxValue</code> 在概念上是类似的,都代表了某种类型的边界.</li></ul></li><li><p><strong><code>char.IsDigit(char c)</code></strong></p><ul><li><strong>是什么:</strong> 这是一个非常方便的静态方法,用于判断一个给定的字符是否是十进制数字（‘0’ 到 ‘9’）.</li><li><strong>为什么用它:</strong> 在C#中,我们当然可以写 <code>s[i] &gt;= '0' &amp;&amp; s[i] &lt;= '9'</code>.但这有几个小缺点:可读性稍差,且它隐含了字符编码是连续的假设（虽然对于数字来说总是如此）.使用 <code>char.IsDigit()</code> 是更推荐的“C#风格”写法,它意图明确,代码更干净.</li></ul></li><li><p><strong>字符的算术运算:<code>s[i] - '0'</code></strong></p><ul><li><strong>是什么:</strong> 这是C#（以及C/C++/Java）中一个非常经典和高效的技巧.在内部,<code>char</code> 类型实际上是存储为一个数字（其Unicode编码值）.幸运的是,所有数字字符 <code>'0', '1', '2', ... '9'</code> 的编码是连续的.</li><li><strong>如何工作:</strong> 因此,用一个数字字符的编码值减去 <code>'0'</code> 的编码值,得到的结果恰好就是这个数字的整数值.例如,<code>'5' - '0'</code> 的结果就是整数 <code>5</code>.这比 <code>int.Parse(s[i].ToString())</code> 这种先转成字符串再解析的方式要快得多,是性能敏感场景下的首选.</li></ul></li><li><p><strong>三元运算符:<code>condition ? value_if_true : value_if_false</code></strong></p><ul><li><p><strong>是什么:</strong> 这是一个紧凑的 <code>if-else</code> 语句的简写形式.</p></li><li><p><strong>在代码中的应用:</strong> <code>sign = (s[i] == '-') ? -1 : 1;</code> 这行代码等价于:</p><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (s[i] == <span class="string">&#x27;-&#x27;</span>) &#123;</span><br><span class="line">    sign = <span class="number">-1</span>;</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    sign = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>对于这种简单的赋值逻辑,三元运算符能让代码更简洁.</p></li></ul></li></ol><hr><h3 id="知识点总结-2">知识点总结:</h3><h4 id="1-边界条件与防御性编程">1. 边界条件与防御性编程</h4><ul><li><strong>核心思想:</strong> 永远不要相信输入.一个健壮的程序必须能处理各种预料之外的、不规范的、甚至是恶意的输入.</li><li><strong>在本题中的体现:</strong><ul><li><strong>空字符串或纯空格:</strong> <code>s = &quot;&quot;</code> 或 <code>s = &quot;   &quot;</code>.</li><li><strong>非数字开头:</strong> <code>s = &quot;words and 987&quot;</code>.</li><li><strong>符号位置不正确:</strong> <code>s = &quot;  + 42&quot;</code> vs <code>s = &quot;42+&quot;</code>.</li><li><strong>溢出:</strong> <code>s = &quot;2147483648&quot;</code> (比 <code>int.MaxValue</code> 大1).</li><li><strong>混合输入:</strong> <code>s = &quot;   -042a123&quot;</code>.</li></ul></li><li><strong>通用启示:</strong> 在编写任何函数或方法时,都要先思考:“最坏的输入情况是什么？”.在函数开头处理这些边缘情况（如检查 <code>null</code> 或空集合）,可以让我们的主逻辑更清晰、更安全.</li></ul><h4 id="2-使用“更大”的数据类型处理溢出">2. 使用“更大”的数据类型处理溢出</h4><ul><li><strong>核心思想:</strong> 当一个计算过程有可能超出某个数值类型的范围时,使用一个范围更大的类型作为“临时计算器”是一种简单而有效的策略.</li><li><strong>在本题中的体现:</strong> 我们使用 <code>long</code> (64位) 来计算一个最终要存入 <code>int</code> (32位) 的结果.这使得我们可以在不丢失信息的情况下,轻松地将中间结果与 <code>int.MaxValue</code> 和 <code>int.MinValue</code> 进行比较.</li><li><strong>通用启示:</strong> 这个技巧不限于 <code>int</code> 和 <code>long</code>.例如,在处理需要高精度的小数运算时,我们可能会选择使用 <code>decimal</code> 类型而不是 <code>float</code> 或 <code>double</code> 来避免精度损失.在处理图形学或物理计算时,有时为了中间步骤的精度,会使用 <code>double</code> 进行计算,最后再将结果转回 <code>float</code>.</li></ul><h4 id="3-状态机思想-State-Machine">3. 状态机思想 (State Machine)</h4><ul><li><strong>核心思想:</strong> 将一个复杂的过程分解为一系列离散的、定义清晰的“状态”,以及在这些状态之间转换的“规则”.</li><li><strong>在本题中的体现:</strong> 我们的解析过程可以看作一个简单的状态机:<ol><li><code>State_Skipping_Whitespace</code> (跳过空格状态)</li><li><code>State_Determining_Sign</code> (确定符号状态)</li><li><code>State_Reading_Digits</code> (读取数字状态)</li><li><code>State_Finished</code> (完成状态)<br>程序根据当前字符,从一个状态转换到下一个状态.</li></ol></li><li><strong>通用启示:</strong> 状态机是解决许多问题的强大模型,尤其是在游戏开发中.角色的AI（站立、行走、攻击、防御）、UI的交互流程（主菜单、设置、游戏中）、网络协议的握手过程等,都可以用状态机来清晰地建模和实现.</li></ul><hr><h3 id="练习题-2">练习题:</h3><h4 id="选择题-2">选择题</h4><p><strong>1. 在一个需要返回 <code>int</code> 的函数中,你正在累加一个可能很大的数字.为了防止计算过程中发生溢出,以下哪个是最佳实践？</strong></p><p>A. 在每次加法后,检查结果是否变成了负数,如果是,说明溢出了.<br>B. 使用 <code>try-catch</code> 块包裹加法运算,捕捉 <code>OverflowException</code> 异常.<br>C. 将累加器变量声明为 <code>long</code> 类型,每次累加后,检查该 <code>long</code> 值是否超出了 <code>int</code> 的范围.<br>D. 在进行加法前,使用 <code>int.MaxValue - currentValue &lt; numberToAdd</code> 的方式进行预判.</p><h4 id="简答题-2">简答题</h4><p><strong>2. 假设你需要编写一个函数 <code>ParseVector2(string s)</code>,它能将形如 <code>&quot;(1.5, -2.0)&quot;</code> 的字符串解析为一个 Unity 的 <code>Vector2</code> 对象.请简要描述你会如何运用“状态机”的思想来设计这个函数的解析逻辑？（不需要写代码,描述步骤即可）</strong></p><hr><h4 id="参考答案-2">参考答案</h4><ol><li><p><strong>答案:C 或 D 都是优秀的实践,但 C 更为通用和简单.</strong><br><strong>解析:</strong></p><ul><li>A 是不可靠的.对于正数溢出,结果会变成负数,但对于负数下溢,结果可能变成正数,逻辑复杂且容易出错.</li><li>B 在默认的 C# 编译设置下是行不通的.整数溢出默认不会抛出异常（为了性能）.你需要使用 <code>checked</code> 关键字才能让它抛出异常,这在性能敏感的循环中可能不是最佳选择.</li><li><strong>C (本题解法)</strong> 是最直观和简单的方法.它将问题从“如何检测溢出”转变为“如何比较大小”,逻辑清晰.</li><li><strong>D (预判法)</strong> 也是一种非常好的、不依赖更大类型的方法,性能很高.它直接在操作前判断本次操作是否会导致溢出.在某些不能使用更大类型的场景下,这是标准做法.</li></ul></li><li><p><strong>答案:</strong><br>我们可以将解析过程设计为以下几个状态:</p><ol><li><strong>寻找左括号状态:</strong> 从头开始,跳过所有空格,直到找到第一个 <code>'('</code>.如果没找到,则格式错误.</li><li><strong>解析X值状态:</strong> 从 <code>'('</code> 之后开始,解析一个浮点数（这本身就可以复用 atoi 的逻辑,但要处理小数点）.直到遇到 <code>','</code> 字符.将解析出的值存为 X.</li><li><strong>寻找逗号状态:</strong> 在解析完 X 后,跳过可能的空格,寻找 <code>','</code>.如果没找到,则格式错误.</li><li><strong>解析Y值状态:</strong> 从 <code>','</code> 之后开始,用与解析 X 值相同的方法解析第二个浮点数.直到遇到 <code>')'</code> 字符.将解析出的值存为 Y.</li><li><strong>寻找右括号状态:</strong> 在解析完 Y 后,跳过可能的空格,寻找 <code>')'</code>.如果没找到,则格式错误.</li><li><strong>完成状态:</strong> 成功找到所有部分,用解析出的 X 和 Y 构建并返回 <code>new Vector2(x, y)</code>.</li></ol><p>在任何一步失败（比如找不到预期的字符,或数字格式错误）,函数都应立即停止并返回一个错误或默认值.</p></li></ol><hr><h3 id="可能的实际应用">可能的实际应用:</h3><ol><li><p><strong>配置文件解析:</strong> 游戏中的配置文件（<code>.ini</code>, <code>.txt</code>, <code>.json</code>, <code>.xml</code>）经常包含需要被读取为数值的设置,如音量大小、图形质量等级、玩家初始生命值等.一个健壮的解析器是读取这些配置的基础.</p></li><li><p><strong>自定义编辑器工具:</strong> 在Unity Editor中,我们可能会创建一些工具窗口,让策划或美术在输入框里填写数值（例如,批量修改一堆怪物的血量）.我们需要将输入框中的字符串安全地转换为整数或浮点数,<code>atoi</code> 的逻辑是这个过程的核心.</p></li><li><p><strong>网络通信:</strong> 从服务器接收的数据包可能是以文本协议（如HTTP）传输的.解析协议头或消息体中的数字字段时,就需要用到类似 <code>atoi</code> 的健壮的字符串到数字的转换逻辑.</p></li><li><p><strong>调试控制台/GM命令:</strong> 游戏内通常会有一个调试控制台,允许开发者输入命令,如 <code>set_player_hp 1000</code>.解析这个命令中的参数 <code>1000</code>,就需要一个能处理各种输入的 <code>atoi</code> 函数.</p></li></ol>]]></content>
      
      
      <categories>
          
          <category> 力扣刷题 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 算法 </tag>
            
            <tag> 力扣 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>力扣刷题日常(5-6)</title>
      <link href="/posts/95bdd374/"/>
      <url>/posts/95bdd374/</url>
      
        <content type="html"><![CDATA[<h1>力扣刷题日常(5-6)</h1><h2 id="第五题-最长回文子串-中等">第五题: 最长回文子串(中等)</h2><h3 id="原题">原题:</h3><p>给你一个字符串 <code>s</code>,找到 <code>s</code> 中最长的 回文 子串.</p><p><strong>示例 1：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入：s = &quot;babad&quot;</span><br><span class="line">输出：&quot;bab&quot;</span><br><span class="line">解释：&quot;aba&quot; 同样是符合题意的答案.</span><br></pre></td></tr></table></figure><p><strong>示例 2：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入：s = &quot;cbbd&quot;</span><br><span class="line">输出：&quot;bb&quot;</span><br></pre></td></tr></table></figure><p><strong>提示：</strong></p><ul><li><code>1 &lt;= s.length &lt;= 1000</code></li><li><code>s</code> 仅由数字和英文字母组成</li></ul><h3 id="概念解释">概念解释:</h3><ul><li><strong>回文性:</strong> 如果字符串向前和向后读都相同,则它满足 <strong>回文性</strong>.</li><li><strong>子字符串:</strong> <strong>子字符串</strong> 是字符串中连续的 <strong>非空</strong> 字符序列.</li></ul><h3 id="原题提示-附翻译">原题提示(附翻译):</h3><ol><li><p>How can we reuse a previously computed palindrome to compute a larger palindrome?(我们如何利用之前计算出的回文序列来计算出更大的回文呢？)</p></li><li><p>If “aba” is a palindrome, is “xabax” a palindrome? Similarly is “xabay” a palindrome?(如果“aba”是一个回文,那么“xabax”也是一个回文吗？同样地,“xabay”也是一个回文吗？)</p></li><li><p>Complexity based hint: If we use brute-force and check whether for every start and end position a substring is a palindrome we have O(n^2) start - end pairs and O(n) palindromic checks. Can we reduce the time for palindromic checks to O(1) by reusing some previous computation.(基于复杂度的提示：如果我们采用穷举法,检查每一个起始位置和结束位置之间的子字符串是否为回文串,那么我们将有 O(n^2) 对起始 - 结束位置组合,以及 O(n) 次回文检查.我们能否通过重复利用之前的计算来将回文检查的时间复杂度降低到 O(1) 呢？)</p></li></ol><hr><h3 id="开始解题">开始解题:</h3><h4 id="方法一-暴力法">方法一: 暴力法</h4><p>我们能想到的第一种办法肯定就是暴力破解法了,我们可以找出所有的子串,然后逐个判断它们是不是回文,然后在所有回文中挑一个最长的.</p><ul><li>第一步: 找出所有子串. 我们可以用两个嵌套循环,一个确定开头位置 <code>i</code> ,一个确认结尾位置 <code>j</code>.这样就能得到 <code>s</code>  所有的子串.</li><li>第二步: 判断是否为回文. 对于每个子串,我们再用两个指针,一个从头往后,一个从后往前,看对应字符是否都相等.</li><li>第三步: 记录最长. 在这个过程中,我们用一个变量记录下最长的那个回文串.</li></ul><p>那为什么这个办法不好呢</p><p>假设字符串的长度为 <code>n</code>.</p><ul><li>找所有子串,时间复杂度为 O(n²)</li><li>对每个子串判断是否为回文,平均长度均为 n/2,所以判断一次的复杂度为 O(n).</li><li>总的时间复杂度就是 O(n³)</li></ul><p>那对于题目中的条件 <code>n&lt;=1000</code> ,如果按照1000来算,我们就需要计算10亿次,这个计算量肯定会超时的,所以我们必须要进行优化.</p><p>再看原题中给我们的提示,我们有没有办法讲 O(n) 的回文检查降低到 O(1) 呢?</p><h4 id="方法二-中心拓展法">方法二: 中心拓展法</h4><p>这个方法很巧妙,用到了回文串的核心特性: 对称性.</p><p>无论多长的回文串,它必定有一个&quot;中心&quot;.这个中心可能是单个字符,也可能是两个字符之间的空隙</p><ul><li>奇数长度的回文串: 中心是唯一的那个字符.</li><li>偶数长度的回文串: 中心是中键那两个相同字符之间的&quot;缝隙&quot;.</li></ul><p><strong>核心逻辑:</strong></p><p>我们可以遍历每一个可能的&quot;中心&quot;,然后从这个中心向两边同时拓展,看看能拓展出多长的回文串.</p><p>具体步骤:</p><ol><li><p><strong>遍历所有的中心点:</strong> 我们从头到尾遍历输入字符串 <code>s</code> .对于字符串中的每一个为止 <code>i</code>,它都可能成为一个回文串的中心.</p></li><li><p><strong>处理两种中心类型:</strong></p><ul><li><p>情况一: 以 <code>s[i]</code> 单个字符为中心</p><p>我们需要设置两个指针 <code>left = i</code>, <code>right = i</code>.然后我们向两边扩展: <code>left</code> 想左移动(<code>left--</code>), <code>right</code> 向右移动(<code>right++</code>). 只要 <code>left</code> 和 <code>right</code> 没有越界,并且 <code>s[left]</code> 和 <code>s[right]</code> 相等, 这就说明我们找到了一个更长的回文串.</p></li><li><p>情况二: 以 <code>s[i]</code> 和 <code>s[i+1]</code> 之间的空隙为中心</p><p>我们设置两个指针 <code>left = i</code>, <code>right = i + 1</code>. 然后同样向两边扩展.这可以找出所有偶数长度的回文串.</p></li></ul></li><li><p><strong>记录和更新结果:</strong></p><p>在每次扩展后,我们都会得到一个以当前中心能扩展出的最长回文串的长度.我们只需要一个变量来记录<strong>全局的最长长度</strong>,以及对应的<strong>起始位置</strong>.每次找到一个比当前记录更长的回文串时,就更新记录.</p></li><li><p><strong>得出最终答案:</strong></p><p>遍历完所有可能的中心点之后,我们记录下的起始位置和长度对应的子串,就是整个字符串的最长回文子串</p></li></ol><p>这个方法的效率如何?</p><p>我们有 n 个单字符中心和 n-1 个双字符中心,总共 2n-1 个中心,对于每个中心,我们最大向外扩展 n/2次. 所以总的时间复杂度是 O(n²),空间复杂度为 O(1)(因为我们只需要几个变量来存储指针和结果).这对于 n=1000 的情况是完全可以接受的.</p><hr><h3 id="代码实现">代码实现:</h3><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Solution</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">string</span> <span class="title">LongestPalindrome</span>(<span class="params"><span class="built_in">string</span> s</span>)</span> &#123;</span><br><span class="line">        <span class="comment">// 边界条件检查</span></span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">string</span>.IsNullOrEmpty(s) || s.Length &lt; <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">&quot;&quot;</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 用两个变量记录最长回文子串的起始和结束索引</span></span><br><span class="line">        <span class="comment">// 这样做比直接存储字符串更高效,可以避免在循环中频繁创建子字符串对象</span></span><br><span class="line">        <span class="built_in">int</span> start = <span class="number">0</span>;</span><br><span class="line">        <span class="built_in">int</span> end = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 遍历字符串中的每一个字符,以其为中心向两边扩展</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">0</span>; i &lt; s.Length; i++) &#123;</span><br><span class="line">            <span class="comment">// 情况一：回文串长度为奇数,中心是 s[i]</span></span><br><span class="line">            <span class="built_in">int</span> len1 = ExpandAroundCenter(s, i, i);</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 情况二：回文串长度为偶数,中心是 s[i] 和 s[i+1] 之间</span></span><br><span class="line">            <span class="built_in">int</span> len2 = ExpandAroundCenter(s, i, i + <span class="number">1</span>);</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 取两种情况中较长的那一个</span></span><br><span class="line">            <span class="built_in">int</span> len = Math.Max(len1, len2);</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 如果找到了更长的回文串,则更新其起始和结束位置</span></span><br><span class="line">            <span class="keyword">if</span> (len &gt; end - start) &#123;</span><br><span class="line">                <span class="comment">// 根据中心点 i 和长度 len 计算新的 start 和 end</span></span><br><span class="line">                start = i - (len - <span class="number">1</span>) / <span class="number">2</span>;</span><br><span class="line">                end = i + len / <span class="number">2</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 循环结束后,根据记录的 start 和 end 截取最长的回文子串</span></span><br><span class="line">        <span class="comment">// Substring 的第二个参数是长度</span></span><br><span class="line">        <span class="keyword">return</span> s.Substring(start, end - start + <span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="built_in">int</span> <span class="title">ExpandAroundCenter</span>(<span class="params"><span class="built_in">string</span> s, <span class="built_in">int</span> left, <span class="built_in">int</span> right</span>)</span> &#123;</span><br><span class="line">        <span class="comment">// 当指针没有越界,并且左右指针指向的字符相同时,继续扩展</span></span><br><span class="line">        <span class="keyword">while</span> (left &gt;= <span class="number">0</span> &amp;&amp; right &lt; s.Length &amp;&amp; s[left] == s[right]) &#123;</span><br><span class="line">            left--;  <span class="comment">// 左指针向左移动</span></span><br><span class="line">            right++; <span class="comment">// 右指针向右移动</span></span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 循环结束时,left 和 right 指向的是回文串边界之外的第一个不匹配字符</span></span><br><span class="line">        <span class="comment">// 所以回文串的长度是 right - left - 1</span></span><br><span class="line">        <span class="keyword">return</span> right - left - <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="代码细节讲解">代码细节讲解:</h4><ul><li><p><strong><code>int start = 0; int end = 0;</code></strong>:</p><ul><li>我们不再保存最长的字符串 <code>longestPalindrome</code>,而是只保存它的<strong>起始和结束索引</strong>.</li><li><strong>为什么这样做？</strong> 在循环中,如果每次找到更长的回文串都调用 <code>Substring</code> 来创建一个新的字符串对象,会产生大量的<strong>内存分配</strong> .对于注重性能的场景（比如Unity游戏循环）,这是需要尽量避免的.通过只记录两个 <code>int</code> 变量,我们在整个循环过程中<strong>零内存分配</strong>,直到最后返回结果时才创建一次字符串对象.这是一种非常重要的优化思想.</li></ul></li><li><p><strong><code>start = i - (len - 1) / 2;</code> 和 <code>end = i + len / 2;</code></strong>:</p><ul><li>这是这段代码中最需要技巧的一步：根据中心 <code>i</code> 和长度 <code>len</code> 推算出回文串的起止索引.这个公式对奇数和偶数长度的回文串都适用,非常巧妙.</li><li><strong>举例（奇数）</strong>: <code>s=&quot;aba&quot;</code>, 中心 <code>i=1</code>, 长度 <code>len=3</code>.<ul><li><code>start = 1 - (3 - 1) / 2 = 1 - 1 = 0</code></li><li><code>end = 1 + 3 / 2 = 1 + 1 = 2</code> (整数除法,<code>3/2</code>得1)</li><li>结果是索引 <code>[0, 2]</code>,正确.</li></ul></li><li><strong>举例（偶数）</strong>: <code>s=&quot;abba&quot;</code>, 中心在 <code>i=1</code> 和 <code>i=2</code> 之间, 长度 <code>len=4</code>.<ul><li><code>start = 1 - (4 - 1) / 2 = 1 - 1 = 0</code> (整数除法,<code>3/2</code>得1)</li><li><code>end = 1 + 4 / 2 = 1 + 2 = 3</code></li><li>结果是索引 <code>[0, 3]</code>,正确.</li></ul></li></ul></li><li><p><strong><code>return right - left - 1;</code></strong>:</p><ul><li>这是另一个巧妙之处.当 <code>while</code> 循环结束时,<code>left</code> 和 <code>right</code> 指向的位置是<strong>刚好不满足</strong>回文条件的地方.</li><li><strong>举例</strong>: <code>s=&quot;aba&quot;</code>, 中心 <code>i=1</code>.<ol><li>初始: <code>left=1</code>, <code>right=1</code>.循环条件满足.</li><li>扩展后: <code>left=0</code>, <code>right=2</code>.<code>s[0]=='a'</code>, <code>s[2]=='a'</code>.循环条件满足.</li><li>扩展后: <code>left=-1</code>, <code>right=3</code>.<code>left &lt; 0</code>,循环终止.</li></ol></li><li>此时,<code>left</code> 为 -1,<code>right</code> 为 3.真正的回文串是索引从 0 到 2.其长度是 <code>3</code>.</li><li>计算 <code>right - left - 1 = 3 - (-1) - 1 = 4 - 1 = 3</code>.公式正确.</li></ul></li></ul><hr><h3 id="知识点总结">知识点总结:</h3><h4 id="1-C-性能细节：-的“短路求值”">1. C#性能细节：<code>&amp;&amp;</code> 的“短路求值”</h4><p>这是一个非常重要但容易被忽略的性能优化点.</p><ul><li>对于逻辑与 <code>&amp;&amp;</code>：如果第一个条件为 <code>false</code>,则整个表达式结果必为 <code>false</code>,C# <strong>不会</strong>再去计算第二个条件.</li><li>对于逻辑或 <code>||</code>：如果第一个条件为 <code>true</code>,则整个表达式结果必为 <code>true</code>,C# <strong>不会</strong>再去计算第二个条件.</li><li><strong>应用</strong>：在写 <code>if (A &amp;&amp; B)</code> 时,应该把<strong>计算成本更低</strong>或<strong>更容易为 <code>false</code></strong> 的条件放在前面.这可以帮助程序“跳过”很多昂贵的计算.</li></ul><h4 id="2-内存管理：理解string的不可变性与GC">2. 内存管理：理解<code>string</code>的不可变性与GC</h4><p>这对于Unity开发者来说是至关重要的知识.</p><ul><li><strong>不可变性 (Immutability)</strong>：在C#中,<code>string</code> 类型是不可变的.任何对字符串的修改（如 <code>Substring</code>, <code>Replace</code>, <code>+</code>拼接）都不会改变原始字符串,而是会创建一个<strong>全新的字符串对象</strong>.</li><li><strong>GC Allocation</strong>：在循环中频繁创建新字符串,会在内存堆上产生大量需要被回收的对象.这会给垃圾回收器（GC）带来压力,在Unity中可能导致游戏画面周期性的卡顿（掉帧）.</li><li><strong>优化策略</strong>：在我们的高效解法中,通过只记录 <code>start</code> 和 <code>end</code> 两个整数（值类型）,避免了在循环中创建任何新的字符串对象,直到最后才生成一次结果.这是典型的用空间换时间、避免GC的优化思路.</li></ul><h4 id="3-双指针技术：一种强大的线性结构处理工具">3. 双指针技术：一种强大的线性结构处理工具</h4><p>“中心扩展”和“判断回文”都用到了双指针.这是一种非常通用的算法技巧.</p><ul><li><strong>核心思想</strong>：在数组、列表或字符串等线性结构上,使用两个指针从不同位置（如两端、一快一慢）开始,同步或异步移动,以完成特定的任务.</li><li><strong>常见应用</strong>：反转数组/字符串、寻找有序数组中和为特定值的两个数、滑动窗口问题等.它通常能将O(n²)的复杂度降低到O(n).</li></ul><hr><h3 id="练习题">练习题:</h3><h4 id="选择题">选择题</h4><p><strong>1. 观察以下C#代码,<code>PerformExpensiveCheck()</code> 方法会被调用吗？</strong></p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">bool</span> isReady = <span class="literal">false</span>;</span><br><span class="line"><span class="function"><span class="built_in">bool</span> <span class="title">PerformExpensiveCheck</span>()</span> </span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 假设这是一个非常耗时的操作</span></span><br><span class="line">    Console.WriteLine(<span class="string">&quot;昂贵的操作被执行了！&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (isReady &amp;&amp; PerformExpensiveCheck()) </span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// ... do something</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>A. 会被调用</p><p>B. 不会被调用</p><p>C. 取决于编译器版本</p><p>D. 代码会报错</p><p><strong>2. 在Unity游戏的一个频繁执行的<code>Update</code>方法中,以下哪种操作最有可能导致性能卡顿问题？</strong></p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 假设在一个循环内</span></span><br><span class="line"><span class="built_in">string</span> logMessage = <span class="string">&quot;Player &quot;</span> + playerName + <span class="string">&quot; scored &quot;</span> + score + <span class="string">&quot; points.&quot;</span>;</span><br></pre></td></tr></table></figure><p>A. 整数 <code>score</code> 的加法运算</p><p>B. 字符串变量 <code>playerName</code> 的读取</p><p>C. 使用 <code>+</code> 号进行多次字符串拼接</p><p>D. <code>logMessage</code> 变量的赋值操作</p><p><strong>3. “双指针”技术最不适合解决以下哪类问题？</strong></p><p>A. 在一个已排序的数组中,找出两个和为100的数.</p><p>B. 将一个字符串原地反转.</p><p>C. 在一个二叉搜索树中查找一个特定的值.</p><p>D. 判断一个链表是否存在环.</p><hr><h4 id="简答题">简答题</h4><p><strong>1. 在我们的“中心扩展法”实现中,为什么我们选择在循环里只更新 <code>start</code> 和 <code>end</code> 两个整数索引,而不是直接更新一个 <code>string</code> 类型的变量来保存当前最长的回文串？这种做法在Unity开发中有什么特别重要的意义？</strong></p><p><strong>2. 将“从中心扩展”的逻辑封装成一个独立的私有方法 <code>ExpandAroundCenter</code> 有哪些好处？</strong></p><hr><h4 id="参考答案">参考答案:</h4><h5 id="选择题答案与解析">选择题答案与解析</h5><p><strong>1. 观察以下C#代码,<code>PerformExpensiveCheck()</code> 方法会被调用吗？</strong></p><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">bool</span> isReady = <span class="literal">false</span>;</span><br><span class="line"><span class="function"><span class="built_in">bool</span> <span class="title">PerformExpensiveCheck</span>()</span> </span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 假设这是一个非常耗时的操作</span></span><br><span class="line">    Console.WriteLine(<span class="string">&quot;昂贵的操作被执行了！&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (isReady &amp;&amp; PerformExpensiveCheck()) </span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// ... do something</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>答案：B. 不会被调用</strong></p><p><strong>解析：</strong><br>这道题考察的是C#中逻辑与操作符 <code>&amp;&amp;</code> 的 <strong>“短路求值” (Short-circuiting)</strong> 特性.</p><ol><li><code>if</code> 语句首先检查 <code>&amp;&amp;</code> 左边的条件 <code>isReady</code>.</li><li><code>isReady</code> 的值是 <code>false</code>.</li><li>因为 <code>false &amp;&amp; (任何东西)</code> 的结果都必然是 <code>false</code>,所以C#为了效率,<strong>不会再继续向右执行</strong> <code>PerformExpensiveCheck()</code> 方法.</li><li>因此,这个昂贵的方法根本不会被调用,控制台也不会打印任何信息.</li></ol><hr><p><strong>2. 在Unity游戏的一个频繁执行的<code>Update</code>方法中,以下哪种操作最有可能导致性能卡顿问题？</strong></p><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 假设在一个循环内</span></span><br><span class="line"><span class="built_in">string</span> logMessage = <span class="string">&quot;Player &quot;</span> + playerName + <span class="string">&quot; scored &quot;</span> + score + <span class="string">&quot; points.&quot;</span>;</span><br></pre></td></tr></table></figure><p><strong>答案：C. 使用 <code>+</code> 号进行多次字符串拼接</strong></p><p><strong>解析：</strong><br>这道题考察的是对C# <code>string</code> 类型不可变性及其性能影响的理解.</p><ol><li>C#中的 <code>string</code> 是<strong>不可变 (immutable)</strong> 的.</li><li>每次使用 <code>+</code> 号拼接字符串时,都不是在原地修改,而是会创建一个<strong>全新的字符串对象</strong>来存储结果.</li><li>上面这行代码实际上会创建多个中间字符串对象（例如 <code>&quot;Player &quot; + playerName</code> 的结果,这个结果再和 <code>&quot; scored &quot;</code> 拼接的结果,等等）.</li><li>在 <code>Update</code> 这种每帧都执行的方法里,这样做会瞬间产生大量临时的字符串对象.这些对象都需要<strong>垃圾回收器 (GC)</strong> 来清理,GC的运行会暂停游戏主线程,从而导致<strong>性能下降和画面卡顿</strong>.在Unity性能分析器(Profiler)中,这会表现为很高的 <strong>GC Allocation</strong>.</li></ol><hr><p><strong>3. “双指针”技术最不适合解决以下哪类问题？</strong><br>A. 在一个已排序的数组中,找出两个和为100的数.<br>B. 将一个字符串原地反转.<br>C. 在一个二叉搜索树中查找一个特定的值.<br>D. 判断一个链表是否存在环.</p><p><strong>答案：C. 在一个二叉搜索树中查找一个特定的值.</strong></p><p><strong>解析：</strong><br>这道题考察的是对“双指针”技术适用范围的理解.双指针通常用于<strong>线性数据结构</strong>（如数组、链表、字符串）.</p><ul><li><strong>A (正确应用):</strong> 这是双指针的经典应用.一个指针从头开始,一个指针从尾开始,根据和的大小向中间移动.</li><li><strong>B (正确应用):</strong> 也是经典应用.一个指针在头,一个在尾,交换字符并向中间移动.</li><li><strong>D (正确应用):</strong> 这是双指针的一种变体,称为“快慢指针”.一个指针每次移动一步,另一个移动两步,如果它们能相遇,就说明链表有环.</li><li><strong>C (不适合):</strong> 二叉搜索树是<strong>树形（非线性）数据结构</strong>.在其中查找值,通常是从根节点开始,根据值的大小关系决定向左子树还是右子树移动,这只需要一个指针或引用来追踪当前节点.它不符合双指针协同工作的模式.</li></ul><hr><h5 id="简答题参考答案与解析">简答题参考答案与解析</h5><ol><li>因为C#中的<code>string</code>是不可变的.如果在循环中每次找到更长的回文串时,都通过<code>s.Substring()</code>来创建一个新的字符串并赋值给结果变量,会频繁地在内存堆上分配新对象.这会导致大量的<strong>垃圾回收（GC）开销</strong>.在Unity中,GC的执行会暂停主线程,导致游戏画面掉帧和卡顿.通过只记录<code>start</code>和<code>end</code>两个轻量的整数（值类型）,我们在整个查找过程中避免了任何不必要的内存分配,只在函数最后返回时创建一次最终的字符串对象,从而保证了程序的性能和流畅度.</li><li>主要有三个好处：<ol><li><strong>代码复用 (Reusability)</strong>：这个逻辑需要被调用两次——一次用于奇数长度的回文（中心为<code>i, i</code>）,一次用于偶数长度的回文（中心为<code>i, i+1</code>）.封装成方法避免了代码重复.</li><li><strong>可读性和模块化 (Readability &amp; Modularity)</strong>：主函数 <code>LongestPalindrome</code> 的逻辑变得非常清晰：遍历中心 -&gt; 调用辅助函数获取长度 -&gt; 比较并更新结果.这使得代码更容易理解和维护.</li><li><strong>单一职责原则 (Single Responsibility Principle)</strong>：<code>ExpandAroundCenter</code> 方法只负责一件事情——从一个给定的中心点找出最长的回文串.这让代码结构更健康,也更容易进行单元测试.</li></ol></li></ol><hr><h3 id="可能的实际应用">可能的实际应用:</h3><p>这个算法的实际应用点不多,故直接略过.</p><hr><h2 id="第六题-Z-字形变换-中等">第六题: Z 字形变换(中等)</h2><h3 id="原题-2">原题:</h3><p>将一个给定字符串 <code>s</code> 根据给定的行数 <code>numRows</code> ,以从上往下、从左到右进行 Z 字形排列.</p><p>比如输入字符串为 <code>&quot;PAYPALISHIRING&quot;</code> 行数为 <code>3</code> 时,排列如下：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">P   A   H   N</span><br><span class="line">A P L S I I G</span><br><span class="line">Y   I   R</span><br></pre></td></tr></table></figure><p>之后,你的输出需要从左往右逐行读取,产生出一个新的字符串,比如：<code>&quot;PAHNAPLSIIGYIR&quot;</code>.</p><p>请你实现这个将字符串进行指定行数变换的函数：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">string convert(string s, int numRows);</span><br></pre></td></tr></table></figure><p><strong>示例 1：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入：s = &quot;PAYPALISHIRING&quot;, numRows = 3</span><br><span class="line">输出：&quot;PAHNAPLSIIGYIR&quot;</span><br></pre></td></tr></table></figure><p><strong>示例 2：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">输入：s = &quot;PAYPALISHIRING&quot;, numRows = 4</span><br><span class="line">输出：&quot;PINALSIGYAHRPI&quot;</span><br><span class="line">解释：</span><br><span class="line">P     I    N</span><br><span class="line">A   L S  I G</span><br><span class="line">Y A   H R</span><br><span class="line">P     I</span><br></pre></td></tr></table></figure><p><strong>示例 3：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入：s = &quot;A&quot;, numRows = 1</span><br><span class="line">输出：&quot;A&quot;</span><br></pre></td></tr></table></figure><p><strong>提示：</strong></p><ul><li><code>1 &lt;= s.length &lt;= 1000</code></li><li><code>s</code> 由英文字母（小写和大写）、<code>','</code> 和 <code>'.'</code> 组成</li><li><code>1 &lt;= numRows &lt;= 1000</code></li></ul><hr><h3 id="开始解题-2">开始解题:</h3><h4 id="方法一-模拟法">方法一: 模拟法</h4><p>这道题目考察的是一种&quot;<strong>变换</strong>&quot;,只不过是针对字符在逻辑空间中的位置.</p><p>我们先不谈代码,先专注于理解这个变换的逻辑</p><p>想象一下,我们并不是在写代码,而是亲自用手排列这些字母,我们应该怎么做</p><p>我们的核心目标: 我们需要确定输入字符串 s 中的每一个在字符,最终应该属于Z字形的哪一行. 只要我们能够把所有字符正确地&quot;<strong>分拣</strong>&quot;到对应的行里,最后再把每一行的字符拼接起来,问题就解决了.</p><p>模拟手工分拣过程:</p><ol><li><strong>准备&quot;行&quot;容器:</strong><ul><li>假设 numRows 为 3 .我们想象我们面前有三个篮子,分别标记为第0行、第1行、第2行.</li><li>我们的任务就是遍历输入字符串 <code>PAYPALISHIRING</code>,把每个字母一次放入正确的篮子里.</li></ul></li><li><strong>定义移动规则</strong><ul><li>我们的笔尖开始时位于第0行.</li><li>我们写下第一个字母 P 到第0行.</li><li>然后笔向下移动到第1行,写下 A.</li><li>在向下移动到第2行,写下 Y.</li><li>现在笔已经到了最下面一行(第 <code>numRows - 1</code> 行),它不能继续往下走了.所以,它必须改变方向,开始向上移动.</li><li>笔移动到第1行,写下 <code>P</code>.</li><li>再向上移动到第0行,写下 <code>A</code>.</li><li>现在笔又到了最上面一行（第0行）,它不能再往上走了.所以,它再次改变方向,开始<strong>向下</strong>移动.</li><li>这个“向下-向上-向下”的过程会一直重复,直到所有字符都被写完.</li></ul></li></ol><p>把这个过程抽象成算法逻辑</p><ol><li><strong>初始化:</strong><ul><li>创建 numRows 个可变的字符串容器(比如 StringBuilder),用来存放每一行的字符</li><li>我们需要一个变量来追踪当前应该在哪一行添加字符,我们叫它 currentRow,初始值为 0.</li><li>我们还需要一个变量来表示当前的移动方向,比如一个布尔值 goingDown.</li></ul></li><li><strong>遍历与放置:</strong><ul><li>我们从头到尾遍历输入字符串 s 的每一个字符</li><li>对于当前字符,我们将它追加到 <code>rows[currentRow]</code> 这个容器的末尾.</li><li>放好字符后,我们需要决定下一个字符应该去哪一行.这就要更新 currentRow了.<ul><li>方向判断: 什么时候需要改变方向? 当 currentRow 到达顶部(第0行)或底部(第 numRows - 1 行)时.</li><li>更新 currentRow:<ul><li>如果当前方向是“向下”（<code>goingDown</code> 为 <code>true</code>）,那么 <code>currentRow</code> 就加1.</li><li>如果当前方向是“向上”（<code>goingDown</code> 为 <code>false</code>）,那么 <code>currentRow</code> 就减1.</li></ul></li></ul></li></ul></li><li><strong>收尾:</strong><ul><li>当所有字符都遍历并放置完毕后,我们 <code>numRows</code> 个容器里就分别装好了每一行的内容.</li><li>最后,我们只需要按顺序（从第0个容器到最后一个）将它们的内容拼接成一个最终的字符串,就是我们的答案.</li></ul></li></ol><p><strong>特殊情况/边缘情况处理:</strong></p><ul><li>如果 <code>numRows</code> 是 <code>1</code>,那么Z字形就是一条直线,不需要任何变换,直接返回原字符串 <code>s</code> 即可.</li><li>如果字符串的长度小于或等于 <code>numRows</code>,那么它也构不成一个完整的“Z”形,每个字符占一行,同样直接返回原字符串 <code>s</code>.</li></ul><h4 id="方法二-索引计算法">方法二: 索引计算法</h4><p>那么这种方法的核心事项就是: 不再模拟字符的Z字形路径,而是通过数学规律,直接计算出每一行所包含的字符在原字符串中的索引,然后按顺序将它们填入一个预先分配好的结果数组中.</p><p>这就像在Unity中,我们不去模拟一个物体从A点移动到B点,而是直接计算出B点的坐标,然后设置 <code>transform.position = B</code>.效率天差地别.</p><p>我们还是以 <code>s = &quot;PAYPALISHIRING&quot;</code>, <code>numRows = 4</code> 为例来分解这个过程.</p><p>第一步: 发现规律,定义&quot;周期&quot;</p><p>我们把字符和它们在原字符串中的索引画出来:</p><p><img src="https://image.nfasystem.top/img-nfa/2025%2F07%2Fb997fa9af928279cd5fce5dc58cce6d6.ext.jpg" alt="cd8cc44131b8b5b872c5b7539707a4ca"></p><p>我们仔细观察,我们会发现一个重复的模式,一个完整的&quot;V&quot;字形(一列垂直向下的,加上一列倾斜向上的)构成了一个周期.</p><p>一个周期由两部分组成：</p><ol><li><p><strong>一根垂直向下的“竖线”</strong>：从第0行到第 <code>numRows - 1</code> 行.它包含的字符数是 <code>numRows</code> 个.</p><ul><li>在我们的例子中,是 <code>P(0), A(1), Y(2), P(3)</code>,共 <strong>4</strong> 个字符.</li></ul></li><li><p><strong>一根倾斜向上的“斜线”</strong>：它连接着V字的底部和下一个V字的顶部.</p><ul><li><strong>关键点</strong>：这条斜线<strong>不包含</strong>V字的最低点和最高点,因为这两个点已经被“竖线”占据了.</li><li>所以,它填充的行是从第 <code>numRows - 2</code> 行到第 <code>1</code> 行.</li><li>它包含的字符数是 <code>(numRows - 2) - 1 + 1 = numRows - 2</code> 个.</li><li>在我们的例子中,是 <code>A(4)</code> (在第2行) 和 <code>L(5)</code> (在第1行),共 <code>4 - 2 = 2</code> 个字符.</li></ul></li></ol><p><strong>周期总长度 (cycleLen):</strong><br>一个周期内所有字符的总数 = (竖线字符数) + (斜线字符数)<br><code>cycleLen = (numRows) + (numRows - 2) = 2 * numRows - 2</code></p><p>对于 <code>numRows = 4</code>,<code>cycleLen = 2 * 4 - 2 = 6</code>.这意味着,在原字符串中,每隔6个字符,模式就会重复一次.<code>P(0)</code> 和 <code>I(6)</code> 就是相隔一个周期.</p><p>这部分的逻辑需要下面结合代码讲解</p><hr><h3 id="代码实现-2">代码实现:</h3><h4 id="模拟法：">模拟法：</h4><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Solution</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">string</span> <span class="title">Convert</span>(<span class="params"><span class="built_in">string</span> s, <span class="built_in">int</span> numRows</span>)</span> &#123;</span><br><span class="line">        <span class="comment">// 1. 处理特殊情况（Edge Case）</span></span><br><span class="line">        <span class="comment">// 如果行数是1,或者字符串长度不足以进行Z字形排列,</span></span><br><span class="line">        <span class="comment">// 那么结果就是原字符串本身.</span></span><br><span class="line">        <span class="keyword">if</span> (numRows == <span class="number">1</span> || s.Length &lt;= numRows) &#123;</span><br><span class="line">            <span class="keyword">return</span> s;</span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">        <span class="comment">// 2. 初始化“行”容器</span></span><br><span class="line">        <span class="comment">// 创建一个列表,用来存放每一行的字符.</span></span><br><span class="line">        <span class="comment">// 我们使用 List&lt;StringBuilder&gt; 而不是 List&lt;string&gt; 是为了高效拼接字符.</span></span><br><span class="line">        <span class="keyword">var</span> rows = <span class="keyword">new</span> List&lt;StringBuilder&gt;();</span><br><span class="line">        <span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">0</span>; i &lt; numRows; i++) &#123;</span><br><span class="line">            rows.Add(<span class="keyword">new</span> StringBuilder());</span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">        <span class="comment">// 3. 遍历与放置</span></span><br><span class="line">        <span class="built_in">int</span> currentRow = <span class="number">0</span>;</span><br><span class="line">        <span class="comment">// 用一个变量来表示方向,1代表向下,-1代表向上.</span></span><br><span class="line">        <span class="comment">// 初始设为-1,是因为我们希望在第一次循环时,</span></span><br><span class="line">        <span class="comment">// 当 currentRow == 0 时,方向能立刻变为 1（向下）.</span></span><br><span class="line">        <span class="built_in">int</span> direction = <span class="number">-1</span>; </span><br><span class="line"> </span><br><span class="line">        <span class="keyword">foreach</span> (<span class="built_in">char</span> c <span class="keyword">in</span> s) &#123;</span><br><span class="line">            <span class="comment">// 将当前字符放入对应的行</span></span><br><span class="line">            rows[currentRow].Append(c);</span><br><span class="line"> </span><br><span class="line">            <span class="comment">// 判断是否到达顶部或底部,如果是,则改变方向</span></span><br><span class="line">            <span class="keyword">if</span> (currentRow == <span class="number">0</span> || currentRow == numRows - <span class="number">1</span>) &#123;</span><br><span class="line">                direction *= <span class="number">-1</span>; <span class="comment">// 乘以-1可以方便地在 1 和 -1 之间切换</span></span><br><span class="line">            &#125;</span><br><span class="line"> </span><br><span class="line">            <span class="comment">// 根据方向更新下一行索引</span></span><br><span class="line">            currentRow += direction;</span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">        <span class="comment">// 4. 收尾：拼接所有行</span></span><br><span class="line">        <span class="keyword">var</span> result = <span class="keyword">new</span> StringBuilder();</span><br><span class="line">        <span class="keyword">foreach</span> (<span class="keyword">var</span> row <span class="keyword">in</span> rows) &#123;</span><br><span class="line">            result.Append(row);</span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">        <span class="keyword">return</span> result.ToString();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="代码部分讲解">代码部分讲解:</h5><ul><li><strong><code>StringBuilder</code> 类</strong><ul><li><strong>是什么</strong>：一个“可变的字符串”.</li><li><strong>为什么用它</strong>：在C#中,<code>string</code> 类型是“不可变”（immutable）的.这意味着每次你用 <code>+</code> 连接两个字符串（例如 <code>string myStr = &quot;a&quot; + &quot;b&quot;;</code>）,系统实际上是创建了一个全新的字符串对象来存放结果 “ab”,并丢弃了原来的 “a”.在循环中大量执行这种操作会产生很多临时的垃圾对象,影响性能,并可能导致GC（垃圾回收）卡顿.</li><li><code>StringBuilder</code> 就是为了解决这个问题而生的.它的 <code>Append()</code> 方法是在内部的字符缓冲区上进行修改,而不是每次都创建新对象.这在需要多次拼接字符串的场景下效率极高.</li><li><strong>方法</strong>:<ul><li><code>new StringBuilder()</code>: 创建一个实例.</li><li><code>.Append(value)</code>: 在末尾追加内容,可以是字符、字符串或其他 <code>StringBuilder</code>.</li><li><code>.ToString()</code>: 当所有拼接操作完成后,调用此方法可以得到一个最终的、不可变的 <code>string</code> 对象.</li></ul></li></ul></li></ul><h4 id="索引计算法">索引计算法</h4><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Solution</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">string</span> <span class="title">Convert</span>(<span class="params"><span class="built_in">string</span> s, <span class="built_in">int</span> numRows</span>)</span> &#123;</span><br><span class="line">        <span class="built_in">int</span> len = s.Length;</span><br><span class="line">        <span class="comment">// 特殊情况处理</span></span><br><span class="line">        <span class="keyword">if</span> (numRows == <span class="number">1</span> || numRows &gt;= len) <span class="keyword">return</span> s;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 1. 准备工作：最高效的内存策略</span></span><br><span class="line">        <span class="built_in">int</span> cycleLen = <span class="number">2</span> * (numRows - <span class="number">1</span>);</span><br><span class="line">        <span class="built_in">char</span>[] result = <span class="keyword">new</span> <span class="built_in">char</span>[len]; <span class="comment">// 一次性分配最终大小的数组</span></span><br><span class="line">        <span class="built_in">int</span> idx = <span class="number">0</span>;                   <span class="comment">// 结果数组的写入指针</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 按行填充：分三段进行</span></span><br><span class="line">      </span><br><span class="line">        <span class="comment">// 2.1 填充第一行 (row = 0)</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="built_in">int</span> j = <span class="number">0</span>; j &lt; len; j += cycleLen) &#123;</span><br><span class="line">            result[idx++] = s[j];</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2.2 填充中间行 (row = 1 to numRows - 2)</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">1</span>; i &lt; numRows - <span class="number">1</span>; i++) &#123;</span><br><span class="line">            <span class="comment">// 内层循环处理当前第 i 行的所有字符</span></span><br><span class="line">            <span class="keyword">for</span> (<span class="built_in">int</span> j = i; j &lt; len; j += cycleLen) &#123;</span><br><span class="line">                <span class="comment">// a) 添加“竖线”上的字符</span></span><br><span class="line">                result[idx++] = s[j];</span><br><span class="line">              </span><br><span class="line">                <span class="comment">// b) 计算并添加“斜线”上的字符</span></span><br><span class="line">                <span class="built_in">int</span> step = <span class="number">2</span> * (numRows - <span class="number">1</span> - i);</span><br><span class="line">                <span class="built_in">int</span> diagIndex = j + step;</span><br><span class="line">              </span><br><span class="line">                <span class="keyword">if</span> (diagIndex &lt; len) &#123;</span><br><span class="line">                    result[idx++] = s[diagIndex];</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2.3 填充最后一行 (row = numRows - 1)</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="built_in">int</span> j = numRows - <span class="number">1</span>; j &lt; len; j += cycleLen) &#123;</span><br><span class="line">            result[idx++] = s[j];</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 收尾</span></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">string</span>(result);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="代码结合讲解：">代码结合讲解：</h5><ol><li><p><strong>准备工作</strong></p><ul><li><code>char[] result = new char[len];</code>：这是性能的关键.我们预先分配了一块连续的内存,大小正好是结果所需.这避免了任何运行时的动态内存分配和数据复制.</li><li><code>int idx = 0;</code>：这是一个简单的整数,作为指向 <code>result</code> 数组下一个可写入位置的指针.它的操作 (<code>idx++</code>) 非常快.</li></ul></li><li><p><strong>填充过程</strong></p><ul><li><p><strong>第一行 (<code>i=0</code>)</strong>: 这一行的字符在原字符串中的索引是 <code>0</code>, <code>cycleLen</code>, <code>2*cycleLen</code>, …循环 <code>for (int j = 0; j &lt; len; j += cycleLen)</code> 精准地跳跃到这些位置.</p></li><li><p><strong>中间行 (<code>i=1</code> 到 <code>numRows-2</code>)</strong>: 这是最核心的部分.</p><ul><li>外层循环 <code>for (int i = ...)</code> 确定我们当前正在为哪一行收集字符.</li><li>内层循环 <code>for (int j = i; ...)</code> 开始为第 <code>i</code> 行收集.<ul><li><code>j = i</code>: 第 <code>i</code> 行的第一个字符,其在原字符串中的索引就是 <code>i</code>.</li><li><code>result[idx++] = s[j];</code>: 这是放置**“竖线”**上的字符.<code>j</code> 会以 <code>cycleLen</code> 为步长,跳到下一个周期的同一“竖线”位置.</li><li><strong>计算“斜线”字符位置</strong>:<ul><li><code>int step = 2 * (numRows - 1 - i);</code> 这个公式计算的是从当前“竖线”字符 <code>j</code> 到它右侧的“斜线”字符的索引距离.</li><li><strong>推导</strong>: 从 <code>j</code> (在第 <code>i</code> 行) 向下走到V字底部 (第 <code>numRows-1</code> 行),需要 <code>(numRows - 1) - i</code> 步.再从底部斜向上走到第 <code>i</code> 行,又需要 <code>(numRows - 1) - i</code> 步.所以总距离是 <code>2 * (numRows - 1 - i)</code>.</li><li><strong>验证 (<code>numRows=4, i=1</code>)</strong>: <code>step = 2 * (4 - 1 - 1) = 4</code>.当 <code>j=1</code> (<code>A</code>) 时,斜线字符在 <code>1+4=5</code>,即 <code>L(5)</code>.正确.</li><li><strong>验证 (<code>numRows=4, i=2</code>)</strong>: <code>step = 2 * (4 - 1 - 2) = 2</code>.当 <code>j=2</code> (<code>Y</code>) 时,斜线字符在 <code>2+2=4</code>,即 <code>A(4)</code>.正确.</li></ul></li><li><code>if (diagIndex &lt; len)</code>: 必须检查,防止计算出的索引超出字符串的实际长度.</li></ul></li></ul></li><li><p><strong>最后一行 (<code>i=numRows-1</code>)</strong>: 规律和第一行一样简单.它的字符索引是 <code>numRows-1</code>, <code>numRows-1 + cycleLen</code>, …</p></li></ul></li><li><p><strong>收尾</strong></p><ul><li><code>new string(result)</code>: 这是从字符数组生成字符串的最高效方法.</li></ul></li></ol><p><strong>总结：</strong><br>这个方法之所以快,是因为它做了以下几件正确的事：</p><ul><li><strong>用数学代替模拟</strong>：避免了逐字符判断方向的逻辑开销.</li><li><strong>一次性内存分配</strong>：用 <code>char[]</code> 避免了运行时的内存动态调整.</li><li><strong>顺序写入</strong>：用 <code>idx++</code> 指针保证了对内存的高效顺序访问.</li><li><strong>简洁的循环</strong>：每个循环的目标单一明确,易于编译器优化.</li></ul><hr><h3 id="知识点总结：">知识点总结：</h3><h4 id="1-算法思维：模拟-vs-数学建模">1. 算法思维：模拟 vs. 数学建模</h4><ul><li><p><strong>模拟法 (方法一的变体)</strong></p><ul><li><strong>核心思想</strong>：模仿问题描述中的物理过程或人为操作步骤.代码逻辑与现实世界中的行为高度一致.</li><li><strong>优点</strong>：非常直观,容易理解和实现,是解决很多问题的首选思路,尤其是在规律不明显时.</li><li><strong>缺点</strong>：可能会因为包含了大量中间步骤和状态判断而导致性能较低.</li><li><strong>本题体现</strong>：通过 <code>currentRow</code> 和 <code>direction</code> 变量,模拟一支笔在纸上“向下再向上”的移动轨迹.</li></ul></li><li><p><strong>数学建模/索引计算法 (方法二)</strong></p><ul><li><strong>核心思想</strong>：分析问题内在的数学规律、周期性或几何特性,推导出可以直接计算结果的公式.</li><li><strong>优点</strong>：性能极高,因为它省去了所有中间过程,直达结果.能体现出更深层次的分析能力.</li><li><strong>缺点</strong>：需要更强的抽象和数学分析能力,规律可能不易发现,代码的直观性可能较差.</li><li><strong>本题体现</strong>：通过发现 <code>cycleLen</code> 这个周期,推导出每个字符在原字符串中的索引公式.</li></ul></li></ul><p><strong>通用启示</strong>：面对一个问题,先尝试用“模拟法”构建一个可行的基础版本.然后,思考能否优化,寻找其中的重复模式和数学规律,看是否能升级为“数学建模法”以获得极致性能.</p><h4 id="2-C-性能基础：内存分配与数据结构选择">2. C# 性能基础：内存分配与数据结构选择</h4><ul><li><p><strong><code>string</code> vs. <code>StringBuilder</code></strong></p><ul><li><code>string</code> 是<strong>不可变 (immutable)</strong> 的.任何修改（如拼接 <code>+</code>）都会创建新的字符串对象,在循环中会产生大量垃圾,引发GC（垃圾回收）,影响性能.</li><li><code>StringBuilder</code> 是<strong>可变 (mutable)</strong> 的.它在内部维护一个字符缓冲区,<code>Append</code> 操作是在此缓冲区上进行,效率远高于字符串拼接.当你需要构建一个复杂的或由多部分组成的字符串时,它是不二之选.</li></ul></li><li><p><strong><code>List&lt;T&gt;</code> vs. <code>Array (T[])</code></strong></p><ul><li><code>List&lt;T&gt;</code> 是<strong>动态数组</strong>.它提供了方便的 <code>Add</code>, <code>Remove</code> 等方法,大小可以动态变化.但这种便利性是有代价的：当内部数组容量不足时,会发生<strong>扩容</strong>（分配一个更大的新数组,并将旧数据复制过去）,这会带来性能开销.</li><li><code>Array (T[])</code> 是<strong>静态数组</strong>.大小在创建时就固定了.它的优点是<strong>性能极致</strong>：内存一次性分配,访问速度最快（直接通过偏移量计算地址）.</li><li><strong>选择原则</strong>：当你在程序开始时就能<strong>精确知道</strong>需要存储多少元素时,优先使用<strong>数组 <code>T[]</code></strong> 以获得最佳性能.如果不确定元素数量,或需要频繁增删,则使用 <code>List&lt;T&gt;</code> 更方便.</li></ul></li></ul><p><strong>通用启示</strong>：性能优化的一个关键方向就是<strong>减少运行时的内存分配</strong>.预先分配、重复利用（对象池思想）是C#（尤其是在Unity这种对GC敏感的环境中）性能优化的常用手段.</p><hr><h3 id="练习题-2">练习题</h3><h4 id="选择题-2">选择题</h4><p><strong>1. 在一个需要对大量短字符串进行拼接的循环中,以下哪种方式是最高效的？</strong></p><p>A. <code>string result = &quot;&quot;; foreach(var s in strings) { result += s; }</code><br>B. <code>StringBuilder sb = new StringBuilder(); foreach(var s in strings) { sb.Append(s); } return sb.ToString();</code><br>C. <code>List&lt;string&gt; list = new List&lt;string&gt;(); foreach(var s in strings) { list.Add(s); } return string.Concat(list);</code><br>D. <code>string result = string.Empty; foreach(var s in strings) { result = string.Concat(result, s); }</code></p><p><strong>2. 你正在编写一个函数,需要读取一个文件的前1024个字节.你应该选择哪种数据结构来存储这些字节？</strong></p><p>A. <code>List&lt;byte&gt;</code>,因为可能不需要读取全部1024个字节.<br>B. <code>byte[] buffer = new byte[1024];</code><br>C. <code>StringBuilder</code>,因为字节可以看作字符.<br>D. <code>Queue&lt;byte&gt;</code>,因为可以按顺序处理.</p><hr><h4 id="简答题-2">简答题</h4><p><strong>题目：</strong><br>假设你需要编写一个函数 <code>string ReverseWords(string s)</code>,该函数接收一个句子（由空格分隔的单词组成）,然后反转句子中单词的顺序.<br>例如,输入 <code>&quot;the sky is blue&quot;</code>,应输出 <code>&quot;blue is sky the&quot;</code>.</p><p>请思考并回答：</p><ol><li>你会如何利用本节课学到的<strong>数据结构选择</strong>知识来解决这个问题？（提示：先拆分,再组合）</li><li>这个问题更适合用<strong>模拟法</strong>还是<strong>数学建模法</strong>来解决？为什么？</li></ol><hr><h4 id="参考答案-2">参考答案:</h4><h5 id="选择题答案">选择题答案:</h5><ol><li><p>答案：B<br>解析：</p><ul><li><p>A 和 D 选项都使用了字符串的直接拼接,每次循环都会创建新的字符串对象,性能最差.</p></li><li><p>C 选项虽然比 A/D 好,但它创建了一个字符串列表,增加了内存开销,string.Concat 内部虽然有优化,但仍不如 StringBuilder 直接在缓冲区操作来得高效.</p></li><li><p>B 选项的 StringBuilder 正是为这种场景设计的,它最小化了内存分配和复制,是标准的高性能字符串构建方法.</p></li></ul></li><li><p>答案：B<br>解析：</p><ul><li>题目明确指出需要存储“1024个字节”,这是一个<strong>固定且已知的大小</strong>.</li><li>B 选项直接创建了一个大小精确的字节数组 byte[],这是最高效、最直接的方式.</li><li>A 选项 List<byte> 适用于大小不确定的情况,这里属于过度设计,且有潜在的性能开销.</li><li>C 选项 StringBuilder 用于处理字符和字符串,不适用于原始字节数据.</li><li>D 选项 Queue<byte> 是一种特定的数据结构（先进先出）,虽然可以工作,但不如数组直接和高效.</li></ul></li></ol><h5 id="简答题参考答案">简答题参考答案:</h5><ol><li><p><strong>数据结构选择：</strong></p><ul><li><strong>拆分阶段：</strong> 首先，我们需要将输入的字符串 <code>s</code> 按空格拆分成单词。C# 的 <code>s.Split(' ', StringSplitOptions.RemoveEmptyEntries)</code> 方法会返回一个字符串数组 <code>string[]</code>。这是一个很好的选择，因为一旦拆分，单词的数量就确定了。</li><li><strong>反转阶段：</strong> 我们可以直接对这个 <code>string[]</code> 数组进行反转。可以写一个循环从两端向中间交换元素，或者使用 <code>Array.Reverse()</code> 这个内置方法。</li><li><strong>组合阶段：</strong> 最后，我们需要将反转后的单词数组重新组合成一个字符串。这里就应该使用 <code>StringBuilder</code>。我们遍历反转后的数组，用 <code>sb.Append(word)</code> 和 <code>sb.Append(&quot; &quot;)</code> 来高效地构建最终结果。最后调用 <code>sb.ToString()</code> 并处理掉末尾多余的空格。或者，更简洁地使用 <code>string.Join(&quot; &quot;, reversedWordsArray)</code>，这个方法内部实现已经为性能做了优化。</li></ul></li><li><p><strong>算法思维选择：</strong></p><ul><li>这个问题更适合用<strong>模拟法</strong>。</li><li><strong>原因：</strong> “反转单词顺序”这个任务本身就是一个清晰、具体的操作步骤序列：1. 拆分句子成单词。 2. 将单词的序列反转。 3. 将新序列的单词组合成新句子。这个逻辑流程非常直观，没有复杂的、可供推导的数学/索引公式。我们直接模拟这个过程就是最高效、最清晰的解法。试图为其进行“数学建模”反而会使问题复杂化，没有必要。</li></ul></li></ol><hr><h3 id="可能的实际应用-2">可能的实际应用:</h3><ul><li><strong>图像处理/渲染</strong>：在游戏开发或图像处理中,对像素数据进行特定的模式重排（如转置、马赛克、滤镜效果）,其底层逻辑就和“索引计算法”非常相似.我们需要计算出目标像素应该从源图像的哪个坐标拾取颜色.</li><li><strong>数据序列化/通信协议</strong>：在网络通信中,数据经常需要按照特定格式（协议）进行打包和解包.Z字形变换可以看作一种简单的“数据混淆”或“交错”模式.理解这种模式变换有助于设计或解析自定义的数据流格式.</li><li><strong>文本编辑器/格式化工具</strong>：开发文本格式化工具时,比如将线性文本格式化为多栏布局,就需要计算每个字符或单词应该被放置在哪一栏的哪一行,这与Z字形变换的行分配逻辑异曲同工.</li><li><strong>性能关键代码的重构</strong>：当我们接手一个项目,发现某段代码（比如日志拼接、数据报表生成）因为大量的字符串操作而成为性能瓶颈时,就可以运用今天学到的知识,用 <code>StringBuilder</code> 或预分配数组的方式对其进行重构优化.</li></ul>]]></content>
      
      
      <categories>
          
          <category> 力扣刷题 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 算法 </tag>
            
            <tag> 力扣 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>力扣刷题日常(3-4)</title>
      <link href="/posts/82e0ee2a/"/>
      <url>/posts/82e0ee2a/</url>
      
        <content type="html"><![CDATA[<h1>力扣刷题日常(3-4)</h1><h2 id="第三题-无重复字符的最长子串-难度-中等">第三题: 无重复字符的最长子串(难度: 中等)</h2><h3 id="原题">原题:</h3><p>给定一个字符串 <code>s</code> ,请你找出其中不含有重复字符的 <strong>最长 子串</strong> 的长度.</p><p><strong>示例 1:</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入: s = &quot;abcabcbb&quot;</span><br><span class="line">输出: 3 </span><br><span class="line">解释: 因为无重复字符的最长子串是 &quot;abc&quot;,所以其长度为 3.</span><br></pre></td></tr></table></figure><p><strong>示例 2:</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入: s = &quot;bbbbb&quot;</span><br><span class="line">输出: 1</span><br><span class="line">解释: 因为无重复字符的最长子串是 &quot;b&quot;,所以其长度为 1.</span><br></pre></td></tr></table></figure><p><strong>示例 3:</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">输入: s = &quot;pwwkew&quot;</span><br><span class="line">输出: 3</span><br><span class="line">解释: 因为无重复字符的最长子串是 &quot;wke&quot;,所以其长度为 3.</span><br><span class="line">     请注意,你的答案必须是 子串 的长度,&quot;pwke&quot; 是一个子序列,不是子串.</span><br></pre></td></tr></table></figure><p><strong>提示:</strong></p><ul><li><code>0 &lt;= s.length &lt;= 5 * 104</code></li><li><code>s</code> 由英文字母、数字、符号和空格组成</li></ul><hr><h3 id="开始解题">开始解题:</h3><h4 id="方法一-暴力求解">方法一: 暴力求解</h4><p>那么看到这个题目的那一刻,我脑袋里的第一反应是<strong>暴力求法</strong>—把所有可能的子串都找出来,然后一个个检查它们是否含有重复字符,最后记录下最长的那个无重复子串的长度.</p><p>比如对于 <code>s = &quot;abcabcbb&quot;</code>:</p><ul><li>“a” (无重复, 长度1)</li><li>“ab” (无重复, 长度 2)</li><li>“abc” (无重复, 长度 3)</li><li>“abca” (有重复 ‘a’, 无效)</li><li>…</li><li>“b” (从第二个字符开始,无重复, 长度 1)</li><li>“bc” (无重复, 长度 2)</li><li>“bca” (无重复, 长度 3)</li><li>“bcab” (有重复 ‘b’, 无效)</li><li>…</li></ul><p>这个方法固然是可行的,但效率异常低下,如果字符串很长,那子串数量会<strong>爆发式增长</strong>,我们的计算量会变得格外大.</p><h4 id="方法二-滑动窗口">方法二: 滑动窗口</h4><p>既然我们的暴力求解太慢,那我们来想象一个&quot;<strong>窗口</strong>&quot;,这个窗口在字符串上<strong>从左到右滑动</strong>.我们始终保持在这个窗口内的所有字符都是不重复的.</p><p>那这个&quot;窗口&quot;就是我们的&quot;子串&quot;,我们现在要做的就是:</p><ol><li>不断尝试扩大这个窗口的<strong>右边界</strong>,把新的字符包含进来.</li><li>如果新进来的字符在窗口内<strong>没有重复</strong>,那就代表我们的无重复字串又变长了一点.我们记录下现在的长度,和<strong>之前记录的最大长度</strong>进行对比,看哪个更大.</li><li>如果新进来的字符在窗口内<strong>已经存在</strong>了,说明窗口内的字串不满足&quot;无重复&quot;的条件了,那我们就必须<strong>收缩窗口的左边界</strong>,知道把那个重复的旧字符&quot;<strong>挤</strong>&quot;出窗口为止.</li></ol><p>这样,我们的窗口就在字符串上&quot;<strong>滑动</strong>&quot;了起来: 右边界一直向右移动,<strong>左边界在必要时也向右移动</strong>,这个过程中,窗口的最大尺寸就是我们要求的答案.</p><p>这个就是&quot;<strong>滑动窗口</strong>&quot;算法思想</p><p><strong>思路讲完了,接下来讲的就是实现了:</strong></p><p>我们需要几个工具</p><ol><li><strong>两个指针</strong>: 一个 <code>left</code> 指针标记窗口的左边界, 一个 <code>right</code> 指针标记窗口的右边界.</li><li>一个&quot;<strong>账本</strong>&quot;: 我们需要一个能快速查询&quot;<strong>某个字符是否已在当前窗口内</strong>&quot;以及&quot;<strong>它在哪个位置</strong>&quot;的工具.在C#中, <code>Dictionary&lt;char,.int&gt;</code>(字典,也叫哈希表)是完美的选择.<ul><li>Key: 存储字符 <code>char</code></li><li>Value: 存储该字符<strong>最近一次出现时的索引</strong> <code>int</code></li></ul></li></ol><p><strong>逻辑步骤拆解(以 <code>s = &quot;pwwkew&quot;</code>为例):</strong></p><ul><li><p><strong>初始化</strong>:</p><ul><li><code>left = 0</code> (窗口左边界)</li><li><code>maxLength = 0</code> (记录最大长度)</li><li><code>charIndexMap = new Dictionary&lt;char, int&gt;()</code> (我们的“账本”)</li></ul></li><li><p><strong>开始滑动</strong> (用 <code>right</code> 指针从 0 遍历到字符串末尾):</p></li></ul><ol><li><p><strong><code>right = 0</code></strong>, 字符是 <code>'p'</code></p><ul><li>账本里没有 <code>'p'</code>.</li><li>把它加入账本: <code>charIndexMap['p'] = 0</code>.</li><li>当前窗口是 “p”,长度为 <code>0 - 0 + 1 = 1</code>.</li><li>更新 <code>maxLength = 1</code>.</li></ul></li><li><p><strong><code>right = 1</code></strong>, 字符是 <code>'w'</code></p><ul><li>账本里没有 <code>'w'</code>.</li><li>加入账本: <code>charIndexMap['w'] = 1</code>.</li><li>当前窗口是 “pw”,长度为 <code>1 - 0 + 1 = 2</code>.</li><li>更新 <code>maxLength = 2</code>.</li></ul></li><li><p><strong><code>right = 2</code></strong>, 字符是 <code>'w'</code></p><ul><li><strong>冲突！</strong> 账本里已经有 <code>'w'</code> 了,它上次出现的位置是 <code>1</code>.</li><li>这意味着我们的窗口 <code>[left, right]</code> (即从索引0到2,“pww”) 包含了重复字符.</li><li>我们必须移动左边界 <code>left</code>.移动到哪里呢？应该移动到重复字符 <code>'w'</code> 上次出现位置的<strong>下一个位置</strong>.</li><li>所以,<code>left</code> 更新为 <code>1 + 1 = 2</code>.</li><li>现在窗口的起始点是索引 2.</li><li>更新账本里 <code>'w'</code> 的位置: <code>charIndexMap['w'] = 2</code>.</li><li>当前窗口是 “w” (从索引2到2),长度为 <code>2 - 2 + 1 = 1</code>.<code>maxLength</code> 仍然是 <code>2</code>.</li></ul></li><li><p><strong><code>right = 3</code></strong>, 字符是 <code>'k'</code></p><ul><li>账本里没有 <code>'k'</code> (注意:此时我们的有效窗口是从 <code>left=2</code> 开始的,所以之前的’p’和第一个’w’已经不算在内了).</li><li>加入账本: <code>charIndexMap['k'] = 3</code>.</li><li>当前窗口是 “wk”,长度为 <code>3 - 2 + 1 = 2</code>.<code>maxLength</code> 仍然是 <code>2</code>.</li></ul></li><li><p><strong><code>right = 4</code></strong>, 字符是 <code>'e'</code></p><ul><li>账本里没有 <code>'e'</code>.</li><li>加入账本: <code>charIndexMap['e'] = 4</code>.</li><li>当前窗口是 “wke”,长度为 <code>4 - 2 + 1 = 3</code>.</li><li>更新 <code>maxLength = 3</code>.</li></ul></li><li><p><strong><code>right = 5</code></strong>, 字符是 <code>'w'</code></p><ul><li><strong>冲突！</strong> 账本里有 <code>'w'</code>,它上次出现的位置是 <code>2</code>.</li><li>当前 <code>left</code> 是 <code>2</code>.重复字符的位置 <code>2</code> 没有比 <code>left</code> 小,所以这个重复是在我们当前窗口内的.</li><li>移动左边界 <code>left</code> 到重复字符 <code>'w'</code> 上次位置的下一个位置.</li><li><code>left</code> 更新为 <code>2 + 1 = 3</code>.</li><li>更新账本里 <code>'w'</code> 的位置: <code>charIndexMap['w'] = 5</code>.</li><li>当前窗口是 “kew” (从索引3到5),长度为 <code>5 - 3 + 1 = 3</code>.<code>maxLength</code> 仍然是 <code>3</code>.</li></ul></li></ol><ul><li><strong>循环结束</strong>.返回 <code>maxLength</code>,最终结果是 <code>3</code>.</li></ul><p>那么这个算法的核心优势在于,我们<strong>只遍历了字符串一次</strong>,所以它的<strong>效率非常高</strong>.</p><hr><h3 id="比喻解释">比喻解释:</h3><p>上面的滑动窗口可能还是有些难以理解,我们来做个<strong>比喻</strong>吧</p><p>想象我们有一个传送带,上面按顺序放着字符串 <code>s = &quot;abcabcbb&quot;</code> 的每个字符.我们有一个篮子（就是我们的“窗口”）,我们的任务是用这个篮子从传送带上尽可能多地连续装走字符,但篮子里<strong>绝对不能</strong>出现两个一样的字符.</p><ol><li><p><strong>开始阶段</strong>:</p><ul><li>传送带过来 <code>'a'</code>,我们把它放进篮子.篮子:<code>{'a'}</code>.长度1.</li><li>传送带过来 <code>'b'</code>,篮子里没有,我们放进去.篮子:<code>{'a', 'b'}</code>.长度2.</li><li>传送带过来 <code>'c'</code>,篮子里没有,我们放进去.篮子:<code>{'a', 'b', 'c'}</code>.长度3.这是目前为止我们装到的最长记录.</li></ul></li><li><p><strong>冲突发生</strong>:</p><ul><li>现在,传送带过来了下一个字符:<code>'a'</code>.</li><li>我们检查篮子,发现里面<strong>已经有一个 <code>'a'</code> 了</strong>！</li><li>根据规则（篮子里不能有重复）,我们<strong>不能</strong>把这个新的 <code>'a'</code> 直接放进去.我们当前的篮子 <code>{'a', 'b', 'c'}</code> 已经满了（就这个新’a’而言）.</li></ul></li><li><p><strong>如何解决冲突？</strong>:</p><ul><li>我们的目标是继续从传送带上拿东西,并且要包含这个新的 <code>'a'</code>.</li><li>为了能把新的 <code>'a'</code> 放进来,我们必须先把篮子里那个<strong>旧的 <code>'a'</code> 扔掉</strong>.</li><li>但是,我们不能只扔掉旧的 <code>'a'</code>,因为我们必须保持篮子里的东西是<strong>连续</strong>从传送带上拿的.旧的 <code>'a'</code> 是我们拿的第一个东西.为了扔掉它,我们必须把我们从它开始拿的所有东西都按顺序扔掉,直到它被扔出去为止.</li><li>所以,我们扔掉旧 <code>'a'</code>.为了保持连续性,我们其实是把我们的“起始点”（也就是 <code>left</code> 指针）移动到了旧 <code>'a'</code> 的下一个位置.</li><li>操作前:篮子对应子串 <code>&quot;abc&quot;</code> (起始点 <code>left=0</code>)</li><li>操作后:我们把旧 <code>'a'</code> 扔了,篮子的起始点变成了 <code>'b'</code> (起始点 <code>left=1</code>).现在篮子里是 <code>{'b', 'c'}</code>.</li><li>现在篮子里没有 <code>'a'</code> 了,我们可以把传送带上那个新的 <code>'a'</code> 放进来了.</li><li>最终,篮子变成了 <code>{'b', 'c', 'a'}</code>.它对应子串 <code>&quot;bca&quot;</code>.这是一个合法的、无重复的子串.</li></ul></li></ol><p><strong>简单一个总结:</strong></p><p>收缩左边界（<code>left</code> 指针右移）的目的,就是<strong>为了修复窗口的合法性</strong>.</p><p>当右指针 <code>right</code> 引入一个重复字符时,窗口 <code>[left, right]</code> 就变得不合法了.为了让它重新合法,我们必须从左边移除字符,直到把那个造成重复的“元凶”（旧的那个重复字符）移除出去为止.这样,窗口就又变回了一个不含重复字符的子串,我们就可以继续尝试向右扩大窗口,寻找更长的可能性了.</p><p>如果不收缩左边界,那么从当前这个 <code>right</code> 指针开始,无论它再往右走多远,形成的子串 <code>[left, right]</code>, <code>[left, right+1]</code>, … 都将永远包含那个重复,永远不合法,也就失去了继续计算的意义.</p><hr><h3 id="解法代码">解法代码:</h3><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Solution</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">int</span> <span class="title">LengthOfLongestSubstring</span>(<span class="params"><span class="built_in">string</span> s</span>)</span> &#123;</span><br><span class="line">        <span class="comment">// 处理边界情况</span></span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">string</span>.IsNullOrEmpty(s)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">        <span class="comment">// 初始化变量</span></span><br><span class="line">        <span class="comment">// key: 字符, value: 该字符最近一次出现的索引</span></span><br><span class="line">        <span class="keyword">var</span> charIndexMap = <span class="keyword">new</span> Dictionary&lt;<span class="built_in">char</span>, <span class="built_in">int</span>&gt;(); </span><br><span class="line">        <span class="built_in">int</span> maxLength = <span class="number">0</span>;</span><br><span class="line">        <span class="built_in">int</span> left = <span class="number">0</span>; <span class="comment">// 滑动窗口的左边界</span></span><br><span class="line"> </span><br><span class="line">        <span class="comment">// 开始滑动窗口</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="built_in">int</span> right = <span class="number">0</span>; right &lt; s.Length; right++) &#123;</span><br><span class="line">            <span class="built_in">char</span> currentChar = s[right];</span><br><span class="line"> </span><br><span class="line">            <span class="comment">// 检查是否出现重复,并且重复的字符在当前窗口内</span></span><br><span class="line">            <span class="keyword">if</span> (charIndexMap.ContainsKey(currentChar)) &#123;</span><br><span class="line">                <span class="comment">// 更新左边界 left</span></span><br><span class="line">                <span class="comment">// Math.Max确保 left 指针不会向左回退</span></span><br><span class="line">                left = Math.Max(left, charIndexMap[currentChar] + <span class="number">1</span>);</span><br><span class="line">            &#125;</span><br><span class="line"> </span><br><span class="line">            <span class="comment">// 更新（或添加）当前字符的最新位置</span></span><br><span class="line">            charIndexMap[currentChar] = right;</span><br><span class="line"> </span><br><span class="line">            <span class="comment">// 计算当前窗口的长度,并尝试更新最大长度</span></span><br><span class="line">            maxLength = Math.Max(maxLength, right - left + <span class="number">1</span>);</span><br><span class="line">        &#125;</span><br><span class="line"> </span><br><span class="line">        <span class="keyword">return</span> maxLength;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="知识点解析">知识点解析:</h4><ol><li><p><strong><code>string.IsNullOrEmpty(s)</code></strong></p><ul><li><strong>讲解</strong>: 这是一个非常实用的静态方法,用于检查一个字符串是否为 <code>null</code> 或者是否为空字符串 <code>&quot;&quot;</code>.在编写健壮的代码时,这是一个很好的习惯,可以避免因为传入了 <code>null</code> 而导致程序在后续操作（如 <code>s.Length</code>）中抛出 <code>NullReferenceException</code>.</li></ul></li><li><p><strong><code>charIndexMap[currentChar] = right;</code></strong></p></li></ol><ul><li><strong>讲解:</strong> 这是 <code>Dictionary</code> 的索引器用法,非常灵活.<ul><li><strong>如果键 <code>currentChar</code> 已经存在</strong>,这行代码会<strong>更新</strong>该键对应的值为新的 <code>right</code>.</li><li><strong>如果键 <code>currentChar</code> 不存在</strong>,这行代码会<strong>添加</strong>一个新的键值对到字典中.</li></ul></li><li>在我们的代码里,它完美地实现了“更新字符最新位置”的需求.</li></ul><ol start="3"><li><strong>滑动窗口思想 (The Sliding Window Pattern)</strong></li></ol><ul><li><strong>核心</strong>: 这是一种用于解决数组/字符串子区间问题的强大算法模型.它使用两个指针（<code>left</code>, <code>right</code>）来定义一个“窗口”,通过移动这两个指针来“滑动”并处理窗口内的数据.</li><li><strong>本质</strong>: 它将一些问题的嵌套循环（暴力解法,如O(n²)）优化为单次遍历（O(n)）,通过巧妙地维护窗口状态,避免了大量重复计算.</li></ul><ol start="4"><li><strong>哈希表/字典的妙用 (The Power of Hash Maps / Dictionaries)</strong></li></ol><ul><li><strong>核心</strong>: 当我们需要快速地“检查一个元素是否存在”或者“查找一个元素的关联信息”时,<code>Dictionary</code> (哈希表) 应该是我们的首选工具.</li><li><strong>优势</strong>: 它提供了平均O(1)时间复杂度的插入、删除和查找操作,性能极高.在算法题中,它是优化时间复杂度的关键先生.</li></ul><ol start="5"><li><strong>空间换时间 (Space-for-Time Tradeoff)</strong></li></ol><ul><li><strong>核心</strong>: 这是一个经典的计算机科学概念.在我们的解法中,我们额外使用了一个<code>Dictionary</code>来存储信息（占用了空间）,但正是这个额外的空间,让我们算法的执行时间从O(n²)或更高降低到了O(n).在很多场景下,用可接受的内存增长来换取巨大的性能提升是非常值得的.</li></ul><ol start="6"><li><strong>编写健壮的代码 (Writing Robust Code)</strong></li></ol><ul><li><strong>核心</strong>: 在函数或方法的开头,优先处理“边缘情况”（Edge Cases）,比如输入为<code>null</code>、空字符串、空集合等.这能让我们的代码更加稳定,避免在后续逻辑中出现意外的运行时错误（如<code>NullReferenceException</code>）.</li></ul><h4 id="额外练习题">额外练习题:</h4><h5 id="选择题"><strong>选择题</strong></h5><ol><li><p>在解决“求满足某条件的<strong>连续</strong>子数组/子串”问题时,以下哪种算法思想通常是最高效的选择？<br>A. 递归 (Recursion)<br>B. 暴力枚举 (Brute Force)<br>C. 滑动窗口 (Sliding Window)<br>D. 排序 (Sorting)</p></li><li><p>在“无重复字符的最长子串”算法中,使用 <code>Dictionary&lt;char, int&gt;</code> 的主要目的是什么？<br>A. 按字母顺序存储字符.<br>B. 统计每个字符出现的总次数.<br>C. 快速判断一个字符是否在当前窗口内,并获取其位置.<br>D. 减少代码行数.</p></li><li><p>我们实现的滑动窗口解法的时间复杂度是多少？（n是字符串的长度）<br>A. O(1) - 常数时间<br>B. O(log n) - 对数时间<br>C. O(n) - 线性时间<br>D. O(n²) - 平方时间</p></li></ol><h5 id="简答题"><strong>简答题</strong></h5><ol><li><p>除了“无重复字符的最长子串”,请你设想一个在游戏开发中可能用得上“滑动窗口”思想的场景.</p></li><li><p>在我们的代码中,更新左边界的代码是 <code>left = Math.Max(left, charIndexMap[currentChar] + 1);</code>.为什么不直接写成 <code>left = charIndexMap[currentChar] + 1;</code>？请用字符串 <code>&quot;abba&quot;</code> 作为例子来解释.</p></li></ol><hr><h5 id="参考答案"><strong>参考答案</strong></h5><p><strong>选择题答案:</strong></p><ol><li><strong>C</strong>. 滑动窗口是专门用来高效处理连续子数组/子串问题的.</li><li><strong>C</strong>. <code>Dictionary</code> 的O(1)查找特性是算法高效的关键,它让我们能瞬间知道新字符是否与窗口内的字符重复.</li><li><strong>C</strong>. <code>right</code> 指针和 <code>left</code> 指针都只从左到右遍历字符串一次,所以总的时间复杂度是线性的 O(n).</li></ol><p><strong>简答题答案:</strong></p><ol><li><p><strong>场景示例</strong>:</p><ul><li><strong>分析玩家操作</strong>: 统计玩家在任意连续的5秒内,点击鼠标或敲击键盘的最高频率（APM - Actions Per Minute）,用于判断玩家是否处于“高能”操作状态.</li><li><strong>伤害统计</strong>: 计算一个持续性伤害技能（如毒雾、火焰地板）在任意3秒内对敌人造成的最大总伤害.</li><li><strong>资源采集监控</strong>: 在一个资源点,判断最近采集的10个资源中,稀有资源的占比是否超过了某个阈值.</li></ul></li><li><p><strong>原因解释</strong>:<br>使用 <code>Math.Max</code> 是为了<strong>防止 <code>left</code> 指针向左回退</strong>.<code>left</code> 应该只增不减,代表窗口的起始点只能向前移动.</p><p>以 <code>&quot;abba&quot;</code> 为例:</p><ul><li><code>right=0</code>, <code>char='a'</code>, <code>left=0</code>, <code>map={'a':0}</code></li><li><code>right=1</code>, <code>char='b'</code>, <code>left=0</code>, <code>map={'a':0, 'b':1}</code></li><li><code>right=2</code>, <code>char='b'</code>, 发现重复.<code>map['b']</code>是1.<code>left</code> 更新为 <code>Math.Max(0, 1 + 1) = 2</code>.此时 <code>left=2</code>, <code>map={'a':0, 'b':2}</code>.窗口是<code>&quot;b&quot;</code>.</li><li><code>right=3</code>, <code>char='a'</code>, 发现重复.<code>map['a']</code>是0.<ul><li>如果用 <code>left = map['a'] + 1</code>,<code>left</code> 会变成 <code>0 + 1 = 1</code>.但我们当前的 <code>left</code> 已经是 <code>2</code> 了！指针向左回退了,这是错误的,因为窗口的起始点不应该回到一个已经被判定为无效的位置.</li><li>如果用 <code>left = Math.Max(left, map['a'] + 1)</code>,<code>left</code> 会变成 <code>Math.Max(2, 0 + 1) = 2</code>.<code>left</code> 保持在 <code>2</code>,逻辑正确.这说明重复的 <code>'a'</code> (在索引0) 早已在当前窗口 <code>[2, 3]</code> 的左边,对我们没有影响,无需移动左边界.</li></ul></li></ul></li></ol><hr><h4 id="可能的实际应用">可能的实际应用</h4><ul><li><p><strong>滑动窗口</strong>:</p><ul><li><strong>性能监控</strong>: 我们可以写一个简单的性能分析工具,它记录最近60帧的<code>Time.deltaTime</code>.用滑动窗口可以实时计算出过去1秒（60帧）内的平均帧率、最低帧率等,如果低于某个阈值就发出警告.</li><li><strong>AI行为分析</strong>: 分析NPC或敌人在过去一段时间内的行为序列,判断其是否在重复某个模式,从而做出反制.</li></ul></li><li><p><strong><code>Dictionary</code> (哈希表)</strong>:</p><ul><li><strong>对象池管理</strong>: 使用 <code>Dictionary&lt;string, Queue&lt;GameObject&gt;&gt;</code> 来管理不同种类的预制体对象池,通过预制体名称（string）快速找到对应的对象队列.</li><li><strong>资源缓存</strong>: 加载过的<code>AudioClip</code>, <code>Texture2D</code>等资源可以存入 <code>Dictionary&lt;string, Object&gt;</code>,键是资源路径,值是资源本身.再次使用时先从字典中查找,避免重复的磁盘IO.这在Unity中是优化加载性能的常用手段.</li><li><strong>状态机 (FSM)</strong>: 用 <code>Dictionary&lt;MyStateEnum, IState&gt;</code> 来存储状态枚举和对应的状态逻辑类,可以非常方便地切换和管理对象（玩家、敌人、UI）的状态.</li></ul></li></ul><hr><h2 id="第四题-寻找两个正序数组的中位数-难度-困难">第四题: 寻找两个正序数组的中位数(难度: 困难)</h2><h3 id="原题-2">原题:</h3><p>给定两个大小分别为 <code>m</code> 和 <code>n</code> 的正序（从小到大）数组 <code>nums1</code> 和 <code>nums2</code>.请你找出并返回这两个正序数组的 <strong>中位数</strong> .</p><p>算法的时间复杂度应该为 <code>O(log (m+n))</code> .</p><p><strong>示例 1:</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入:nums1 = [1,3], nums2 = [2]</span><br><span class="line">输出:2.00000</span><br><span class="line">解释:合并数组 = [1,2,3] ,中位数 2</span><br></pre></td></tr></table></figure><p><strong>示例 2:</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入:nums1 = [1,2], nums2 = [3,4]</span><br><span class="line">输出:2.50000</span><br><span class="line">解释:合并数组 = [1,2,3,4] ,中位数 (2 + 3) / 2 = 2.5</span><br></pre></td></tr></table></figure><p><strong>提示:</strong></p><ul><li><code>nums1.length == m</code></li><li><code>nums2.length == n</code></li><li><code>0 &lt;= m &lt;= 1000</code></li><li><code>0 &lt;= n &lt;= 1000</code></li><li><code>1 &lt;= m + n &lt;= 2000</code></li><li><code>-106 &lt;= nums1[i], nums2[i] &lt;= 106</code></li></ul><hr><h3 id="开始解题-2">开始解题</h3><p>这道题目要求了我们的时间复杂度应该是 <code>O(log(m+n))</code> , 看到 <code>log</code> 级别的复杂度,我们的第一反应通常是二分搜索</p><p>但是我们应该在哪里进行二分搜索了</p><p>一个比较直观但是不符合要求的思路是:</p><ol><li>创建一个新的大数组.</li><li>将 <code>nums1</code> 和 <code>nums2</code> 合并到这个大数组中并排序.</li><li>根据大数组的长度是奇数还是偶数,直接找到中位数.<br>这个方法的时间复杂度是 <code>O(m+n)</code>（合并过程）,空间复杂度也是 <code>O(m+n)</code>,不满足题目 <code>O(log(m+n))</code> 的时间复杂度要求.因此,我们必须寻找更巧妙的办法.</li></ol><h4 id="核心思想-寻找-分割线-而不是-中位数">核心思想: 寻找&quot;分割线&quot;而不是&quot;中位数&quot;</h4><p>这道题的精髓在于,我们不直接去寻找具体的中位数数值,而是去寻找一个&quot;分割线&quot;,这个分割线能把两个数组划分成&quot;左半部分&quot;和&quot;右半部分&quot;,并且同时满足以下两个条件:</p><ol><li>长度相等: 左半部分所有的元素个数 = 右半部分所有元素的个数(或当总数为奇数时,左半部分比右半部分多一个)</li><li>顺序正确: 左半部分的所有元素 &lt;= 右半部分的所有元素</li></ol><p>那么当我们找到了这个分割线之后1,中位数就由分割线两侧的元素决定了</p><h4 id="那么我们应该如何寻找这个-分割线-呢">那么我们应该如何寻找这个&quot;分割线&quot;呢</h4><p>我们用二分搜索来寻找这条分割线在其中一个数组(比如 <code>num1</code> )中的位置</p><ol><li><p><strong>定义分割线</strong>:<br>假设我们在 <code>nums1</code> 的索引 <code>i</code> 处切一刀,把 <code>nums1</code> 分成 <code>nums1[0...i-1]</code> (左半部分) 和 <code>nums1[i...m-1]</code> (右半部分).<br>为了满足上面提到的“长度相等”条件,<code>nums2</code> 的分割线位置 <code>j</code> 就被确定了.如果两个数组总长度为 <code>total = m + n</code>,我们希望左半部分总共有 <code>(total + 1) / 2</code> 个元素.这个公式很巧妙,无论 <code>total</code> 是奇数还是偶数都适用.<br>所以,<code>i + j = (m + n + 1) / 2</code>,从而 <code>j = (m + n + 1) / 2 - i</code>.</p></li><li><p><strong>二分搜索过程</strong>:<br>我们在较短的那个数组（假设是 <code>nums1</code>）上进行二分搜索,搜索范围是 <code>[0, m]</code>,目的是找到一个合适的 <code>i</code>.</p><ul><li>在每一次循环中,我们取一个中间位置 <code>i</code>.</li><li>根据 <code>i</code> 计算出 <code>j</code>.</li><li>现在我们有了四个关键的“边界值”:<ul><li><code>nums1</code> 左半部分的最大值: <code>maxLeft1 = nums1[i-1]</code></li><li><code>nums1</code> 右半部分的最小值: <code>minRight1 = nums1[i]</code></li><li><code>nums2</code> 左半部分的最大值: <code>maxLeft2 = nums2[j-1]</code></li><li><code>nums2</code> 右半部分的最小值: <code>minRight2 = nums2[j]</code></li></ul></li></ul></li><li><p><strong>判断分割是否正确</strong>:<br>要满足“顺序正确”的条件,必须保证 <code>左半部分的最大值 ≤ 右半部分的最小值</code>.具体来说,就是:<br><code>maxLeft1 ≤ minRight2</code> <strong>并且</strong> <code>maxLeft2 ≤ minRight1</code></p></li><li><p><strong>调整二分搜索范围</strong>:</p><ul><li>如果 <code>maxLeft1 &gt; minRight2</code>:说明 <code>nums1</code> 的分割点 <code>i</code> 太靠右了,导致 <code>nums1</code> 左边的元素太大.我们需要把 <code>i</code> 向左移动.在二分搜索中,这意味着收缩右边界 <code>high = i - 1</code>.</li><li>如果 <code>maxLeft2 &gt; minRight1</code>:说明 <code>nums1</code> 的分割点 <code>i</code> 太靠左了,导致 <code>nums2</code> 左边的元素太大（因为 <code>i</code> 小了,<code>j</code> 就会变大）.我们需要把 <code>i</code> 向右移动.在二分搜索中,这意味着收缩左边界 <code>low = i + 1</code>.</li><li>如果上述两个条件都满足,那我们就找到了完美的分割线 <code>i</code> 和 <code>j</code>.</li></ul></li></ol><h4 id="那当我们找到分割线之后-要如何计算中位数呢">那当我们找到分割线之后,要如何计算中位数呢</h4><ul><li><strong>如果总长度 <code>(m+n)</code> 是奇数</strong>:中位数就是左半部分所有元素里的那个最大值.即 <code>max(maxLeft1, maxLeft2)</code>.</li><li><strong>如果总长度 <code>(m+n)</code> 是偶数</strong>:中位数是左半部分最大值和右半部分最小值的平均数.即 <code>(max(maxLeft1, maxLeft2) + min(minRight1, minRight2)) / 2.0</code>.</li></ul><h4 id="举个栗子-nums1-1-3-nums2-2">举个栗子:<code>nums1 = [1,3]</code>, <code>nums2 = [2]</code></h4><ul><li><code>m=2</code>, <code>n=1</code>, <code>total=3</code>.左半部分应有 <code>(3+1)/2 = 2</code> 个元素.</li><li>我们在 <code>nums1</code> 上二分搜索 <code>i</code>.搜索范围 <code>[0, 2]</code>.</li><li><strong>尝试1</strong>: <code>low=0, high=2</code> -&gt; <code>i = 1</code>.<ul><li><code>j = (3+1)/2 - i = 2 - 1 = 1</code>.</li><li>分割如下:<ul><li><code>nums1</code> 左: <code>[1]</code> (<code>maxLeft1=1</code>) | <code>nums1</code> 右: <code>[3]</code> (<code>minRight1=3</code>)</li><li><code>nums2</code> 左: <code>[2]</code> (<code>maxLeft2=2</code>) | <code>nums2</code> 右: <code>[]</code> (空)</li></ul></li><li>检查条件:<code>maxLeft2 (2) &gt; minRight1 (3)</code> ？不成立.</li><li>检查条件:<code>maxLeft1 (1) &gt; minRight2</code> ？<code>minRight2</code> 不存在,我们先不考虑这个边界.</li><li>我们发现 <code>maxLeft2 &gt; maxLeft1</code>,但 <code>maxLeft2</code> 并没有大于 <code>minRight1</code>.</li><li>让我们重新检查条件:<code>maxLeft1(1) &lt;= minRight2(不存在)</code> 和 <code>maxLeft2(2) &lt;= minRight1(3)</code>.</li><li><code>2 &lt;= 3</code> 是成立的.另一个条件因为 <code>minRight2</code> 不存在,我们可以认为它也成立（在代码实现中会用 <code>int.MaxValue</code> 处理）.</li><li>所以,分割是正确的！</li></ul></li><li><strong>计算中位数</strong>:<ul><li>总长度是3（奇数）.</li><li>中位数是左半部分的最大值:<code>max(maxLeft1, maxLeft2) = max(1, 2) = 2</code>.</li><li>最终结果是 <code>2.0</code>.</li></ul></li></ul><h4 id="更形象的理解方式-切卡片游戏">更形象的理解方式:切卡片游戏</h4><p>想象一下,<code>nums1</code> 和 <code>nums2</code> 是两摞已经按数字从小到大排好序的卡片.</p><p><strong>我们的目标</strong>:找到一个“想象中的”合并后的大卡片堆的<strong>中位数</strong>.</p><p><strong>中位数的本质是什么？</strong> 它就是一条分割线,这条线恰好把所有卡片分成数量相等的“左半堆”和“右半堆”,并且左半堆里最大的那张卡片,一定小于等于右半堆里最小的那张卡片.</p><p><strong>直接合并的笨办法</strong>:把两摞卡片拿过来,一张一张比较,排成新的一大摞,然后从中间拿起那张卡片.这就是 <code>O(m+n)</code> 的方法,太慢了.</p><p><strong>聪明的办法（二分搜索）</strong>:我们不真的去合并,我们只在这两摞卡片上“比划”一下,找到那条完美的“分割线”应该切在哪里.</p><h4 id="游戏开始">游戏开始:</h4><ol><li><strong>确定目标</strong>:假设 <code>nums1</code> 有 <code>m</code> 张,<code>nums2</code> 有 <code>n</code> 张.总共 <code>m+n</code> 张. 我们的“左半堆”需要有 <code>k = (m+n+1)/2</code> 张卡片.（这个 <code>+1</code> 的技巧可以同时兼容总数是奇数和偶数的情况,非常方便）.</li><li><strong>核心问题</strong>:我们怎么凑够这 <code>k</code> 张卡片作为“左半堆”呢？ 我们可以从 <code>nums1</code> 里拿 <code>i</code> 张,那么就必须从 <code>nums2</code> 里拿 <code>j = k - i</code> 张. 看到了吗？只要我们确定了在 <code>nums1</code> 上切一刀的位置（<code>i</code>）,在 <code>nums2</code> 上的切分位置（<code>j</code>）就<strong>自动确定</strong>了！</li><li><strong>二分搜索的用武之地</strong>:我们的任务就是找到那个<strong>刚刚好的 <code>i</code></strong>. <code>i</code> 的取值范围是从 <code>0</code> (一张都不从<code>nums1</code>拿) 到 <code>m</code> (把<code>nums1</code>全拿了). 我们不用一个一个地去试 <code>i=0, i=1, i=2...</code>,那样太慢了.我们可以用“猜”的办法,也就是<strong>二分搜索</strong>.</li></ol><h4 id="猜-i-的过程">猜 <code>i</code> 的过程:</h4><p>假设 <code>nums1 = [1, 2]</code>, <code>nums2 = [3, 4]</code> <code>m=2, n=2</code>, 总数是4.左半堆需要 <code>k = (4+1)/2 = 2</code> 张卡片. 我们在 <code>nums1</code> 上猜一个切割位置 <code>i</code>（<code>i</code> 的范围是 <code>[0, 2]</code>）.</p><ul><li><strong>第一次猜</strong>:我们猜中间,<code>i = 1</code>.<ul><li>这意味着我们从 <code>nums1</code> 拿 <code>1</code> 张卡片 <code>[1]</code> 作为左半堆的一部分.</li><li>为了凑够2张,我们必须从 <code>nums2</code> 拿 <code>j = 2 - 1 = 1</code> 张卡片 <code>[3]</code>.</li><li>现在我们想象中的“左半堆”是 <code>{1, 3}</code>,“右半堆”是 <code>{2, 4}</code>.</li><li>检查一下这个分割是否合格？<ul><li>左半堆最大的卡片是 <code>3</code>.</li><li>右半堆最小的卡片是 <code>2</code>.</li><li>出问题了！<code>3 &gt; 2</code>！这不符合“左边都比右边小”的规则.</li></ul></li><li><strong>分析原因</strong>:左半堆的 <code>3</code> (来自<code>nums2</code>) 太大了.说明我们从 <code>nums2</code> 拿得太多了,<code>j</code> 太大了.因为 <code>j = k - i</code>,所以是我们的 <code>i</code> 猜得<strong>太小了</strong>.</li><li><strong>调整策略</strong>:既然 <code>i=1</code> 太小,那正确答案肯定在 <code>i</code> 的右边.我们接下来在 <code>[2, 2]</code> 的范围里猜.</li></ul></li><li><strong>第二次猜</strong>:范围 <code>[2, 2]</code> 里只能猜 <code>i = 2</code>.<ul><li>这意味着我们从 <code>nums1</code> 拿 <code>2</code> 张卡片 <code>[1, 2]</code>.</li><li>为了凑够2张,我们必须从 <code>nums2</code> 拿 <code>j = 2 - 2 = 0</code> 张卡片 <code>[]</code>.</li><li>现在我们想象中的“左半堆”是 <code>{1, 2}</code>,“右半堆”是 <code>{3, 4}</code>.</li><li>再次检查:<ul><li>左半堆最大的卡片是 <code>2</code>.</li><li>右半堆最小的卡片是 <code>3</code>.</li><li><code>2 &lt;= 3</code>,完美！</li></ul></li><li><strong>找到答案了！</strong> 这就是我们要的分割.</li></ul></li></ul><p><strong>总结一下</strong>: 二分搜索不是在数组的值 <code>[1,2,3,4]</code> 上搜索,而是在<strong>分割点的位置 <code>i</code></strong> (<code>[0, m]</code>) 上进行搜索. 每一次猜测一个 <code>i</code>,我们就得到了一个“候选分割方案”.然后我们通过检查 <code>nums1[i-1]</code>、<code>nums1[i]</code>、<code>nums2[j-1]</code>、<code>nums2[j]</code> 这四个边界值,来判断我们的 <code>i</code> 是猜大了、猜小了,还是刚刚好.从而不断缩小 <code>i</code> 的搜索范围,直到找到最终答案.</p><hr><h3 id="解法代码-2">解法代码:</h3><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Solution</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">double</span> <span class="title">FindMedianSortedArrays</span>(<span class="params"><span class="built_in">int</span>[] nums1, <span class="built_in">int</span>[] nums2</span>)</span> &#123;</span><br><span class="line">        <span class="comment">// 1. 预处理:确保nums1是较短的数组</span></span><br><span class="line">        <span class="comment">// 这样做可以优化二分查找的范围,使其在更小的集合上进行,提高效率.</span></span><br><span class="line">        <span class="keyword">if</span> (nums1.Length &gt; nums2.Length) &#123;</span><br><span class="line">            <span class="keyword">return</span> FindMedianSortedArrays(nums2, nums1);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 初始化变量</span></span><br><span class="line">        <span class="built_in">int</span> m = nums1.Length;</span><br><span class="line">        <span class="built_in">int</span> n = nums2.Length;</span><br><span class="line">        <span class="comment">// low 和 high 是二分查找在短数组nums1上的搜索范围</span></span><br><span class="line">        <span class="built_in">int</span> low = <span class="number">0</span>;</span><br><span class="line">        <span class="built_in">int</span> high = m;</span><br><span class="line">        <span class="comment">// halfLen 是合并后数组左半部分的元素总数</span></span><br><span class="line">        <span class="built_in">int</span> halfLen = (m + n + <span class="number">1</span>) / <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 二分查找过程</span></span><br><span class="line">        <span class="keyword">while</span> (low &lt;= high) &#123;</span><br><span class="line">            <span class="comment">// i 是 nums1 分割线右侧第一个元素的索引,也是其左侧部分的元素个数</span></span><br><span class="line">            <span class="built_in">int</span> i = low + (high - low) / <span class="number">2</span>;</span><br><span class="line">            <span class="comment">// j 是 nums2 分割线右侧第一个元素的索引,由 i 推导得出</span></span><br><span class="line">            <span class="built_in">int</span> j = halfLen - i;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 4. 移动分割线 i</span></span><br><span class="line">            <span class="keyword">if</span> (i &lt; high &amp;&amp; nums2[j - <span class="number">1</span>] &gt; nums1[i]) &#123;</span><br><span class="line">                <span class="comment">// i 太小了,需要向右移动,增大 i</span></span><br><span class="line">                <span class="comment">// nums2左侧最大值 &gt; nums1右侧最小值,说明i的分割点需要右移</span></span><br><span class="line">                low = i + <span class="number">1</span>;</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> (i &gt; low &amp;&amp; nums1[i - <span class="number">1</span>] &gt; nums2[j]) &#123;</span><br><span class="line">                <span class="comment">// i 太大了,需要向左移动,减小 i</span></span><br><span class="line">                <span class="comment">// nums1左侧最大值 &gt; nums2右侧最小值,说明i的分割点需要左移</span></span><br><span class="line">                high = i - <span class="number">1</span>;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// 5. 找到了完美的分割线 i,计算中位数</span></span><br><span class="line">                </span><br><span class="line">                <span class="comment">// 确定左半部分的最大值</span></span><br><span class="line">                <span class="built_in">int</span> maxLeft;</span><br><span class="line">                <span class="keyword">if</span> (i == <span class="number">0</span>) &#123; <span class="comment">// nums1的左侧部分为空</span></span><br><span class="line">                    maxLeft = nums2[j - <span class="number">1</span>];</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (j == <span class="number">0</span>) &#123; <span class="comment">// nums2的左侧部分为空</span></span><br><span class="line">                    maxLeft = nums1[i - <span class="number">1</span>];</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    maxLeft = Math.Max(nums1[i - <span class="number">1</span>], nums2[j - <span class="number">1</span>]);</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 如果总元素个数是奇数,中位数就是左半部分的最大值</span></span><br><span class="line">                <span class="keyword">if</span> ((m + n) % <span class="number">2</span> == <span class="number">1</span>) &#123;</span><br><span class="line">                    <span class="keyword">return</span> maxLeft;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 如果总元素个数是偶数,还需要确定右半部分的最小值</span></span><br><span class="line">                <span class="built_in">int</span> minRight;</span><br><span class="line">                <span class="keyword">if</span> (i == m) &#123; <span class="comment">// nums1的右侧部分为空</span></span><br><span class="line">                    minRight = nums2[j];</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (j == n) &#123; <span class="comment">// nums2的右侧部分为空</span></span><br><span class="line">                    minRight = nums1[i];</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    minRight = Math.Min(nums1[i], nums2[j]);</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 中位数是左半部分最大值和右半部分最小值的平均数</span></span><br><span class="line">                <span class="keyword">return</span> (maxLeft + minRight) / <span class="number">2.0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h4 id="这里特别难理解-我再讲一下吧-从4-移动分割线-i-开始">这里特别难理解,我再讲一下吧(从4.移动分割线 i 开始)</h4><h5 id="场景回顾"><strong>场景回顾:</strong></h5><p>我们已经进入了 <code>while</code> 循环.在循环的每一次迭代中,我们都做了一件事:在 <code>nums1</code> 上“试探性地”切了一刀,位置是 <code>i</code>.然后根据 <code>i</code> 计算出了 <code>nums2</code> 上配套的切割位置 <code>j</code>.</p><p>现在,我们手上有四个关键的“边界”数字:</p><ul><li><code>nums1[i-1]</code>:<code>nums1</code> 左半部分的最大值 (L1_Max)</li><li><code>nums1[i]</code>:<code>nums1</code> 右半部分的最小值 (R1_Min)</li><li><code>nums2[j-1]</code>:<code>nums2</code> 左半部分的最大值 (L2_Max)</li><li><code>nums2[j]</code>:<code>nums2</code> 右半部分的最小值 (R2_Min)</li></ul><p><strong>我们的终极目标</strong>:找到一个 <code>i</code>,使得 <strong><code>L1_Max &lt;= R2_Min</code></strong> 并且 <strong><code>L2_Max &lt;= R1_Min</code></strong>. 这意味着我们找到了完美的分割,左边的都比右边的小.</p><p>现在,我们来逐行分析代码是如何实现这个目标的.</p><hr><h5 id="深入讲解-if-else-if-else-逻辑">深入讲解 <code>if-else if-else</code> 逻辑</h5><h6 id="1-if-i-high-nums2-j-1-nums1-i">1. <code>if (i &lt; high &amp;&amp; nums2[j - 1] &gt; nums1[i])</code></h6><ul><li><p><strong>代码翻译</strong>:这个条件检查的是 <code>L2_Max &gt; R1_Min</code>.也就是 <code>nums2</code> 左边的最大值,是否大于 <code>nums1</code> 右边的最小值.</p></li><li><p><strong>意味着什么？</strong> 如果这个条件成立,说明我们的分割出错了.我们把一个本应属于“右半堆”的小数字(<code>nums1[i]</code>)留在了右边,却把一个本应属于“左半堆”的大数字(<code>nums2[j-1]</code>)划入了左边.</p></li><li><p><strong>用栗子说话</strong>: <code>nums1 = [10, 20]</code>, <code>nums2 = [1, 2, 3]</code></p><ul><li><code>m=2, n=3</code>, <code>halfLen = (2+3+1)/2 = 3</code>.</li><li>假设我们二分查找到 <code>i=1</code>.那么 <code>j = 3 - 1 = 2</code>.</li><li>分割如下:<ul><li><code>nums1</code> 左: <code>[10]</code> (L1_Max=10) | <code>nums1</code> 右: <code>[20]</code> (R1_Min=20)</li><li><code>nums2</code> 左: <code>[1, 2]</code> (L2_Max=2) | <code>nums2</code> 右: <code>[3]</code> (R2_Min=3)</li></ul></li><li>此时 <code>L2_Max(2) &lt;= R1_Min(20)</code>,但 <code>L1_Max(10) &gt; R2_Min(3)</code>.我们的代码会进入下一个 <code>else if</code> 分支.</li></ul></li><li><p><strong>再看一个例子</strong>: <code>nums1 = [1, 5]</code>, <code>nums2 = [2, 3, 4, 6]</code></p><ul><li><code>m=2, n=4</code>, <code>halfLen = (2+4+1)/2 = 3</code>.</li><li>假设我们二分查找到 <code>i=1</code>.那么 <code>j = 3 - 1 = 2</code>.</li><li>分割如下:<ul><li><code>nums1</code> 左: <code>[1]</code> (L1_Max=1) | <code>nums1</code> 右: <code>[5]</code> (R1_Min=5)</li><li><code>nums2</code> 左: <code>[2, 3]</code> (L2_Max=3) | <code>nums2</code> 右: <code>[4, 6]</code> (R2_Min=4)</li></ul></li><li>此时 <code>L1_Max(1) &lt;= R2_Min(4)</code> 成立.但 <code>L2_Max(3) &lt;= R1_Min(5)</code> 也成立.这是完美分割！</li></ul></li><li><p><strong>回到这个 <code>if</code> 判断</strong>:<code>nums2[j - 1] &gt; nums1[i]</code></p><ul><li><strong>原因分析</strong>:这个不等式成立,说明 <code>i</code> 的值<strong>太小了</strong>.因为 <code>i</code> 小,导致 <code>j = halfLen - i</code> 的值偏大,使得我们从 <code>nums2</code> 中取了太多的元素进入左半部分,其中就包括了那个“太大”的 <code>nums2[j-1]</code>.</li><li><strong>如何修正</strong>:我们需要增大 <code>i</code>.在二分搜索中,这意味着正确答案在当前 <code>i</code> 的右侧.所以我们更新搜索区间的下界:<code>low = i + 1</code>.</li></ul></li></ul><h6 id="2-else-if-i-low-nums1-i-1-nums2-j">2. <code>else if (i &gt; low &amp;&amp; nums1[i - 1] &gt; nums2[j])</code></h6><ul><li><strong>代码翻译</strong>:这个条件检查的是 <code>L1_Max &gt; R2_Min</code>.也就是 <code>nums1</code> 左边的最大值,是否大于 <code>nums2</code> 右边的最小值.</li><li><strong>意味着什么？</strong> 这同样说明分割出错了.我们把一个“太大”的数字 <code>nums1[i-1]</code> 划入了左半堆.</li><li><strong>原因分析</strong>:这个不等式成立,说明 <code>i</code> 的值<strong>太大了</strong>.因为 <code>i</code> 大,我们从 <code>nums1</code> 中取了太多的元素进入左半部分,其中就包括了那个“太大”的 <code>nums1[i-1]</code>.</li><li><strong>如何修正</strong>:我们需要减小 <code>i</code>.在二分搜索中,这意味着正确答案在当前 <code>i</code> 的左侧.所以我们更新搜索区间的上界:<code>high = i - 1</code>.</li></ul><h6 id="3-else">3. <code>else</code></h6><ul><li><strong>代码翻译</strong>:如果上面两个 <code>if</code> 都不成立,这意味着什么？<ul><li><code>nums2[j - 1] &lt;= nums1[i]</code> <strong>并且</strong></li><li><code>nums1[i - 1] &lt;= nums2[j]</code></li></ul></li><li><strong>意味着什么？</strong> 这就是我们寻找到的<strong>完美分割</strong>,我们成功地把两个数组划分成了左右两半,并且左半部分的所有元素都小于等于右半部分的所有元素.</li><li><strong>接下来做什么？</strong> 计算中位数！</li></ul><hr><h5 id="深入讲解中位数计算-在-else-块内">深入讲解中位数计算 (在 <code>else</code> 块内)</h5><p>现在我们已经找到了完美的 <code>i</code> 和 <code>j</code>.</p><h6 id="1-计算-maxLeft-左半部分的最大值">1. 计算 <code>maxLeft</code> (左半部分的最大值)</h6><p>左半部分由 <code>nums1</code> 的前 <code>i</code> 个元素和 <code>nums2</code> 的前 <code>j</code> 个元素组成.它的最大值,必然是 <code>nums1[i-1]</code> 和 <code>nums2[j-1]</code> 中较大的那一个.</p><p>但是,这里有<strong>边界情况</strong>需要处理:</p><ul><li><code>if (i == 0)</code>: <code>nums1</code> 的左半部分是空的,那么整个左半部分的最大值就是 <code>nums2[j-1]</code>.</li><li><code>else if (j == 0)</code>: <code>nums2</code> 的左半部分是空的,那么整个左半部分的最大值就是 <code>nums1[i-1]</code>.</li><li><code>else</code>: 两边都不为空,<code>maxLeft</code> 就是 <code>Math.Max(nums1[i-1], nums2[j-1])</code>.</li></ul><h6 id="2-判断总长度是奇数还是偶数">2. 判断总长度是奇数还是偶数</h6><ul><li><code>if ((m + n) % 2 == 1)</code>: 如果总长度是奇数,中位数就是正中间那个数.在我们的分割方案里,左半部分比右半部分多一个元素,这个多出来的元素 <code>maxLeft</code> 就是中位数.所以直接 <code>return maxLeft</code>.</li></ul><h6 id="3-计算-minRight-右半部分的最小值-仅在总长度为偶数时需要">3. 计算 <code>minRight</code> (右半部分的最小值) - 仅在总长度为偶数时需要</h6><p>如果总长度是偶数,中位数是中间两个数的平均值.我们已经找到了左边最大的 <code>maxLeft</code>,现在需要找右边最小的 <code>minRight</code>.</p><p>右半部分由 <code>nums1</code> 从 <code>i</code> 开始的元素和 <code>nums2</code> 从 <code>j</code> 开始的元素组成.它的最小值,必然是 <code>nums1[i]</code> 和 <code>nums2[j]</code> 中较小的那一个.</p><p>同样,处理<strong>边界情况</strong>:</p><ul><li><code>if (i == m)</code>: <code>nums1</code> 的右半部分是空的！那么整个右半部分的最小值就是 <code>nums2[j]</code>.</li><li><code>else if (j == n)</code>: <code>nums2</code> 的右半部分是空的！那么整个右半部分的最小值就是 <code>nums1[i]</code>.</li><li><code>else</code>: 两边都不为空,<code>minRight</code> 就是 <code>Math.Min(nums1[i], nums2[j])</code>.</li></ul><h6 id="4-返回最终结果-总长度为偶数">4. 返回最终结果 (总长度为偶数)</h6><p>中位数是 <code>(maxLeft + minRight) / 2.0</code>.使用 <code>2.0</code> 来确保结果是 <code>double</code> 类型的浮点数,而不是整数除法.</p><hr><h4 id="知识点总结：">知识点总结：</h4><ol><li><strong>二分搜索的泛化应用</strong>： 二分搜索的核心是“<strong>在一个有序的集合中，通过不断排除一半的可能性来寻找目标</strong>”。这个“集合”不一定是一个存有具体数值的数组。它可以是<strong>答案的可能范围</strong>、<strong>索引的范围</strong>，或者任何具有单调性的抽象概念。在这道题中，我们搜索的就不是一个值，而是一个满足特定条件的“<strong>分割点索引</strong>”。</li><li><strong>问题转化的思想</strong>： 直接解决一个问题可能很困难，但我们可以尝试将其<strong>转化为一个等价且更容易解决的问题</strong>。这道题的精髓就是将“寻找中位数”这个目标，转化为了“<strong>寻找一个完美的分割线</strong>”。一旦找到了这条线，中位数就自然浮现了。</li><li><strong>边界条件的严谨处理</strong>： 算法的正确性很大程度上取决于对边界条件的正确处理。在这道题中，分割点 <code>i</code> 或 <code>j</code> 可能会取到 <code>0</code> 或者数组的长度 <code>m/n</code>，这意味着某一个“左半部分”或“右半部分”可能是空的。代码中通过 <code>if (i == 0)</code> 等判断来处理这些边界，是保证算法健壮性的关键。</li><li><strong>利用不变量和约束</strong>： 在我们的算法中，<code>i + j = halfLen</code> 是一个恒成立的<strong>约束条件</strong>（不变量）。这个约束极大地简化了问题，让我们只需要在一个变量 <code>i</code> 上进行搜索，另一个变量 <code>j</code> 就会被自动确定。在设计算法时，寻找并利用这样的不变量，可以有效降低问题的复杂度。</li></ol><hr><h4 id="练习题：">练习题：</h4><h5 id="选择题-2"><strong>选择题</strong></h5><p><strong>问题：</strong> 在解决 “寻找两个正序数组的中位数” 这个问题时，我们使用二分搜索的主要目的是什么？</p><p>(A) 在合并后的大数组中快速查找中位数的值。</p><p>(B) 在两个数组中分别查找值为中位数的元素。</p><p>© 在较短数组的所有可能“分割点”索引中，查找一个能满足特定条件的最佳分割点。</p><p>(D) 减少将两个数组合并所需的时间复杂度。</p><h5 id="简答题-2"><strong>简答题</strong></h5><p><strong>问题：</strong> 在该算法中，我们将“寻找中位数”问题转化为了“寻找完美分割线”的问题。请简述我们是如何判断一个分割线（由 <code>nums1</code> 的分割点 <code>i</code> 和 <code>nums2</code> 的分割点 <code>j</code> 定义）是否是“完美”的？</p><hr><h5 id="参考答案-2">参考答案</h5><p><strong>选择题答案是 ©。</strong></p><p><strong>解析：</strong></p><ul><li>(A) 是错误的，该算法的核心就是避免了合并数组。</li><li>(B) 是错误的，中位数的值可能并不存在于任何一个原始数组中（例如 <code>[1,2]</code> 和 <code>[3,4]</code> 的中位数是2.5）。</li><li>© 正确地描述了算法的核心思想。我们的二分搜索是在 <code>nums1</code> 的索引范围 <code>[0, m]</code> 上进行的，目标是找到那个完美的 <code>i</code>。</li><li>(D) 虽然结果上确实避免了 <code>O(m+n)</code> 的合并，但二分搜索的直接目的不是“减少合并时间”，而是作为一种“不合并”的解决方案，其搜索目标是分割点，而非优化合并本身。</li></ul><p><strong>简答题参考答案：</strong></p><p>一个分割线是“完美”的，需要同时满足两个交叉条件：</p><ol><li><code>nums1</code> 左半部分的最大值 (<code>nums1[i-1]</code>) 必须小于或等于 <code>nums2</code> 右半部分的最小值 (<code>nums2[j]</code>)。</li><li><code>nums2</code> 左半部分的最大值 (<code>nums2[j-1]</code>) 必须小于或等于 <code>nums1</code> 右半部分的最小值 (<code>nums1[i]</code>)。</li></ol><p>这两个条件确保了所有划分到“左半堆”的元素都小于或等于所有划分到“右半堆”的元素，这正是中位数分割线的本质定义。</p><hr><h4 id="可能的实际应用-2">可能的实际应用</h4><ol><li><strong>游戏开发 (性能优化)</strong>： 假设我们要在一个巨大的有序列表（如排行榜、伤害记录）中找到第 K 名的玩家或数据，而这个列表分布在多个服务器或文件中。使用类似的思想，我们可以在不合并所有数据的情况下，高效地定位到第 K 个元素。</li><li><strong>资源调度与负载均衡</strong>： 想象一下，我们有两组服务器，每组的服务器按负载从低到高排序。我们想重新分配任务，使得所有服务器的中位数负载最低。这个问题就可以借鉴本题的思路，通过二分查找寻找一个最优的分配方案，而不是暴力尝试所有可能性。</li><li><strong>数据分析与统计</strong>： 在处理海量数据流时，数据可能被分片存储在不同的地方。当需要计算全局的百分位数（中位数是50%分位数）时，这种分治和二分的思想可以让我们在不加载全部数据到内存的情况下，高效地估算出结果。</li></ol>]]></content>
      
      
      <categories>
          
          <category> 力扣刷题 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 算法 </tag>
            
            <tag> 力扣 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>力扣刷题日常(1-2)</title>
      <link href="/posts/7eb38127/"/>
      <url>/posts/7eb38127/</url>
      
        <content type="html"><![CDATA[<h1>力扣刷题日常(1-2)</h1><h2 id="第一题-两数之和-难度-简单">第一题: 两数之和(难度: 简单)</h2><h3 id="原题">原题:</h3><p>给定一个整数数组 <code>nums</code> 和一个整数目标值 <code>target</code>，请你在该数组中找出 <strong>和为目标值</strong> <em><code>target</code></em> 的那 <strong>两个</strong> 整数，并返回它们的数组下标。</p><p>你可以假设每种输入只会对应一个答案，并且你不能使用两次相同的元素。</p><p>你可以按任意顺序返回答案。</p><p><strong>示例 1：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入：nums = [2,7,11,15], target = 9</span><br><span class="line">输出：[0,1]</span><br><span class="line">解释：因为 nums[0] + nums[1] == 9 ，返回 [0, 1] 。</span><br></pre></td></tr></table></figure><p><strong>示例 2：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入：nums = [3,2,4], target = 6</span><br><span class="line">输出：[1,2]</span><br></pre></td></tr></table></figure><p><strong>示例 3：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入：nums = [3,3], target = 6</span><br><span class="line">输出：[0,1]</span><br></pre></td></tr></table></figure><p><strong>提示：</strong></p><ul><li><code>2 &lt;= nums.length &lt;= 104</code></li><li><code>-109 &lt;= nums[i] &lt;= 109</code></li><li><code>-109 &lt;= target &lt;= 109</code></li><li><strong>只会存在一个有效答案</strong></li></ul><p>**进阶：**你可以想出一个时间复杂度小于 <code>O(n2)</code> 的算法吗？</p><hr><h3 id="官方提示-附翻译">官方提示(附翻译)</h3><ol><li><p>A really brute force way would be to search for all possible pairs of numbers but that would be too slow. Again, it’s best to try out brute force solutions for just for completeness. It is from these brute force solutions that you can come up with optimizations.(一种非常暴力的方法是搜索所有可能的数字对，但这会太慢。同样，为了完整性，最好尝试一下暴力解决方案。正是从这些暴力解决方案中，你才能想出优化方法。)</p></li><li><p>So, if we fix one of the numbers, say <code>x</code>, we have to scan the entire array to find the next number <code>y</code> which is <code>value - x</code> where value is the input parameter. Can we change our array somehow so that this search becomes faster?(所以，如果我们固定其中一个数字，比如说 <code>x</code> ，我们就得扫描整个数组来寻找下一个数字 <code>y</code> ，它等于 <code>value - x</code> ，其中 <code>value</code> 是输入参数。我们能否以某种方式改变我们的数组，从而使这种搜索变得更快？)</p></li><li><p>The second train of thought is, without changing the array, can we use additional space somehow? Like maybe a hash map to speed up the search?(第二种思路是，在不改变数组的情况下，我们能否以某种方式使用额外的空间？比如，或许可以使用哈希表来加快搜索速度？)</p></li></ol><hr><h3 id="开始解题">开始解题:</h3><h4 id="思路一-暴力解题">思路一: 暴力解题</h4><p>老实说在第一眼看到这个题的时候,我也是有点懵的,因为这和我在其他平台见过的<strong>两数之和</strong>完全不同,不过没关系,我们完全可以先暴力解题.那对于这道题来说,暴力解题的方法显而易见,两个 <code>for</code> 循环,当然各位也可能直接就有了自己的想法,我这里只阐述我自己的看法.</p><p>那么它的流程就应该像下面这样</p><ol><li><strong>选择第一个数:</strong> 我们从数组的第一个元素开始,用一个大循环(也可以叫做外层循环)来依次选择第一个数,比如 <code>nums[i]</code>.</li><li><strong>选择第二个数:</strong> 对于每一个选定的 <code>nums[i]</code> ,我们都需要在数组的剩余部分寻找一个数 <code>nums[j]</code> ,看看它们俩的和是不是 <code>target</code> .所以,我们在大循环的内部再嵌套一个小循环(内层循环), 从 <code>i+1</code> 的位置开始遍历(因为我们找的是和,所以是一个组合问题,不是排列问题),这样我们不会重复使用同一个元素</li><li><strong>判断和:</strong> 在小循环中,我们检查 <code>nums[i] + nums[j]</code> 是否等于 <code>target</code> .<ul><li>如果相等,则可以直接直接输出它们的下标 <code>[i,j]</code>.</li><li>如果不相等，则内圈循环继续，知道所有的可能性检查完。</li></ul></li></ol><p><strong>复杂度分析:</strong></p><ul><li><strong>时间复杂度</strong>: <code>O(n²)</code>。因为我们用了两层嵌套循环，在最坏的情况下，外层循环执行n次，内层循环平均执行n/2次，总的计算量与 <code>n * n</code> 成正比。</li><li><strong>空间复杂度</strong>: <code>O(1)</code>。我们没有使用额外的、随数组大小变化的存储空间。</li></ul><p><strong>思路总结:</strong> 这个方法虽然简单，但当数组非常大时，<code>n²</code> 的计算量会变得非常缓慢。题目的“进阶”提示也暗示我们，有比 <code>O(n²)</code> 更好的方法。</p><hr><h4 id="思路二-哈希表优化">思路二: 哈希表优化</h4><p>那么这类问题的标准是什么呢.核心思想就是用<em>空间换时间</em>.</p><p>我们上面讲的暴力解法的瓶颈在于“寻找”第二个数的过程太慢了。对于每个 <code>nums[i]</code>，我们都要遍历一遍数组的剩余部分来寻找 <code>target - nums[i]</code>。如果能让这个“寻找”过程变得飞快，问题就解决了。</p><p>那什么数据结构查找起来最快呢？<strong>哈希表</strong>（在C#中是 <code>Dictionary</code>，在C++中是 <code>unordered_map</code>）。它能提供平均 <code>O(1)</code> 时间复杂度的查找、插入和删除操作。</p><p>那么我们的实现逻辑应该是这样的</p><ol><li><strong>创建哈希表</strong>：我们创建一个哈希表，用来存储我们“遍历过”的数字及其对应的下标。键（Key）是数组中的值，值（Value）是该值对应的下标。</li><li><strong>单次遍历</strong>：我们只需要遍历一次数组。对于数组中的每一个元素 <code>nums[i]</code>：<br>a. <strong>计算补数</strong>：计算出我们需要的“另一半”：<code>complement = target - nums[i]</code>。<br>b. <strong>查找哈希表</strong>：现在，我们不去数组里傻傻地找 <code>complement</code> 了，而是去哈希表里查一下，看看 <code>complement</code> 这个键（Key）是否存在。<ul><li><strong>如果存在</strong>：这说明 <code>complement</code> 这个数字我们之前已经遇到过了。我们直接从哈希表中取出它的下标，再配上当前元素的下标 <code>i</code>，就是最终答案。</li><li><strong>如果不存在</strong>：说明到目前为止，我们还没遇到能和 <code>nums[i]</code> 配对的数字。那么，<code>nums[i]</code> 本身可能就是未来某个数字的“另一半”。所以，我们把当前数字 <code>nums[i]</code> 和它的下标 <code>i</code> 存入哈希表中，以备后续的元素查询。</li></ul></li></ol><p>这里我们举个<strong>栗子</strong>: <code>nums = [2, 7, 11, 15]</code>, <code>target = 9</code></p><ul><li><strong>初始化</strong>：创建一个空的哈希表 <code>map</code>。</li><li><strong>循环 <code>i = 0</code></strong>，当前元素 <code>nums[0]</code> (值为 2)。<ul><li>计算补数：<code>complement = 9 - 2 = 7</code>。</li><li>查哈希表：<code>map</code> 中有键 <code>7</code> 吗？没有，<code>map</code> 是空的。</li><li>将当前元素存入哈希表：<code>map.Add(2, 0)</code>。现在 <code>map</code> 是 <code>{2: 0}</code>。</li></ul></li><li><strong>循环 <code>i = 1</code></strong>，当前元素 <code>nums[1]</code> (值为 7)。<ul><li>计算补数：<code>complement = 9 - 7 = 2</code>。</li><li>查哈希表：<code>map</code> 中有键 <code>2</code> 吗？<strong>有！</strong> 它的值（下标）是 <code>0</code>。</li><li>我们找到了,返回 <code>map</code> 中 <code>2</code> 的下标 <code>0</code> 和当前下标 <code>1</code>。最终结果 <code>[0, 1]</code>。</li></ul></li></ul><p><strong>复杂度分析:</strong></p><ul><li><strong>时间复杂度</strong>: <code>O(n)</code>。我们只遍历了数组一次。哈希表的插入和查找操作平均都是 <code>O(1)</code>。</li><li><strong>空间复杂度</strong>: <code>O(n)</code>。在最坏的情况下，我们可能需要将数组中所有元素都存入哈希表。</li></ul><p><strong>思路总结:</strong> 这个方法用一个哈希表的额外空间，将时间复杂度从 <code>O(n²)</code> 成功降到了 <code>O(n)</code>，是这道题的最优解。</p><hr><h3 id="解法语句-仅方法二">解法语句(仅方法二):</h3><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Solution</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">int</span>[] <span class="title">TwoSum</span>(<span class="params"><span class="built_in">int</span>[] nums, <span class="built_in">int</span> target</span>)</span> &#123;</span><br><span class="line"></span><br><span class="line">        Dictionary&lt;<span class="built_in">int</span>, <span class="built_in">int</span>&gt; map = <span class="keyword">new</span> Dictionary&lt;<span class="built_in">int</span>, <span class="built_in">int</span>&gt;();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">0</span>; i &lt; nums.Length; i++) &#123;</span><br><span class="line">            <span class="built_in">int</span> currentNum = nums[i];</span><br><span class="line">            <span class="built_in">int</span> complement = target - currentNum;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (map.ContainsKey(complement)) &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">int</span>[] &#123; map[complement], i &#125;;</span><br><span class="line">            &#125;</span><br><span class="line">            map[currentNum] = i;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">int</span>[<span class="number">0</span>]; </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里最主要的方法是**<code>map.ContainsKey(complement)</code>**</p><ul><li>这是一个非常重要且常用的方法，用于检查字典中是否包含指定的键（Key）。它返回一个布尔值（<code>true</code> 或 <code>false</code>），并且这个操作的平均时间复杂度是 <code>O(1)</code>，非常高效。</li></ul><hr><h3 id="知识点总结与举一反三-待补充">知识点总结与举一反三(待补充)</h3><hr><h2 id="第二题-两数相加-难度-中等">第二题: 两数相加(难度: 中等)</h2><p>原题:</p><p>给你两个 <strong>非空</strong> 的链表，表示两个非负的整数。它们每位数字都是按照 <strong>逆序</strong> 的方式存储的，并且每个节点只能存储 <strong>一位</strong> 数字。</p><p>请你将两个数相加，并以相同形式返回一个表示和的链表。</p><p>你可以假设除了数字 0 之外，这两个数都不会以 0 开头。</p><p><strong>示例 1：</strong></p><p><img src="https://assets.leetcode-cn.com/aliyun-lc-upload/uploads/2021/01/02/addtwonumber1.jpg" alt="img"></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">输入：l1 = [2,4,3], l2 = [5,6,4]</span><br><span class="line">输出：[7,0,8]</span><br><span class="line">解释：342 + 465 = 807.</span><br></pre></td></tr></table></figure><p><strong>示例 2：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入：l1 = [0], l2 = [0]</span><br><span class="line">输出：[0]</span><br></pre></td></tr></table></figure><p><strong>示例 3：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入：l1 = [9,9,9,9,9,9,9], l2 = [9,9,9,9]</span><br><span class="line">输出：[8,9,9,9,0,0,0,1]</span><br></pre></td></tr></table></figure><p><strong>提示：</strong></p><ul><li>每个链表中的节点数在范围 <code>[1, 100]</code> 内</li><li><code>0 &lt;= Node.val &lt;= 9</code></li><li>题目数据保证列表表示的数字不含前导零</li></ul><hr><h3 id="开始解题-2">开始解题:</h3><p>那我作为 Unity 工程师呢,我可以把这个问题进行一个比喻.就比如我们现在有几条路(链表),路上插着几个独立的路标(节点),每个路标上只有一个数字,并且指向下一块路标,我们现在的任务就是沿着两条这样的路径，把每个对应路标上的数字加起来，形成一条新的路径。</p><h4 id="算法逻辑详解">算法逻辑详解:</h4><p>其实这道题的思路和我们小学学过的列竖式加法的过程类似.</p><p>以 <code>342 + 465</code> 为例,我们从个位开始加. <code>2+5=7</code>.然后是十位 <code>4+6=10</code>,我们写下0,然后往百位进一位.最后是百位, <code>3+4</code>,再加上刚才进上来的<code>1</code>,最后等于<code>8</code>.最后结果为<code>807</code>.</p><p>那么这道题目给我们创造了这一个环境,数字是 <strong>逆序</strong> 存储的.也就是说,链表的第一个节点,就是数字的 <strong>个位</strong>.</p><ul><li><code>l1 = [2,4,3]</code> 代表 <code>342</code></li><li><code>l2 = [5,6,4]</code> 代表 <code>465</code></li></ul><p>链表的头节点 <code>l1.val=2</code> 和 <code>l2.val=5</code> 正好就是个位数</p><p>那么我们的算法步骤就可以进行分解了</p><ol><li><p>初始化:</p><ul><li>我们需要一个变量 <code>carry</code> 来存储“进位”，初始时没有进位，所以 <code>carry = 0</code>。</li><li>我们需要一条新的链表来存放结果。为了方便操作，我们通常会创建一个“虚拟头节点”（dummy head）。这就像在拼乐高前，先放一个临时的底座，最后拼好了再把这个底座拿掉。我们用一个指针 <code>current</code> 指向这个虚拟头节点，之后每产生一个新节点，就接在 <code>current</code> 后面。</li></ul></li><li><p>遍历与计算:</p><ul><li><p>我们从两个链表的头节点开始，同步向后遍历。</p></li><li><p>在每一步，我们都取出两个链表当前节点的值。如果其中一个链表已经遍历完了（比如 <code>l1</code> 比 <code>l2</code> 短），我们就认为它的当前值是 <code>0</code>。</p></li><li><p>计算总和：<code>sum = l1当前节点值 + l2当前节点值 + carry</code>。</p><ul><li>根据 <code>sum</code> 计算出要存入新节点的值和新的进位：<ul><li>新节点的值应该是 <code>sum % 10</code> （<code>%</code> 是取余数，比如 <code>17 % 10</code> 结果是 <code>7</code>）。</li><li>新的进位 <code>carry</code> 应该是 <code>sum / 10</code> （在C#中，两个整数相除会自动舍去小数，比如 <code>17 / 10</code> 结果是 <code>1</code>）。</li></ul></li></ul></li><li><p>创建这个新节点，并将其链接到结果链表的末尾。然后，<code>current</code> 指针也向后移动到这个新节点上，为下一次链接做准备。</p></li></ul></li><li><p>循环终止条件: 只要两个链表中还有一个没有遍历完，或者 <code>carry</code> 不为0（处理最高位的进位，例如 <code>99+1=100</code>），我们的循环就要继续。</p></li><li><p>返回结果: 循环结束后，我们想要的完整结果链表就是我们创建的“虚拟头节点”的下一个节点。</p></li></ol><p>我们简单举一个栗子吧:</p><p><strong><code>l1 = [2,4,3]</code>, <code>l2 = [5,6,4]</code></strong></p><ul><li><strong>第1步 (个位):</strong><ul><li><code>carry = 0</code></li><li><code>sum = 2 + 5 + 0 = 7</code></li><li>新节点值为 <code>7 % 10 = 7</code>，新 <code>carry = 7 / 10 = 0</code>。</li><li>结果链表: <code>[7]</code></li></ul></li><li><strong>第2步 (十位):</strong><ul><li><code>carry = 0</code></li><li><code>sum = 4 + 6 + 0 = 10</code></li><li>新节点值为 <code>10 % 10 = 0</code>，新 <code>carry = 10 / 10 = 1</code>。</li><li>结果链表: <code>[7, 0]</code></li></ul></li><li><strong>第3步 (百位):</strong><ul><li><code>carry = 1</code></li><li><code>sum = 3 + 4 + 1 = 8</code></li><li>新节点值为 <code>8 % 10 = 8</code>，新 <code>carry = 8 / 10 = 0</code>。</li><li>结果链表: <code>[7, 0, 8]</code></li></ul></li><li><strong>结束:</strong><ul><li><code>l1</code> 和 <code>l2</code> 都遍历完了，<code>carry</code> 也变回了 <code>0</code>。循环结束。返回结果 <code>[7, 0, 8]</code>。</li></ul></li></ul><hr><h3 id="解法语句">解法语句:</h3><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Definition for singly-linked list.</span></span><br><span class="line"><span class="comment"> * public class ListNode &#123;</span></span><br><span class="line"><span class="comment"> *     public int val;</span></span><br><span class="line"><span class="comment"> *     public ListNode next;</span></span><br><span class="line"><span class="comment"> *     public ListNode(int val=0, ListNode next=null) &#123;</span></span><br><span class="line"><span class="comment"> *         this.val = val;</span></span><br><span class="line"><span class="comment"> *         this.next = next;</span></span><br><span class="line"><span class="comment"> *     &#125;</span></span><br><span class="line"><span class="comment"> * &#125;</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Solution</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> ListNode <span class="title">AddTwoNumbers</span>(<span class="params">ListNode l1, ListNode l2</span>)</span> &#123;</span><br><span class="line">        <span class="comment">// 1. 初始化：创建一个虚拟头节点，方便我们操作链表</span></span><br><span class="line">        ListNode dummyHead = <span class="keyword">new</span> ListNode(<span class="number">0</span>); </span><br><span class="line">        <span class="comment">// 创建一个“当前”指针，用于构建新的结果链表</span></span><br><span class="line">        ListNode current = dummyHead; </span><br><span class="line">        <span class="comment">// 初始化进位值为0</span></span><br><span class="line">        <span class="built_in">int</span> carry = <span class="number">0</span>; </span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 循环：只要l1或l2不为空，或者还有进位，就继续计算</span></span><br><span class="line">        <span class="keyword">while</span> (l1 != <span class="literal">null</span> || l2 != <span class="literal">null</span> || carry != <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="comment">// 获取l1和l2的当前节点值，如果节点为空，则值为0</span></span><br><span class="line">            <span class="built_in">int</span> v1 = (l1 != <span class="literal">null</span>) ? l1.val : <span class="number">0</span>;</span><br><span class="line">            <span class="built_in">int</span> v2 = (l2 != <span class="literal">null</span>) ? l2.val : <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 计算总和 = 节点1的值 + 节点2的值 + 上一步的进位</span></span><br><span class="line">            <span class="built_in">int</span> sum = v1 + v2 + carry;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 更新进位值，为下一次循环做准备</span></span><br><span class="line">            carry = sum / <span class="number">10</span>;</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 创建新节点，值为总和的个位数</span></span><br><span class="line">            <span class="comment">// 并将其链接到结果链表的末尾</span></span><br><span class="line">            current.next = <span class="keyword">new</span> ListNode(sum % <span class="number">10</span>);</span><br><span class="line">            </span><br><span class="line">            <span class="comment">// 将“当前”指针移动到新创建的节点上</span></span><br><span class="line">            current = current.next;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 将l1和l2指针向后移动（如果它们不为空）</span></span><br><span class="line">            <span class="keyword">if</span> (l1 != <span class="literal">null</span>) &#123;</span><br><span class="line">                l1 = l1.next;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (l2 != <span class="literal">null</span>) &#123;</span><br><span class="line">                l2 = l2.next;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 返回结果：虚拟头节点的下一个节点才是我们真正结果链表的头</span></span><br><span class="line">        <span class="keyword">return</span> dummyHead.next;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h4 id="代码讲解">代码讲解:</h4><ol><li>那么上面的代码用到了一个特殊的用法</li></ol><p><strong><code>int v1 = (l1 != null) ? l1.val : 0;</code> - 三元运算符</strong></p><ul><li>这是一个非常简洁的 <code>if-else</code> 写法，称为<strong>三元运算符</strong>。</li><li>它的结构是 <code>condition ? value_if_true : value_if_false</code>。</li><li>这行代码等价于：</li></ul><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">int</span> v1;</span><br><span class="line"><span class="keyword">if</span> (l1 != <span class="literal">null</span>) &#123;</span><br><span class="line">    v1 = l1.val;</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    v1 = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ol start="2"><li>对 <code>dummyHead</code> 和最后的 <code>return dummyHead.next</code> 的讲解:</li></ol><p><code>dummyHead</code> 有一个名字----虚拟头节点/哨兵节点,是一个非常经典且有用的技巧.</p><p>我们先来一个简单的比喻吧----穿珠子</p><p>想象一下，我们要用线把一堆珠子（<code>ListNode</code>）串成一条手链（结果链表）。</p><ul><li><p><strong>没有 <code>dummyHead</code> 的情况：</strong></p><ol><li>我们拿起第一个珠子。因为是第一个，我们的两只手都很忙，一只手要捏住珠子，另一只手要拿线准备穿过去。这个动作很特别。</li><li>然后，我们拿起第二个珠子。现在我们一只手捏着已经穿好的第一个珠子和线头，另一只手拿新珠子穿进去。这个动作和第一步不一样。</li><li>之后所有珠子，都重复第二步的动作。</li></ol><p>我们看，<strong>处理第一个珠子的方式和处理后续所有珠子的方式是不同的</strong>。在代码里，这就意味着你需要在循环里写一个 <code>if</code> 判断，来专门处理“头节点为空”的特殊情况。</p></li><li><p><strong>有 <code>dummyHead</code> 的情况：</strong></p><ol><li>我们不是直接开始穿珠子，而是先在线的末端系上一个<strong>临时的、不起眼的小疙瘩</strong>（这就是 <code>dummyHead</code>）。这个小疙瘩你最后会剪掉。</li><li>现在，我们拿起第一个“真正”的珠子，把它穿在线上，挨着那个小疙瘩。</li><li>我们拿起第二个“真正”的珠子，把它穿在线上，挨着第一个珠子。</li><li>我们拿起第 N 个“真正”的珠子，把它穿在线上，挨着第 N-1 个珠子。</li></ol><p>发现了么？有了这个“小疙瘩”作为起点，我们<strong>每一次穿珠子的动作都变得完全一样了</strong>！你不需要再特殊处理第一个珠子。</p></li></ul><p>这个比喻可能还是有些难以理解,没关系,我们回到代码里看看</p><ul><li>为什么需要 <code>dummyHead</code>？——为了简化操作</li></ul><p>我们的目标是创建一个新链表。在循环的每一步，我们都会创建一个新节点 <code>newNode</code>。</p><p><strong>如果没有 <code>dummyHead</code>，代码会长这样：</strong></p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> ListNode <span class="title">AddTwoNumbers</span>(<span class="params">ListNode l1, ListNode l2</span>)</span> &#123;</span><br><span class="line">    ListNode resultHead = <span class="literal">null</span>; <span class="comment">// 结果链表的头，一开始是空的</span></span><br><span class="line">    ListNode current = <span class="literal">null</span>;    <span class="comment">// 当前节点指针</span></span><br><span class="line">    <span class="built_in">int</span> carry = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (l1 != <span class="literal">null</span> || l2 != <span class="literal">null</span> || carry != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// sum, carry 的逻辑不变</span></span><br><span class="line">        <span class="built_in">int</span> sum = ...;</span><br><span class="line">        carry = sum / <span class="number">10</span>;</span><br><span class="line">      </span><br><span class="line">        ListNode newNode = <span class="keyword">new</span> ListNode(sum % <span class="number">10</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 这里是关键的、繁琐的部分</span></span><br><span class="line">        <span class="keyword">if</span> (resultHead == <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 如果是第一个节点</span></span><br><span class="line">            resultHead = newNode;</span><br><span class="line">            current = newNode;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// 如果不是第一个节点</span></span><br><span class="line">            current.next = newNode;</span><br><span class="line">            current = current.next;</span><br><span class="line">        &#125;</span><br><span class="line">      </span><br><span class="line">        <span class="comment">//移动 l1, l2 的逻辑不变</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> resultHead;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>看到 <code>if-else</code> 了吗？它必须在<strong>每一次循环</strong>中都进行判断，这既不优雅，也稍微增加了一点点计算开销。</p><p><strong>有了 <code>dummyHead</code>，代码就变得统一和简洁：</strong></p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">ListNode dummyHead = <span class="keyword">new</span> ListNode(<span class="number">0</span>); <span class="comment">// 创建“小疙瘩”</span></span><br><span class="line">ListNode current = dummyHead;         <span class="comment">// 用手先捏住这个“小疙瘩”</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> (l1 != <span class="literal">null</span> || l2 != <span class="literal">null</span> || carry != <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="comment">// 计算逻辑不变</span></span><br><span class="line">  </span><br><span class="line">    <span class="comment">// 每次的动作都完全一样：</span></span><br><span class="line">    <span class="comment">// 把新珠子(newNode)接到手捏着的那个珠子(current)后面</span></span><br><span class="line">    current.next = <span class="keyword">new</span> ListNode(sum % <span class="number">10</span>); </span><br><span class="line">    <span class="comment">// 然后手向前移动，捏住刚穿好的新珠子</span></span><br><span class="line">    current = current.next; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>dummyHead</code> 的存在，确保了 <code>current</code> 指针一开始就指向一个实际的对象，而不是 <code>null</code>。因此，<code>current.next = ...</code> 这个操作永远不会因为 <code>current</code> 是 <code>null</code> 而报错。我们成功地<strong>消除了对第一个节点的特殊处理</strong>。</p><ul><li>为什么是 <code>return dummyHead.next</code>？——因为“小疙瘩”不是我们要的结果</li></ul><p>循环结束后，我们的链表结构是这样的（以 <code>[7,0,8]</code> 为例)：</p><p><img src="https://image.nfasystem.top/img-nfa/2025%2F07%2F551a275f6e18a66f86eb2405111b28d8.ext.jpg" alt="IMG_20250727_223103"></p><ul><li><code>dummyHead</code> 这个变量，从始至终都指向我们最开始创建的那个值为 <code>0</code> 的<strong>临时节点</strong>。它就像那个我们不打算送人的“小疙瘩”。</li><li>我们真正想要的手链（结果链表），是从第一个有意义的珠子，也就是值为 <code>7</code> 的那个节点开始的。</li><li>这个值为 <code>7</code> 的节点，正好是 <code>dummyHead</code> 节点的 <code>next</code> 指针所指向的地方。</li></ul><p>所以，<code>return dummyHead.next;</code> 的意思就是：“好了，手链穿完了，现在我们把开头的那个临时小疙瘩忽略掉，把从第一个真正的珠子开始的整条链返回给别人。”</p><p><strong>总结一下：</strong></p><ul><li><strong><code>dummyHead</code></strong> 是一个编程技巧，它是一个<strong>辅助性的、不属于最终结果</strong>的节点。它的核心作用是<strong>简化链表头部的插入操作，让循环逻辑保持统一</strong>，避免对空链表进行特殊判断。</li><li><strong><code>return dummyHead.next</code></strong> 是因为 <code>dummyHead</code> 本身是我们的“脚手架”，而真正的“建筑”（结果链表）是从它的下一个节点开始的。返回 <code>dummyHead.next</code> 就是返回我们真正构建的结果。</li></ul>]]></content>
      
      
      <categories>
          
          <category> 力扣刷题 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 算法 </tag>
            
            <tag> 力扣 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>从 Rider 的一条建议谈起：深入理解 C# 表达式体成员</title>
      <link href="/posts/ec87c3ca/"/>
      <url>/posts/ec87c3ca/</url>
      
        <content type="html"><![CDATA[<h2 id="问题引入">问题引入</h2><p>在日常使用 Rider 编辑器进行 Unity 开发时，我们可能会在编写属性时遇到这样一条建议：“代码主体不符合代码样式设置: 请使用表达式体 (Code body does not match code style settings: Use expression body)”。</p><p>例如，在下面这段用于控制游戏音乐开关的属性代码中，Rider 会在 <code>get</code> 访问器的 <code>return</code> 语句处给出该提示：</p><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="built_in">bool</span> MusicOn</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">get</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> PlayerPrefs.GetInt(<span class="string">&quot;Music&quot;</span>, <span class="number">1</span>) == <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">set</span></span><br><span class="line">    &#123;</span><br><span class="line">        PlayerPrefs.SetInt(<span class="string">&quot;Music&quot;</span>, <span class="keyword">value</span> ? <span class="number">1</span> : <span class="number">0</span>);</span><br><span class="line">        SoundManager.Instance.MusicOn = <span class="keyword">value</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这个建议是什么意思？它背后又涉及到了哪些 C# 的知识点？这正是我们本文将要深入探讨的主题。</p><h2 id="什么是表达式体成员？">什么是表达式体成员？</h2><p>Rider 的这条建议，实际上是在引导我们使用 C# 6.0 版本引入的一个特性：<strong>表达式体成员 (Expression-bodied Members)</strong>。</p><p>我们可以将这个特性理解为一种“语法糖”。所谓“语法糖”，并不是什么复杂的魔法，而是一种更简洁、更优雅的语法，它能让我们用更少的代码实现同样的功能，从而提高代码的可读性和编写效率。</p><h3 id="核心思想：用更少的话，说清楚一件事">核心思想：用更少的话，说清楚一件事</h3><p>想象一个生活中的场景，当朋友问我们“现在几点了？”，我们通常会直接回答“三点半”，而不是说“我将要告诉你现在的时间，现在的时间是三点半。”</p><p>表达式体成员正是基于同样直截了当的思想。对于那些逻辑非常简单、只包含<strong>单一操作</strong>的类成员（如方法或属性），我们无需编写完整的 <code>{}</code> 代码块，而是可以直接通过 <code>=&gt;</code> 符号来表达其实现。</p><p>这里的 <code>=&gt;</code> 符号，我们通常不读作 Lambda 表达式中的 “Lambda”，而是理解为 “goes to” (指向)。它的含义是：<strong>“这个成员的实现，就是紧跟在它后面的这个表达式”。</strong></p><p>我们回头看最初的例子：</p><p><strong>传统代码块主体 (Block Body):</strong></p><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">get</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> PlayerPrefs.GetInt(<span class="string">&quot;Music&quot;</span>, <span class="number">1</span>) == <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>表达式体 (Expression Body):</strong></p><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">get</span> =&gt; PlayerPrefs.GetInt(<span class="string">&quot;Music&quot;</span>, <span class="number">1</span>) == <span class="number">1</span>;</span><br></pre></td></tr></table></figure><p>通过 <code>=&gt;</code>，我们将三行代码缩减为一行，代码的意图一目了然：<code>get</code> 的结果就是 <code>PlayerPrefs.GetInt(&quot;Music&quot;, 1) == 1</code> 这个表达式的计算结果。</p><h2 id="表达式体成员的演进">表达式体成员的演进</h2><p>这个特性并非一开始就支持所有场景，了解它的发展历程有助于我们理解在不同版本的 C# 代码中看到的不同用法。</p><ul><li><strong>C# 6.0</strong>: 首次引入，支持<strong>方法 (Method)</strong> 和<strong>只读属性 (Read-only Property)</strong>。</li><li><strong>C# 7.0</strong>: 功能得到极大扩展，开始支持<strong>构造函数 (Constructor)</strong>、<strong>终结器 (Finalizer)</strong>、<strong>属性的 <code>get</code> 和 <code>set</code> 访问器</strong>以及<strong>索引器 (Indexer)</strong>。</li></ul><p>对于 Unity 开发者而言，随着 Unity 版本的迭代，其内置的 C# 编译器版本也在升级，使得我们现在可以广泛地使用这些现代 C# 特性。</p><h2 id="表达式体成员的实际应用">表达式体成员的实际应用</h2><p>下面我们通过具体的代码示例，来对比传统写法和表达式体写法在不同场景下的应用。</p><h3 id="方法-Methods">方法 (Methods)</h3><p>当一个方法只包含一条 <code>return</code> 语句，或仅执行一个操作时。</p><ul><li><strong>传统写法:</strong><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="built_in">float</span> <span class="title">GetCircleArea</span>(<span class="params"><span class="built_in">float</span> radius</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">3.14159f</span> * radius * radius;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><strong>表达式体写法:</strong><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="built_in">float</span> <span class="title">GetCircleArea</span>(<span class="params"><span class="built_in">float</span> radius</span>)</span> =&gt; <span class="number">3.14159f</span> * radius * radius;</span><br></pre></td></tr></table></figure></li></ul><h3 id="属性-Properties">属性 (Properties)</h3><p>这是我们最开始遇到的场景。</p><ul><li><p><strong>只读属性</strong></p><ul><li><strong>传统写法:</strong><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="built_in">string</span> PlayerStatus</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">get</span> &#123; <span class="keyword">return</span> _isAlive ? <span class="string">&quot;Alive&quot;</span> : <span class="string">&quot;Defeated&quot;</span>; &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">private</span> <span class="built_in">bool</span> _isAlive = <span class="literal">true</span>;</span><br></pre></td></tr></table></figure></li><li><strong>表达式体写法:</strong><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="built_in">string</span> PlayerStatus =&gt; _isAlive ? <span class="string">&quot;Alive&quot;</span> : <span class="string">&quot;Defeated&quot;</span>;</span><br><span class="line"><span class="keyword">private</span> <span class="built_in">bool</span> _isAlive = <span class="literal">true</span>;</span><br></pre></td></tr></table></figure></li></ul></li><li><p><strong>可读可写属性的 <code>get</code> 和 <code>set</code> 访问器</strong></p><ul><li><strong>传统写法:</strong><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="built_in">float</span> _health;</span><br><span class="line"><span class="keyword">public</span> <span class="built_in">float</span> Health</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">get</span> &#123; <span class="keyword">return</span> _health; &#125;</span><br><span class="line">    <span class="keyword">set</span> &#123; _health = Mathf.Clamp(<span class="keyword">value</span>, <span class="number">0</span>, <span class="number">100</span>); &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><strong>表达式体写法:</strong><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="built_in">float</span> _health;</span><br><span class="line"><span class="keyword">public</span> <span class="built_in">float</span> Health</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">get</span> =&gt; _health;</span><br><span class="line">    <span class="keyword">set</span> =&gt; _health = Mathf.Clamp(<span class="keyword">value</span>, <span class="number">0</span>, <span class="number">100</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul></li></ul><h3 id="构造函数-Constructors">构造函数 (Constructors)</h3><p>当构造函数只执行一个简单的赋值操作时。</p><ul><li><strong>传统写法:</strong><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Player</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="built_in">string</span> _name;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Player</span>(<span class="params"><span class="built_in">string</span> name</span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">this</span>._name = name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><strong>表达式体写法:</strong><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Player</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="built_in">string</span> _name;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Player</span>(<span class="params"><span class="built_in">string</span> name</span>)</span> =&gt; <span class="keyword">this</span>._name = name;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h3 id="索引器-Indexers">索引器 (Indexers)</h3><p>当索引器的 <code>get</code> 和 <code>set</code> 访问器逻辑足够简单时。</p><ul><li><strong>传统写法:</strong><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Team</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">private</span> Player[] _players = <span class="keyword">new</span> Player[<span class="number">5</span>];</span><br><span class="line">    <span class="keyword">public</span> Player <span class="keyword">this</span>[<span class="built_in">int</span> index]</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">get</span> &#123; <span class="keyword">return</span> _players[index]; &#125;</span><br><span class="line">        <span class="keyword">set</span> &#123; _players[index] = <span class="keyword">value</span>; &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><strong>表达式体写法:</strong><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Team</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">private</span> Player[] _players = <span class="keyword">new</span> Player[<span class="number">5</span>];</span><br><span class="line">    <span class="keyword">public</span> Player <span class="keyword">this</span>[<span class="built_in">int</span> index]</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">get</span> =&gt; _players[index];</span><br><span class="line">        <span class="keyword">set</span> =&gt; _players[index] = <span class="keyword">value</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h2 id="规则与限制：何时不应使用">规则与限制：何时不应使用</h2><p>理解一个特性的边界和限制，与掌握其用法同等重要。</p><ol><li><p><strong>必须是单一表达式</strong><br>这是最核心的规则。<code>=&gt;</code> 之后必须是且仅是一个表达式，不能是多个用分号隔开的语句。我们最初的例子中，<code>set</code> 访问器包含两条语句，因此它不能被简化为表达式体。</p></li><li><p><strong>不能包含复杂的流程控制语句</strong><br>诸如 <code>if-else</code> (三元运算符 <code>?:</code> 是例外)、<code>for</code>、<code>foreach</code>、<code>while</code>、<code>switch</code> 等语句块不能直接用于表达式体。</p><ul><li><strong>错误示范:</strong><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 编译错误！不能在表达式体中使用 if 语句块</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="built_in">string</span> <span class="title">GetWeather</span>(<span class="params"><span class="built_in">float</span> temp</span>)</span> =&gt; <span class="keyword">if</span> (temp &gt; <span class="number">30</span>) <span class="keyword">return</span> <span class="string">&quot;Hot&quot;</span>; <span class="keyword">else</span> <span class="keyword">return</span> <span class="string">&quot;Cool&quot;</span>;</span><br></pre></td></tr></table></figure></li><li><strong>正确替代 (使用三元运算符):</strong><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="built_in">string</span> <span class="title">GetWeather</span>(<span class="params"><span class="built_in">float</span> temp</span>)</span> =&gt; temp &gt; <span class="number">30</span> ? <span class="string">&quot;Hot&quot;</span> : <span class="string">&quot;Cool&quot;</span>;</span><br></pre></td></tr></table></figure></li></ul></li></ol><h2 id="结论与建议">结论与建议</h2><p>表达式体成员是 C# 提供的一个强大的代码简化工具。它能让我们的代码更加紧凑，意图更加清晰。</p><p>通过理解并恰当运用表达式体成员，我们可以编写出更具现代感、更优雅、更易于维护的 C# 代码。</p>]]></content>
      
      
      <categories>
          
          <category> 技术分享 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Unity </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>从 Rider 的一个建议谈起：深入理解 C# 模式匹配</title>
      <link href="/posts/fc4aea50/"/>
      <url>/posts/fc4aea50/</url>
      
        <content type="html"><![CDATA[<h2 id="问题的引入">问题的引入</h2><p>在日常的 Unity 开发中，我们经常会与各种集成开发环境（IDE）打交道，如 Rider 或 Visual Studio。这些强大的工具不仅能帮助我们编写代码，其智能提示功能有时更能成为我们学习新知识的契机。</p><p>最近，我在在编写一段处理对话节点的逻辑时，我写下了这样一段非常普遍的代码：</p><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// nodeToDisplay 是一个包含对话选项的对象</span></span><br><span class="line"><span class="comment">// choices 是一个字符串数组</span></span><br><span class="line"><span class="keyword">if</span> (nodeToDisplay.choices != <span class="literal">null</span> &amp;&amp; nodeToDisplay.choices.Length &gt; <span class="number">0</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 如果存在选项，则显示选项按钮...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这段代码的意图非常明确：为了安全地访问数组的 <code>Length</code> 属性，我们首先检查 <code>choices</code> 数组本身是否为 <code>null</code>，然后再检查其长度是否大于零。这是一个健壮且完全正确的写法。</p><p>然而，Rider 编辑器在 <code>&amp;&amp;</code> 符号下给出了一条建议：“合并到模式中”（Merge into pattern）。这不禁让我思考：</p><ul><li>这个建议是什么意思？</li><li>它推荐的写法是怎样的，又有什么优势？</li><li>其背后蕴含了 C# 语言的何种特性？</li></ul><p>这便是本文将要深入探讨的核心：<strong>C# 模式匹配（Pattern Matching）</strong>。</p><h2 id="从“指令”到“画像”：模式匹配的核心思想">从“指令”到“画像”：模式匹配的核心思想</h2><p>在深入了解具体的语法之前，我们首先需要理解模式匹配的哲学思想。我们可以用一个门卫的例子来做类比。</p><ul><li><p><strong>传统的过程式指令</strong>：我们给门卫下达一系列按部就班的指令。“第一步，检查访客是否持有邀请函（<code>!= null</code>）。第二步，如果持有，打开邀请函检查其身份是否为 VIP（<code>.Status == &quot;VIP&quot;</code>）。第三步，如果身份是 VIP，再检查其姓名是否在白名单上…”。这一系列指令必须严格按顺序执行，逻辑链条清晰，但略显繁琐。</p></li><li><p><strong>现代的声明式“画像”</strong>：我们直接给门卫一张目标人物的“画像”。“放一个<strong>持有 VIP 邀请函、且姓名在白名单上的人</strong>进来”。我们不再描述“如何检查”，而是直接描述“目标长什么样”。</p></li></ul><p>C# 的模式匹配，正是这样一种让我们能够用声明式“画像”来检查数据“形状”和“值”的强大工具。它让代码的意图变得更加直观。</p><h2 id="模式匹配的语法结构">模式匹配的语法结构</h2><p>要熟练运用模式匹配，我们首先需要掌握其基本的语法构建块。模式匹配主要通过 <code>is</code> 关键字（在 <code>if</code> 语句中）和 <code>switch</code> 语句的 <code>case</code> 标签来实现。</p><p>一个模式匹配表达式的基本形式是：<code>&lt;待检查的变量&gt; is &lt;模式&gt;</code></p><p>下面是构成 <code>&lt;模式&gt;</code> 部分的核心组件：</p><ol><li><p><strong>常量模式 (Constant Pattern)</strong></p><ul><li><strong>语法</strong>: 直接使用一个常量值。</li><li><strong>示例</strong>: <code>is null</code>, <code>is 10</code>, <code>is &quot;Completed&quot;</code></li><li><strong>用途</strong>: 判断变量是否等于某个具体的常量值。</li></ul></li><li><p><strong>类型模式 (Type Pattern)</strong></p><ul><li><strong>语法</strong>: <code>&lt;类型&gt; &lt;新变量名&gt;</code></li><li><strong>示例</strong>: <code>is Player player</code>, <code>is Rigidbody rb</code></li><li><strong>用途</strong>: 判断变量是否为指定类型。如果判断成功，会自动将变量转换为该类型并赋值给新的局部变量（如 <code>player</code> 或 <code>rb</code>），该变量仅在 <code>if</code> 语句块内有效。</li></ul></li><li><p><strong>属性模式 (Property Pattern)</strong></p><ul><li><strong>语法</strong>: <code>{ 属性1: &lt;模式1&gt;, 属性2: &lt;模式2&gt; }</code></li><li><strong>示例</strong>: <code>{ Health: &gt; 0, Name: &quot;Goblin&quot; }</code></li><li><strong>用途</strong>: 检查对象的一个或多个公共属性。它使用 <code>{}</code> 包裹，内部可以包含多个“属性: 模式”对，用逗号分隔。重要的是，属性后面跟的也是一个模式（可以是常量模式、关系模式等）。</li></ul></li><li><p><strong>关系模式 (Relational Pattern)</strong></p><ul><li><strong>语法</strong>: <code>&gt;</code>、<code>&lt;</code>、<code>&gt;=</code>、<code>&lt;=</code></li><li><strong>示例</strong>: <code>&gt; 10</code>, <code>&lt;= 0</code></li><li><strong>用途</strong>: 用于和数值进行比较，通常嵌套在属性模式中使用。</li></ul></li><li><p><strong>逻辑模式 (Logical Patterns)</strong></p><ul><li><strong>语法</strong>: <code>and</code>、<code>or</code>、<code>not</code></li><li><strong>示例</strong>: <code>&gt; 0 and &lt; 100</code>, <code>not null</code></li><li><strong>用途</strong>: 用于组合多个模式，形成更复杂的逻辑判断。</li></ul></li><li><p><strong><code>when</code> 子句 (附加条件)</strong></p><ul><li><strong>语法</strong>: <code>case &lt;模式&gt; when &lt;布尔表达式&gt;:</code></li><li><strong>用途</strong>: 这是 <code>switch</code> 语句中的一个强大补充。当仅靠模式无法完全表达判断条件时（例如需要调用一个方法），可以使用 <code>when</code> 关键字来添加一个额外的布尔表达式作为判断条件。</li></ul></li></ol><p>在掌握了这些基础语法之后，我们再通过具体的案例来加深理解。</p><hr><h2 id="模式匹配的“兵器谱”：实战案例">模式匹配的“兵器谱”：实战案例</h2><h3 id="类型模式-Type-Pattern">类型模式 (Type Pattern)</h3><ul><li><strong>场景</strong>：在 <code>OnTriggerEnter</code> 中判断碰撞到的物体是否为“玩家”。</li><li><strong>语法应用</strong>：<code>other.GetComponent&lt;Player&gt;() is Player player</code></li><li><strong>代码示例</strong>：<figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">OnTriggerEnter</span>(<span class="params">Collider other</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// &quot;如果 GetComponent 的结果是 Player 类型，则将其赋值给新变量 player&quot;</span></span><br><span class="line">    <span class="keyword">if</span> (other.GetComponent&lt;Player&gt;() <span class="keyword">is</span> Player player)</span><br><span class="line">    &#123;</span><br><span class="line">        player.TakeDamage(<span class="number">10</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>这里，我们使用了类型模式，将类型检查、<code>null</code> 检查、类型转换和变量赋值四个步骤一气呵成。</li></ul><h3 id="属性模式-Property-Pattern">属性模式 (Property Pattern)</h3><ul><li><strong>场景</strong>：我们希望攻击一个敌人，但前提是该敌人必须存活（<code>Health &gt; 0</code>）且并非处于无敌状态（<code>isInvincible == false</code>）。</li><li><strong>语法应用</strong>：<code>enemy is { Health: &gt; 0, isInvincible: false }</code></li><li><strong>代码示例</strong>：<figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">TryAttack</span>(<span class="params">Enemy enemy</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 这里组合了属性模式、关系模式(&gt; 0)和常量模式(false)</span></span><br><span class="line">    <span class="keyword">if</span> (enemy <span class="keyword">is</span> &#123; Health: &gt; <span class="number">0</span>, isInvincible: <span class="literal">false</span> &#125;)</span><br><span class="line">    &#123;</span><br><span class="line">        enemy.TakeDamage(<span class="number">25</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><code>{...}</code> 构成的属性模式就像一个清晰的模板，直观地描述了我们所期望的数据状态。它自带的 <code>null</code> 检查保证了代码的安全性。</li></ul><h3 id="逻辑模式-Logical-Patterns">逻辑模式 (Logical Patterns)</h3><ul><li><strong>场景</strong>：一个玩家角色只有在生命值满格（<code>Health &gt;= 100</code>）或者法力值大于 50（<code>Mana &gt; 50</code>）时，才能释放某个终极技能。</li><li><strong>语法应用</strong>：<code>player is { Health: &gt;= 100 } or { Mana: &gt; 50 }</code></li><li><strong>代码示例</strong>：<figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (player <span class="keyword">is</span> &#123; Health: &gt;= <span class="number">100</span> &#125; <span class="keyword">or</span> &#123; Mana: &gt; <span class="number">50</span> &#125;)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// Cast ultimate skill</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><code>or</code> 关键字让这个组合条件的意图变得非常清晰，比传统的 <code>||</code> 连接的多个表达式更易于阅读。</li></ul><hr><h2 id="switch-表达式：模式匹配的终极舞台"><code>switch</code> 表达式：模式匹配的终极舞台</h2><p>如果说 <code>if</code> 语句是模式匹配的“训练场”，那么 <code>switch</code> 语句和 <code>switch</code> 表达式就是其威力的“终极舞台”。</p><ul><li><strong>场景</strong>：重构一个复杂的 <code>OnTriggerEnter</code> 方法，根据碰撞到的不同对象执行不同逻辑。</li><li><strong>代码示例</strong>：<figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">OnTriggerEnter</span>(<span class="params">Collider other</span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">switch</span> (other.gameObject)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 类型模式 + when 子句</span></span><br><span class="line">        <span class="keyword">case</span> GameObject g <span class="keyword">when</span> g.CompareTag(<span class="string">&quot;Player&quot;</span>):</span><br><span class="line">            Debug.Log(<span class="string">&quot;碰到玩家了！&quot;</span>);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 属性模式 + when 子句 + 类型模式</span></span><br><span class="line">        <span class="keyword">case</span> &#123; tag: <span class="string">&quot;Enemy&quot;</span> &#125; <span class="keyword">when</span> other.GetComponent&lt;Enemy&gt;() <span class="keyword">is</span> &#123; Health: &gt; <span class="number">0</span> &#125; enemy:</span><br><span class="line">            enemy.TakeDamage(<span class="number">99</span>);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 属性模式，并把 layer 值赋给新变量 l</span></span><br><span class="line">        <span class="keyword">case</span> &#123; layer: <span class="built_in">int</span> l &#125; <span class="keyword">when</span> l == LayerMask.NameToLayer(<span class="string">&quot;Collectable&quot;</span>):</span><br><span class="line">            Destroy(other.gameObject);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">      </span><br><span class="line">        <span class="comment">// 常量模式，优雅地处理 null</span></span><br><span class="line">        <span class="keyword">case</span> <span class="literal">null</span>:</span><br><span class="line">            Debug.LogWarning(<span class="string">&quot;碰到的物体已被销毁！&quot;</span>);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 默认情况</span></span><br><span class="line">        <span class="literal">default</span>:</span><br><span class="line">            Debug.Log(<span class="string">&quot;碰到了其他东西。&quot;</span>);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>这个 <code>switch</code> 结构清晰地罗列了所有可能的情况，将条件判断与逻辑执行解耦，每个 <code>case</code> 分支都是一个独立的“画像”，使得整个逻辑一目了然。</li></ul><hr><h2 id="结论：为何我们应当拥抱模式匹配">结论：为何我们应当拥抱模式匹配</h2><p>通过以上的分析，我们可以总结出在项目中使用模式匹配的几点核心优势：</p><ol><li><strong>简洁性 (Conciseness)</strong>：用更少的代码表达更复杂的逻辑，告别冗长的 <code>if-null-then-check-property</code> 链条。</li><li><strong>可读性 (Readability)</strong>：代码即文档。模式匹配的声明式语法使其意图更加清晰，更接近自然语言描述。</li><li><strong>安全性 (Safety)</strong>：内置的 <code>null</code> 检查机制能从源头上规避大量潜在的 <code>NullReferenceException</code>，这对于所有开发者来说，都是一个巨大的福音。</li><li><strong>表现力 (Expressiveness)</strong>：尤其是在 <code>switch</code> 语句中，模式匹配允许我们构建出以往用 <code>if-else</code> 难以优雅实现的复杂、扁平的逻辑分支结构。</li></ol><p>C# 模式匹配是现代 C# 语言赠予开发者的一把利器。熟练掌握它，将使我们的代码在健壮性、可读性和维护性上都迈上一个新的台阶。</p>]]></content>
      
      
      <categories>
          
          <category> 技术分享 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Unity </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Unity 对象生存期的隐式检查</title>
      <link href="/posts/a266d19c/"/>
      <url>/posts/a266d19c/</url>
      
        <content type="html"><![CDATA[<h2 id="问题引入">问题引入</h2><p>今天在写代码的时候偶然注意到了Rider编辑器给我弹出的一个提示啊，也不算是警告，就是这个 Unity 对象生存期的隐式检查</p><p><img src="https://image.nfasystem.top/img/Unity_Rider_2.png" alt="alt text"></p><p><img src="https://image.nfasystem.top/img/Unity_Rider_1.png" alt="alt text"></p><p>处于好奇啊，我就简单去了解了一下，结果没想到啊，这个东西涉及到了 Unity 的底层工作方式了，那我就边写边学了。</p><hr><h2 id="C-中的-null是什么">C#中的 <code>null</code>是什么</h2><p>在标准的C#程序中，一个变量如果等于 <code>null</code>，意味着它不指向任何内存地址。它就是“空”的，如果尝试访问它的任何成员（比如方法、属性）都会立即抛出 <code>NullReferenceException</code> 异常，这非常直接。</p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 这是一个普通的C#类</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">MyNormalClass</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">DoSomething</span>()</span> &#123; &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">MyNormalClass myInstance = <span class="literal">null</span>;</span><br><span class="line"><span class="comment">// 下面这行代码会立即抛出 NullReferenceException</span></span><br><span class="line">myInstance.DoSomething(); </span><br></pre></td></tr></table></figure><hr><h2 id="Unity-对象的特殊性：C-层与C-层的双重存在">Unity 对象的特殊性：C#层与C++层的双重存在</h2><p>Unity引擎的核心是用C++编写的，而我们写的逻辑代码为C#，场景中每一个 <code>GameObject</code> 或附加在上面的组件（如 <code>Transform</code> , <code>Rigidbody</code> , <code>MonoBehaviour</code> 脚本）在内存中都有两个部分：</p><ul><li><strong>C#包装器对象（Managed Object）：</strong> 这是在C#代码中能直接访问和操作的对象。</li><li><strong>C++引擎对象（Native Object）：</strong> 这是真正在引擎底层负责渲染、物理计算等的实际对象。</li></ul><p>C#对象持有一个指向对应C++对象的指针。</p><hr><h2 id="Destory-的工作机制"><code>Destory()</code> 的工作机制</h2><p>当我们调用 <code>Destory(myGameObject);</code> 时，会发生一件有趣的事情：</p><ul><li><strong>它不是立即销毁的：</strong> Unity不会，马上从内存中清除这个对象，它只是把对应的C++引擎对象标记为了“已销毁”。</li><li><strong>延迟销毁：</strong> 真正的内存清理工作会延迟到当前帧的末尾（在所有Update之后，渲染之前）进行。</li></ul><p>这就产生了一个中间状态：在调用 <code>Destory()</code> 之后但在帧末清理之前，<strong>C#包装器对象依然存在，但它指向的C++引擎对象已经“死亡”了。</strong></p><p>如果这个时候我们用标准的C# <code>null</code> 检查，它会告诉你C#对象还存在（因为它确实还在内存里），这就会导致问题。如果我们尝试访问一个已经“死亡”的对象，可能会引发一个更难理解的错误。</p><hr><h2 id="Unity的解决方案：重载-操作符">Unity的解决方案：重载 == 操作符</h2><p>为了解决这个问题，Unity做了一个非常聪明的设计：它为所有继承自 <code>UnityEngine.Object</code> 的类（基本上就是在Unity中用到了所有东西）<strong>重载（overloaded）了 <code>==</code> 操作符。</strong></p><p>当我们写下这样的代码时：</p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (myGameObject == <span class="literal">null</span>) </span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>你以为你在做一个简单的C# <code>null</code> 检查，但是实际上你调用的是Unity的自定义比较函数。这个函数的工作流程是：</p><ol><li><p>先检查C#包装器对象本身是不是真的 <code>null</code></p></li><li><p>如果不是，它会<strong>跨越到C++层</strong>，询问对应的引擎对象：“还活着吗老弟，还是说被打上销毁的标签了？”</p></li></ol><p>这个“跨越到C++层去询问”的过程，就是Rider所说的“<strong>隐式检查</strong>”。它是“隐式”的，因为从代码表面上看，你只是在做 <code>== null</code> ，但背后却发生了很多的事情。</p><hr><h2 id="为什么Rider要提示呢？">为什么Rider要提示呢？</h2><p>Rider作为一款专业的IDE，它知道这个背后的机制，所以它的提示主要有两个原因：</p><ol><li><p><strong>性能意识 (Performance Awareness)</strong>：这个“跨越到C++层”的检查虽然很快，但相比于一个纯粹的C# <code>null</code> 检查，它是有性能开销的。在大多数情况下，这点开销微不足道。但如果我们在 <code>Update</code> 或 <code>FixedUpdate</code> 等每帧执行多次的函数中，对大量对象进行这种检查，它可能会累积起来，对性能产生微小的影响。Rider在提醒你：“提示一下，这里正在发生一次Unity特有的、有轻微开销的操作。”</p></li><li><p><strong>代码清晰度 (Clarity)</strong>：Rider希望我们清楚地知道我们正在使用的是Unity的特殊行为，而不是标准的C#行为。这有助于我们写出更健壮、更易于理解的代码。</p></li></ol><hr><h2 id="总结与最佳实践">总结与最佳实践</h2><p>所以，当Rider提示我们“Unity 对象生存期的隐式检查”时，它并不是说我们写错了，而是在对我们进行一次友好的、专业的教学。</p><p><strong>我们应该怎么做？</strong></p><ol><li><p><strong>绝大多数情况，请继续使用 <code>== null</code></strong>：<br>这是最安全、最符合直觉的方式来判断一个Unity对象是否已经被销毁。它能正确处理“伪空”对象（即C#对象存在但C++对象已销毁的情况）。</p><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> GameObject player;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Update</span>()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 这是正确且安全的方式，即使Rider有提示</span></span><br><span class="line">    <span class="keyword">if</span> (player == <span class="literal">null</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        Debug.Log(<span class="string">&quot;玩家对象已被销毁！&quot;</span>);</span><br><span class="line">        <span class="comment">// 停止相关逻辑</span></span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 在这里可以安全地使用player</span></span><br><span class="line">    player.transform.position = Vector3.zero; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong>使用更符合Unity风格的简写</strong>：<br><code>UnityEngine.Object</code> 还重载了布尔转换操作符。所以你可以这样写，效果和 <code>!= null</code> 完全一样，代码更简洁。</p><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// if (player != null) 可以简写为:</span></span><br><span class="line"><span class="keyword">if</span> (player) </span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 玩家对象存在且未被销毁</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// if (player == null) 可以简写为:</span></span><br><span class="line"><span class="keyword">if</span> (!player)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 玩家对象为null或已被销毁</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这种写法同样会触发“隐式检查”。</p></li><li><p><strong>在极度追求性能的场景下（非常罕见）</strong>：<br>如果你能100%确定一个对象只会被真正地设为 <code>null</code>，而不会被 <code>Destroy()</code>，并且这段代码在性能瓶颈上，你可以使用 <code>System.Object.ReferenceEquals</code> 来强制进行纯C#的引用比较，绕过Unity的检查。</p><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (System.Object.ReferenceEquals(player, <span class="literal">null</span>))</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 这只会检查C#引用是否为null。</span></span><br><span class="line">    <span class="comment">// 如果player被Destroy()了，这里会判断为false，可能导致后续代码出错！</span></span><br><span class="line">    <span class="comment">// 新手请谨慎使用！</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ol><hr><h2 id="引申几个知识点">引申几个知识点</h2><h3 id="MissingReferenceException-vs-NullReferenceException"><code>MissingReferenceException</code> vs <code>NullReferenceException</code></h3><p>这是最直接的引申点。很多初学者会混淆这两个异常。</p><ul><li><p><strong><code>NullReferenceException</code> (空引用异常)</strong></p><ul><li><strong>发生原因</strong>：这是纯C#层面的错误。你试图访问一个<strong>真正为 <code>null</code></strong> 的变量的成员。这个变量从未被赋值，或者被显式地设为了 <code>null</code>。</li><li><strong>例子</strong>：<figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> Rigidbody rb; <span class="comment">// 没有在Inspector里拖拽赋值</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Start</span>()</span> &#123;</span><br><span class="line">    rb.AddForce(Vector3.up); <span class="comment">// 100% 抛出 NullReferenceException</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul></li><li><p><strong><code>MissingReferenceException</code> (丢失引用异常)</strong></p><ul><li><strong>发生原因</strong>：这是Unity特有的错误。你试图访问一个**“伪空”对象**的成员。也就是说，C#包装器对象还存在，但它底层的C++引擎对象已经被 <code>Destroy()</code> 了。</li><li><strong>例子</strong>：<figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> GameObject enemy;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Start</span>()</span> &#123;</span><br><span class="line">    Destroy(enemy, <span class="number">2f</span>); <span class="comment">// 2秒后销毁敌人</span></span><br><span class="line">    StartCoroutine(AttackEnemyAfterThreeSeconds());</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">IEnumerator <span class="title">AttackEnemyAfterThreeSeconds</span>()</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">yield</span> <span class="keyword">return</span> <span class="keyword">new</span> <span class="title">WaitForSeconds</span>(<span class="params"><span class="number">3f</span></span>)</span>;</span><br><span class="line">    <span class="comment">// 此时enemy的C++对象已销毁，但C#变量enemy本身不是真null</span></span><br><span class="line">    <span class="comment">// 下面这行会抛出 MissingReferenceException</span></span><br><span class="line">    enemy.transform.position = Vector3.zero; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><strong>关键点</strong>：当你看到 <code>MissingReferenceException</code>，我们的第一反应应该是：“哦，这个东西不是不存在，而是<strong>曾经存在过，但现在被销毁了</strong>”。</li></ul></li></ul><hr><h3 id="垃圾回收-Garbage-Collection-GC-的压力">垃圾回收 (Garbage Collection, GC) 的压力</h3><p>我们知道了 <code>Destroy()</code> 并不会立即清理内存。它只负责标记C++对象。那么C#包装器对象呢？它最终由C#的**垃圾回收器（GC）**来回收。</p><ul><li><p><strong><code>Instantiate()</code> 和 <code>Destroy()</code> 的代价</strong>：</p><ul><li><code>Instantiate()</code>：在内存中创建新的C#和C++对象，会产生内存分配。</li><li><code>Destroy()</code>：标记C++对象，并将C#对象留在内存中，等待GC处理。</li></ul></li><li><p><strong>问题所在</strong>：如果在游戏中频繁地创建和销毁对象（比如射击游戏里的子弹、特效），就会产生大量的“内存垃圾”（等待被回收的C#对象）。GC会在它认为合适的时候启动，暂停你的游戏主线程，去清理这些垃圾。这个暂停过程如果很长，就会导致游戏<strong>卡顿或掉帧</strong>。</p></li></ul><hr><h3 id="解决方案：对象池-Object-Pooling">解决方案：对象池 (Object Pooling)</h3><p>既然频繁创建和销毁的代价高，那么最好的办法就是<strong>避免</strong>它。对象池就是为此而生的设计模式。</p><ul><li><p><strong>核心思想</strong>：<strong>重用</strong>而不是销毁。</p><ol><li><strong>预加载</strong>：游戏开始时，一次性创建一批你需要的对象（比如100颗子弹），把它们全部设置为非激活状态(<code>SetActive(false)</code>)，并存放在一个列表里。</li><li><strong>“获取”</strong>：当需要一颗子弹时，你不是 <code>Instantiate</code> 一个新的，而是从池子里取出一个非激活的，把它移动到发射位置，然后设置为激活状态(<code>SetActive(true)</code>)。</li><li><strong>“归还”</strong>：当子弹击中目标或飞出屏幕时，你不是 <code>Destroy</code> 它，而是把它重新设置为非激活状态(<code>SetActive(false)</code>)，放回池子，等待下一次使用。</li></ol></li><li><p><strong>好处</strong>：整个游戏过程中，几乎没有新的内存分配和销毁操作，从而极大地减轻了GC的压力，让游戏运行得更平滑。这是从初级到中级开发者必须掌握的一个核心优化技巧。</p></li></ul><hr><h3 id="值类型-Structs-vs-引用类型-Classes">值类型 (Structs) vs 引用类型 (Classes)</h3><p>我们讨论的 <code>GameObject</code> 和 <code>Component</code> 都是<strong>类（Class）</strong>，它们是<strong>引用类型</strong>。这意味着变量存储的是指向内存中对象的“地址”。</p><p>但Unity中还有很多常用的<strong>结构体（Struct）</strong>，它们是<strong>值类型</strong>。比如：</p><ul><li><p><code>Vector3</code></p></li><li><p><code>Quaternion</code></p></li><li><p><code>Color</code></p></li><li><p><code>RaycastHit</code></p></li><li><p><strong>关键区别</strong>：值类型变量直接包含数据本身，而不是引用。它们没有C++/C#双重存在的概念，也不受 <code>Destroy()</code> 影响。</p><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">Vector3 position = <span class="keyword">new</span> Vector3(<span class="number">1</span>, <span class="number">1</span>, <span class="number">1</span>);</span><br><span class="line">Vector3 anotherPosition = position; <span class="comment">// 这里是完整的数据拷贝</span></span><br><span class="line"></span><br><span class="line">anotherPosition.x = <span class="number">5</span>;</span><br><span class="line"><span class="comment">// position.x 仍然是 1，因为anotherPosition是position的一个全新副本。</span></span><br></pre></td></tr></table></figure></li><li><p><strong>知识点</strong>：<code>Vector3</code> 这样的结构体，你永远不需要对它进行 <code>== null</code> 的判断，因为它根本不可能是 <code>null</code>。它就是一个值的集合，就像整数 <code>int</code> 一样。理解这一点有助于你写出更高效、更准确的代码。</p></li></ul><hr><h3 id="总结一下这些引申点之间的关系：">总结一下这些引申点之间的关系：</h3><ol><li>因为Unity对象有 <strong>C#/C++双重结构</strong>，所以 <code>Destroy()</code> 产生了“伪空”对象。</li><li>访问这种“伪空”对象会抛出 <strong><code>MissingReferenceException</code></strong>，以区别于C#原生的 <code>NullReferenceException</code>。</li><li>频繁的 <code>Instantiate</code>/<code>Destroy</code> 会给 <strong>GC</strong> 带来巨大压力，导致卡顿。</li><li>为了解决GC压力，我们采用 <strong>对象池</strong> 技术进行性能优化。</li><li>这个特殊的生命周期问题只存在于继承自 <code>UnityEngine.Object</code> 的<strong>引用类型</strong>，而像 <code>Vector3</code> 这样的<strong>值类型</strong>则完全不同。</li></ol>]]></content>
      
      
      <categories>
          
          <category> 技术分享 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Unity </tag>
            
            <tag> Fire </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>在 Unity 中构建基于 log4net 的生产级日志系统：从实现到疑难问题排查</title>
      <link href="/posts/878304ff/"/>
      <url>/posts/878304ff/</url>
      
        <content type="html"><![CDATA[<p>在游戏开发中，一个健壮、可配置、可持久化的日志系统是不可或令的。它不仅是开发阶段调试的利器，更是项目上线后追踪线上问题、分析用户行为的关键依据。Unity 自带的 <code>Debug.Log</code> 系统虽然便捷，但其功能单一，无法满足日志分级、持久化存储、灵活配置等生产环境的需求。</p><p>本文将详细介绍如何在 Unity 2023 中，利用强大的第三方日志框架 <code>log4net</code>，从零开始构建一个生产级别的日志系统。内容将覆盖系统的设计与实现、具体使用方法，并深入剖析一个在集成过程中可能遇到的、极具迷惑性的“日志不生效”问题，提供完整的排查思路与解决方案。</p><h2 id="系统设计目标"><strong>系统设计目标</strong></h2><p>我们旨在构建一个满足以下需求的日志系统：</p><ul><li><strong>日志持久化</strong>：将日志信息保存到本地文件，便于离线分析。</li><li><strong>日志分级</strong>：支持 Info, Warning, Error 等不同级别的日志，方便筛选关键信息。</li><li><strong>统一管理</strong>：能够捕获并统一处理 Unity 引擎自身（如 <code>Debug.Log</code>）产生的日志。</li><li><strong>高性能</strong>：在 Release 版本中，可以轻松剥离不必要的日志输出，减少性能开销。</li><li><strong>易于使用</strong>：通过静态类提供简洁的 API，在项目任何位置都能方便地调用。</li></ul><h2 id="核心组件：log4net"><strong>核心组件：log4net</strong></h2><p><code>log4net</code> 是 Apache 基金会旗下的一个成熟、功能强大的日志框架。它的核心优势在于其高度的可配置性，通过一个简单的 XML 文件，就可以定义日志的输出格式、目标（文件、控制台、数据库等）以及过滤规则。</p><h2 id="系统实现步骤"><strong>系统实现步骤</strong></h2><h3 id="步骤-3-1：集成-log4net"><strong>步骤 3.1：集成 log4net</strong></h3><ol><li><strong>获取 DLL</strong>：从 <code>log4net</code> 官方网站下载二进制包，解压后找到适用于 .NET Standard 2.x 或者 .NET 4.6.2 的 <code>log4net.dll</code> 文件。</li><li><strong>导入 Unity</strong>：在 Unity 项目的 <code>Assets</code> 目录下创建 <code>Plugins</code> 文件夹，并将 <code>log4net.dll</code> 拖入其中。</li></ol><p><strong><a href="/downloads/apache-log4net-binaries-3.1.0.zip">点击下载 log4net </a></strong></p><h3 id="步骤-3-2：创建-log4net-配置文件"><strong>步骤 3.2：创建 log4net 配置文件</strong></h3><p><code>log4net</code> 的行为由配置文件驱动。在 <code>Assets/StreamingAssets/</code> 目录下创建一个名为 <code>log4net.config</code> 的文件。<code>StreamingAssets</code> 目录能确保该文件在打包后被原样保留。</p><p>一个推荐的基础配置如下：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;utf-8&quot;</span> ?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">log4net</span>&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- 定义一个滚动文件输出器 (Appender) --&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">appender</span> <span class="attr">name</span>=<span class="string">&quot;RollingFileAppender&quot;</span> <span class="attr">type</span>=<span class="string">&quot;log4net.Appender.RollingFileAppender&quot;</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- </span></span><br><span class="line"><span class="comment">      文件路径使用 PatternString，允许动态替换 %property&#123;LogPath&#125; 变量。</span></span><br><span class="line"><span class="comment">      这个变量将在代码中被设置为 Application.persistentDataPath。</span></span><br><span class="line"><span class="comment">    --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">file</span> <span class="attr">type</span>=<span class="string">&quot;log4net.Util.PatternString&quot;</span> <span class="attr">value</span>=<span class="string">&quot;%property&#123;LogPath&#125;/gamelog.log&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">appendToFile</span> <span class="attr">value</span>=<span class="string">&quot;true&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">rollingStyle</span> <span class="attr">value</span>=<span class="string">&quot;Size&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">maxSizeRollBackups</span> <span class="attr">value</span>=<span class="string">&quot;5&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">maximumFileSize</span> <span class="attr">value</span>=<span class="string">&quot;5MB&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">staticLogFileName</span> <span class="attr">value</span>=<span class="string">&quot;true&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">layout</span> <span class="attr">type</span>=<span class="string">&quot;log4net.Layout.PatternLayout&quot;</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">conversionPattern</span> <span class="attr">value</span>=<span class="string">&quot;%date [%thread] %-5level %logger - %message%newline&quot;</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">layout</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">appender</span>&gt;</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">&lt;!-- 根 Logger 配置 --&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">root</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 设置日志记录的最低级别为 DEBUG --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">level</span> <span class="attr">value</span>=<span class="string">&quot;DEBUG&quot;</span> /&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 将上面定义的 RollingFileAppender 附加到根 Logger --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">appender-ref</span> <span class="attr">ref</span>=<span class="string">&quot;RollingFileAppender&quot;</span> /&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">root</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">log4net</span>&gt;</span></span><br></pre></td></tr></table></figure><p><strong>注意</strong>：创建此文件时，务必确保其文本编码为 <strong>UTF-8 (无BOM)</strong>，否则可能导致 <code>log4net</code> 解析失败且不抛出任何错误。</p><h3 id="步骤-3-3：编写日志-API-封装类-GameLogger-cs"><strong>步骤 3.3：编写日志 API 封装类 (<code>GameLogger.cs</code>)</strong></h3><p>为了避免与 <code>UnityEngine.Log</code> 产生命名冲突，并提供一个简洁的接口，我们创建一个静态工具类。为了代码的健壮性和可维护性，将其置于自定义的命名空间下。</p><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// GameLogger.cs</span></span><br><span class="line"><span class="keyword">using</span> log4net;</span><br><span class="line"><span class="keyword">using</span> UnityEngine;</span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> <span class="title">YourGame.Core.Logging</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title">GameLogger</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">static</span> ILog log;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">Init</span>(<span class="params"><span class="built_in">string</span> loggerName</span>)</span></span><br><span class="line">        &#123;</span><br><span class="line">            log = LogManager.GetLogger(loggerName);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 在编辑器模式下，将日志消息转发到 Unity 控制台</span></span><br><span class="line">        [<span class="meta">System.Diagnostics.Conditional(<span class="string">&quot;UNITY_EDITOR&quot;</span>)</span>]</span><br><span class="line">        <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">ForwardToUnityConsole</span>(<span class="params"><span class="built_in">object</span> message, LogType type</span>)</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">switch</span> (type)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">case</span> LogType.Log: Debug.Log(message); <span class="keyword">break</span>;</span><br><span class="line">                <span class="keyword">case</span> LogType.Warning: Debug.LogWarning(message); <span class="keyword">break</span>;</span><br><span class="line">                <span class="keyword">case</span> LogType.Error: Debug.LogError(message); <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">Info</span>(<span class="params"><span class="built_in">object</span> message, <span class="built_in">bool</span> forwardToConsole = <span class="literal">true</span></span>)</span></span><br><span class="line">        &#123;</span><br><span class="line">            log?.Info(message);</span><br><span class="line">            <span class="keyword">if</span> (forwardToConsole) ForwardToUnityConsole(message, LogType.Log);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">Warn</span>(<span class="params"><span class="built_in">object</span> message, <span class="built_in">bool</span> forwardToConsole = <span class="literal">true</span></span>)</span></span><br><span class="line">        &#123;</span><br><span class="line">            log?.Warn(message);</span><br><span class="line">            <span class="keyword">if</span> (forwardToConsole) ForwardToUnityConsole(message, LogType.Warning);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">Error</span>(<span class="params"><span class="built_in">object</span> message, <span class="built_in">bool</span> forwardToConsole = <span class="literal">true</span></span>)</span></span><br><span class="line">        &#123;</span><br><span class="line">            log?.Error(message);</span><br><span class="line">            <span class="keyword">if</span> (forwardToConsole) ForwardToUnityConsole(message, LogType.Error);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>此设计通过一个可选参数 <code>forwardToConsole</code> 和条件编译特性 <code>[Conditional(&quot;UNITY_EDITOR&quot;)]</code>，实现了在编辑器模式下将日志同时输出到控制台和文件，而在发布版本中仅写入文件，兼顾了开发便利性与发布性能。</p><h3 id="步骤-3-4：创建-Unity-日志桥接器-UnityLogBridge-cs"><strong>步骤 3.4：创建 Unity 日志桥接器 (<code>UnityLogBridge.cs</code>)</strong></h3><p>为了捕获 <code>Debug.Log</code> 等原生日志，需要监听 <code>Application.logMessageReceivedThreaded</code> 事件。</p><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// UnityLogBridge.cs</span></span><br><span class="line"><span class="keyword">using</span> UnityEngine;</span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> <span class="title">YourGame.Core.Logging</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">class</span> <span class="title">UnityLogBridge</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">Init</span>()</span></span><br><span class="line">        &#123;</span><br><span class="line">            Application.logMessageReceivedThreaded += OnLogMessageReceived;</span><br><span class="line">            GameLogger.Info(<span class="string">&quot;UnityLogBridge Inited. Start listening to Unity&#x27;s log messages.&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">OnLogMessageReceived</span>(<span class="params"><span class="built_in">string</span> condition, <span class="built_in">string</span> stackTrace, LogType type</span>)</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">// 拼接消息和堆栈信息</span></span><br><span class="line">            <span class="built_in">string</span> message = <span class="string">$&quot;<span class="subst">&#123;condition&#125;</span>\n<span class="subst">&#123;stackTrace&#125;</span>&quot;</span>;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 调用 GameLogger 时，将 forwardToConsole 设为 false，避免无限循环</span></span><br><span class="line">            <span class="keyword">switch</span> (type)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">case</span> LogType.Error:</span><br><span class="line">                <span class="keyword">case</span> LogType.Exception:</span><br><span class="line">                    GameLogger.Error(message, forwardToConsole: <span class="literal">false</span>);</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="keyword">case</span> LogType.Warning:</span><br><span class="line">                    GameLogger.Warn(message, forwardToConsole: <span class="literal">false</span>);</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="literal">default</span>:</span><br><span class="line">                    GameLogger.Info(message, forwardToConsole: <span class="literal">false</span>);</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>此设计的关键在于，从桥接器转发日志时，必须将 <code>forwardToConsole</code> 参数设为 <code>false</code>，以切断 <code>GameLogger</code> -&gt; <code>Debug.Log</code> -&gt; <code>UnityLogBridge</code> -&gt; <code>GameLogger</code> 的无限递归调用链。</p><h3 id="步骤-3-5：初始化系统"><strong>步骤 3.5：初始化系统</strong></h3><p>在游戏启动时，如在一个 <code>GameManager</code> 脚本的 <code>Awake</code> 方法中，执行初始化序列。</p><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// GameManager.cs</span></span><br><span class="line"><span class="keyword">using</span> UnityEngine;</span><br><span class="line"><span class="keyword">using</span> System.IO;</span><br><span class="line"><span class="keyword">using</span> YourGame.Core.Logging; <span class="comment">// 引入自定义的命名空间</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">GameManager</span> : <span class="title">MonoBehaviour</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">Awake</span>()</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 1. 设置 log4net 的动态属性，指定日志文件保存路径</span></span><br><span class="line">        log4net.GlobalContext.Properties[<span class="string">&quot;LogPath&quot;</span>] = Application.persistentDataPath;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 加载 log4net 配置文件</span></span><br><span class="line">        <span class="built_in">string</span> configFilePath = Path.Combine(Application.streamingAssetsPath, <span class="string">&quot;log4net.config&quot;</span>);</span><br><span class="line">        log4net.Config.XmlConfigurator.Configure(<span class="keyword">new</span> FileInfo(configFilePath));</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 初始化日志 API 类</span></span><br><span class="line">        GameLogger.Init(<span class="string">&quot;Unity&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 4. 初始化 Unity 日志桥接器</span></span><br><span class="line">        UnityLogBridge.Init();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// --- 系统初始化完成，可以开始使用 ---</span></span><br><span class="line">        GameLogger.Info(<span class="string">&quot;Log system initialized successfully!&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="使用方法"><strong>使用方法</strong></h2><p>在项目中的任何脚本里，只需引入命名空间，即可调用：</p><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">using</span> YourGame.Core.Logging;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">MyFeature</span> : <span class="title">MonoBehaviour</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">Start</span>()</span></span><br><span class="line">    &#123;</span><br><span class="line">        GameLogger.Info(<span class="string">&quot;MyFeature has started.&quot;</span>);</span><br><span class="line">        GameLogger.Warn(<span class="string">&quot;Player health is low.&quot;</span>);</span><br><span class="line">      </span><br><span class="line">        <span class="keyword">try</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">// ... some code that might fail ...</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">catch</span> (System.Exception ex)</span><br><span class="line">        &#123;</span><br><span class="line">            GameLogger.Error(<span class="string">$&quot;An exception occurred: <span class="subst">&#123;ex.Message&#125;</span>&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="疑难问题排查：日志系统“静默失败”"><strong>疑难问题排查：日志系统“静默失败”</strong></h2><p>在集成过程中，可能会遇到一个非常棘手的问题：调用 <code>GameLogger.Info()</code> 后，<strong>既没有在控制台看到输出，也没有在目标路径生成日志文件</strong>，但程序不抛出任何异常。</p><h3 id="问题现象分析"><strong>问题现象分析</strong></h3><ul><li><strong>自定义日志无响应</strong>：<code>GameLogger.Info()</code> 等调用无效。</li><li><strong>原生日志正常</strong>：<code>Debug.Log()</code> 正常在控制台输出。</li><li><strong>调试器行为迷惑</strong>：使用调试器“步入”<code>GameLogger.Info()</code> 方法，可以正常进入，但方法内的 <code>log.Info()</code> 调用似乎被跳过。</li><li><strong>IDE 警告</strong>：IDE (如 Rider) 可能会在调用点提示 <code>'GameLogger' is a type, which is not valid in the given context.</code>，即使已经改名避免了与 <code>UnityEngine.Log</code> 的直接冲突。</li></ul><h3 id="根本原因定位"><strong>根本原因定位</strong></h3><p>经过层层排查，最终定位到问题根源：<strong><code>log4net</code> 的 XML 配置文件未能被正确解析，导致 Appender (输出器) 未能被创建并附加到 Logger 对象上。</strong></p><p><code>log4net</code> 在解析配置文件时，如果遇到无法识别的文件编码（如带BOM的UTF-8）或文件开头的隐藏字符，它会选择<strong>静默失败 (Silently Fail)</strong>。这意味着它不会抛出异常，也不会给出任何警告，只是简单地停止解析。</p><p>这就解释了所有现象：</p><ul><li><code>log4net.Config.XmlConfigurator.Configure()</code> 方法执行了，但内部什么都没做。</li><li><code>LogManager.GetLogger()</code> 返回了一个有效的 Logger 对象，但这个对象没有任何 Appender 关联。</li><li>调用 <code>log.Info()</code> 时，因为没有 Appender，所以日志消息无处可去，被直接丢弃。</li></ul><h3 id="解决方案与诊断流程"><strong>解决方案与诊断流程</strong></h3><p>解决此问题的关键是确保 <code>log4net.config</code> 文件是“纯净”的。</p><ol><li><p><strong>确认文件编码</strong>：删除现有配置文件，使用专业的文本编辑器（如 VS Code, Notepad++）重新创建，并在保存时<strong>明确选择 <code>UTF-8 (无BOM)</code> 编码</strong>。</p></li><li><p><strong>复制标准内容</strong>：直接从可靠来源（如本文档）复制配置内容，避免从网页等处复制时引入不可见字符。</p></li><li><p><strong>开启内部调试</strong>：在 <code>log4net.config</code> 的根节点 <code>&lt;log4net&gt;</code> 中添加 <code>debug=&quot;true&quot;</code> 属性。如果 <code>log4net</code> 能够开始解析，它会将其内部的初始化步骤、警告和错误输出到Unity控制台，为诊断提供关键线索。</p></li><li><p><strong>代码级诊断</strong>：如果问题依旧，可以通过代码直接检查 Logger 的状态，确认 Appender 是否被成功附加。</p><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 在 GameLogger.Init() 中添加诊断代码</span></span><br><span class="line"><span class="keyword">var</span> logger = log.Logger <span class="keyword">as</span> log4net.Repository.Hierarchy.Logger;</span><br><span class="line"><span class="keyword">if</span> (logger != <span class="literal">null</span>)</span><br><span class="line">&#123;</span><br><span class="line">    Debug.Log(<span class="string">$&quot;Logger Appender count: <span class="subst">&#123;logger.Appenders.Count&#125;</span>&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (logger.Appenders.Count == <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        Debug.LogError(<span class="string">&quot;No Appenders attached! Check log4net.config file integrity and encoding.&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当看到 <code>Appender count: 0</code> 的输出时，即可确信问题出在配置文件的解析上。</p></li></ol><h3 id="总结"><strong>总结</strong></h3><p>通过结合 <code>log4net</code> 和自定义的封装层，我们可以在 Unity 中构建一个功能完善、易于扩展的日志系统。它不仅解决了 Unity 原生日志系统的局限性，还通过精巧的设计兼顾了开发效率和发布性能。理解并掌握像“静默失败”这类疑难问题的排查方法，是提升项目稳定性和开发能力的重要一环。一个健壮的日志系统，是保障大型、长期项目成功的基石。</p><h3 id="由这个问题引发的思考在这里面的第三条-swig￼12">由这个问题引发的思考在这里面的第三条<a href="/posts/8596feee/" title="各类杂碎知识点">零碎知识点</a></h3>]]></content>
      
      
      <categories>
          
          <category> 技术分享 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Unity </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>各类杂碎知识点</title>
      <link href="/posts/8596feee/"/>
      <url>/posts/8596feee/</url>
      
        <content type="html"><![CDATA[<h2 id="Unity高版本TMP中文字体问题"><em>Unity高版本TMP中文字体问题</em></h2><ul><li>解决方案：选择中文字体库大的中文字体粘贴进项目中，然后右键 -&gt; Create -&gt; TextMeshPro -&gt; Font Asset -&gt; SDF，完成此过程就会创建新字体，然后尝试该字体是否能正常显示，不能正常显示则尝试调整新字体文件的<strong>Atlas Width/Atlas Height</strong>，一直调整到最大都不行的话就换字体。</li><li>目前已经尝试成功的字体<ul><li>阿里巴巴普惠体</li><li>思源黑体</li></ul></li></ul><hr><h2 id="可序列化问题"><strong>可序列化问题</strong></h2><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">using</span> UnityEngine;</span><br><span class="line"></span><br><span class="line">[<span class="meta">CreateAssetMenu(fileName = <span class="string">&quot;NewStoryNode&quot;</span>, menuName = <span class="string">&quot;Game Data/Story Node&quot;</span>)</span>]</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">StoryNode</span> : <span class="title">ScriptableObject</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="built_in">int</span> ID;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">public</span> Choice[] choices;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>查看以上代码，分析一下为什么Int能在Inspector面板正确显示，但Choice[]却不能正确显示</p><p>这里需要引出一个知识点————Unity 的 Inspector 只能显示那些被标记为 <code>[Serializable]</code> 的<strong>类</strong>或<strong>结构体</strong>的字段。</p><p>也就是说 <code>choices</code> 的不显示很可能是没给 <code>Choice</code> 类加标签导致的</p><p>解决方案也很简单，给 <code>Choice</code> 类加标签就行，就像下面这样</p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">[<span class="meta">System.Serializable</span>]</span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">Choice</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="built_in">string</span> choiceText;</span><br><span class="line">    <span class="keyword">public</span> StoryNode nextStoryNode;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这里要注意一点，Unity 的 <code>Inspector</code> 不支持直接序列化引用类型的数组（如 <code>MonoBehaviour[]</code> 或 <code>ScriptableObject[]</code>）</p><p>这意味着 <code>Choice</code> 必须是一个普通的 C# 类</p><hr><h2 id="文件编码问题可能导致的、其他非常隐蔽的异常和bug">文件编码问题可能导致的、其他非常隐蔽的异常和bug</h2><h3 id="1-脚本编译与解析错误"><strong>1. 脚本编译与解析错误</strong></h3><p><strong>场景：C# 脚本文件被意外保存为带BOM的UTF-8</strong></p><ul><li><p><strong>隐蔽异常</strong>：</p><ul><li><strong>编译器错误指向第一行</strong>：Unity 或 C# 编译器可能会报告一个莫名其妙的语法错误，位置就在文件的第一行第一个字符。错误信息可能是“意外的字符”、“需要命名空间、类型或委托声明”等。</li><li><strong><code>#if</code> 等预处理指令失效</strong>：如果BOM恰好出现在 <code>#if UNITY_EDITOR</code> 这样的预处理指令之前，可能会导致编译器无法正确识别该指令，使得本应在特定平台编译的代码被错误地包含或排除。</li><li><strong>Assembly Definition (.asmdef) 文件解析失败</strong>：如果 <code>.asmdef</code> 这个JSON格式的文件被保存为带BOM的UTF-8，Unity的程序集解析器可能会直接忽略这个文件或报错，导致整个程序集的代码无法被正确编译和引用，引发大量的“找不到类型或命名空间”的错误。</li></ul></li><li><p><strong>为什么隐蔽</strong>：</p><ul><li>BOM是不可见字符，你在代码编辑器里看第一行是完全正常的 <code>using UnityEngine;</code>。</li><li>错误信息通常不会直接说“文件编码错误”或“发现BOM”，而是会给出一些看似是语法问题的误导性提示。开发者很容易陷入检查语法、括号匹配等无用功中。</li></ul></li></ul><h3 id="2-配置文件与数据文件解析失败"><strong>2. 配置文件与数据文件解析失败</strong></h3><p><strong>场景：JSON, XML, CSV, INI 等数据文件</strong></p><ul><li><p><strong>隐蔽异常</strong>：</p><ul><li><strong>JSON 解析失败</strong>：一个严格的JSON解析器（比如很多网络库中使用的）在遇到BOM时会直接抛出“无效的JSON令牌”或类似的解析错误，因为它期望文件的第一个字符是 <code>{</code> 或 <code>[</code>。</li><li><strong>XML 解析失败</strong>：和 <code>log4net</code> 的情况类似，XML解析器期望文件以 <code>&lt;?xml ... ?&gt;</code> 或 <code>&lt;</code> 开头。BOM的存在会让它认为文件格式不正确。</li><li><strong>CSV 数据错位</strong>：如果一个CSV文件的第一行是表头，BOM可能会被当作第一个表头字段的一部分，导致 <code>Header1</code> 变成 <code>\uFEFFHeader1</code>。当你按表头名字去取数据列时，就会失败。更糟糕的是，有些解析器可能会把BOM当作一个空字段，导致所有列向后错位一格。</li></ul></li><li><p><strong>为什么隐蔽</strong>：</p><ul><li>和脚本文件一样，肉眼看不出问题。</li><li>失败的方式多种多样，可能是直接崩溃，也可能是返回 <code>null</code> 或空数据，让你误以为是文件内容本身是空的。</li></ul></li></ul><h3 id="3-网络通信与HTTP请求问题"><strong>3. 网络通信与HTTP请求问题</strong></h3><p><strong>场景：HTTP请求的Body或Header</strong></p><ul><li><p><strong>隐蔽异常</strong>：</p><ul><li><strong>服务器端认证失败 (401/403)</strong>：假设你需要计算请求体(Request Body)的HMAC签名并放在请求头(Header)里。如果你从一个带BOM的文件中读取内容作为请求体，计算出的签名就会是错误的，因为BOM也参与了计算。服务器端用不带BOM的原始数据计算签名，两者永远对不上，导致请求被拒绝。</li><li><strong>服务器端数据解析失败 (400 Bad Request)</strong>：你发送一个POST请求，请求体是JSON格式。如果你从一个带BOM的文本文件读取内容直接发送，服务器端的JSON解析器会因为BOM的存在而解析失败，返回400错误。</li><li><strong>文件上传后损坏</strong>：上传一个文本文件（比如Shader、配置文件），如果服务器端的处理逻辑对BOM敏感，这个文件在服务器上可能就无法被其他程序正确使用。</li></ul></li><li><p><strong>为什么隐蔽</strong>：</p><ul><li>你本地看到的数据是好的，发送前检查也是好的，但服务器就是不认。</li><li>错误发生在你看不到的服务器端，你只能得到一个笼统的HTTP错误码，很难直接联想到是本地文件编码的问题。</li></ul></li></ul><h3 id="4-跨平台与脚本执行问题"><strong>4. 跨平台与脚本执行问题</strong></h3><p><strong>场景：Shell脚本、Python脚本等</strong></p><ul><li><p><strong>隐蔽异常</strong>：</p><ul><li><strong>Shebang (<code>#!</code>) 失效</strong>：在Linux或macOS上，脚本文件的第一行通常是 <code>#!/bin/bash</code> 或 <code>#!/usr/bin/env python</code>，用来告诉系统用哪个解释器执行此脚本。如果文件被保存为带BOM的UTF-8，BOM会出现在 <code>#!</code> 之前。系统在读取时，无法识别这个shebang，可能会导致脚本无法执行，或者报“找不到命令”的错误。</li><li><strong>跨平台协作问题</strong>：一个在Windows上用记事本（旧版默认带BOM）编辑过的脚本，拷贝到Linux服务器上就无法运行，这在DevOps和自动化流程中是常见的“坑”。</li></ul></li><li><p><strong>为什么隐蔽</strong>：</p><ul><li>在Windows上双击可能没问题（因为它不依赖shebang），但在需要它的系统上就完全失效。</li><li>对于不熟悉Linux的开发者来说，这种问题几乎无法凭直觉解决。</li></ul></li></ul><h3 id="总结与防范"><strong>总结与防范</strong></h3><p>文件编码问题，尤其是UTF-8 BOM问题，其隐蔽性在于：</p><ol><li><strong>不可见性</strong>：问题根源是肉眼无法看到的字符。</li><li><strong>误导性错误</strong>：产生的错误信息往往指向表层现象（如语法错误、解析失败），而非根本原因（编码错误）。</li><li><strong>环境依赖性</strong>：同一个文件在某些软件或平台下工作正常，在另一些环境下就失败，增加了定位难度。</li></ol><p><strong>如何防范：</strong></p><ul><li><strong>统一团队开发环境</strong>：为团队配置统一的IDE（如VS Code, Rider, Visual Studio），并配置其默认文件保存格式为 <strong>UTF-8 (without BOM)</strong>。这是最一劳永逸的方法。</li><li><strong>使用版本控制的<code> .gitattributes</code></strong>：在Git项目中，可以添加一个 <code>.gitattributes</code> 文件，强制指定某些文件类型的编码和行尾符，例如：<figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"># 强制所有文本文件在检出时为LF行尾</span><br><span class="line">* text=auto eol=lf</span><br><span class="line"># 强制特定文件类型为UTF-8无BOM</span><br><span class="line">*.cs text eol=lf</span><br><span class="line">*.json text eol=lf</span><br><span class="line">*.xml text eol=lf</span><br></pre></td></tr></table></figure></li><li><strong>养成使用专业编辑器的习惯</strong>：避免使用Windows记事本等可能产生编码问题的简单编辑器来处理项目文件。专业编辑器通常会在状态栏显示当前文件的编码格式，便于检查。</li><li><strong>遇到“灵异事件”时，优先检查编码</strong>：当你遇到看似无法解释的解析错误、编译错误或跨平台问题时，把“检查文件编码”作为你的一个标准排查步骤。</li></ul><hr>]]></content>
      
      
      <categories>
          
          <category> Unity基础&amp;复习 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Unity </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Scroll View复习</title>
      <link href="/posts/efdc60b2/"/>
      <url>/posts/efdc60b2/</url>
      
        <content type="html"><![CDATA[<h2 id="什么是Scroll-View">什么是Scroll View</h2><p>Scroll View简单来说就是Unity自带的一个可滚动区域的UI组件，即当内容超出屏幕显示范围是，其可以让用户自行滑动。</p><hr><h2 id="Scroll-View的基本构成">Scroll View的基本构成</h2><p>完整的Scroll View主要由以下部分组成</p><ul><li><strong>Scroll View</strong>(Rect Transform): 此为整个滚动区域的<strong>容器</strong>，定义了用户可以看到内容的矩形区域。超出这个区域的内容会被裁剪掉（默认情况下）。</li><li><strong>Viewport</strong>(Rect Transform): 这是<strong>很重要</strong>的一个子对象，其为 Scroll View 的<strong>直接子级</strong>，并且与 Scroll View 的大小相同或略小。 Viewport 的作用是<strong>裁剪内容</strong>。只有位于 Viewport 内部的内容才会被显示出来。</li><li><strong>Content(Rect Transform):</strong> 这是所有可滚动内容的“<strong>父容器</strong>”。所有想要滚动显示的内容都应该作为 Content 的<strong>子对象</strong>。Content 的大小会根据其内部的内容的多少而动态变化，也可以手动设置。当Content的大小超出Viewport的大小的时候，滚动功能就会生效。</li><li><strong>Scrollbar(Optional):</strong> 滚动条是可选的，但它们提供了视觉指示和另一种滚动方式。<ul><li>Horizontal Scrollbar: 用于水平滚动</li><li>Vertical Scrollbar: 用于垂直滚动</li></ul></li></ul><p><img src="https://image.nfasystem.top/img/ScrollView%E7%BB%93%E6%9E%84.png" alt="alt text"></p><hr><h2 id="Scroll-View组件详解-Inspector面板">Scroll View组件详解(Inspector面板)</h2><p><img src="https://image.nfasystem.top/img/ScrollView%E9%9D%A2%E6%9D%BF.png" alt="alt text"></p><p><strong>Scroll Rect组件</strong>————这是Scroll View的核心逻辑组件。</p><blockquote><p><strong>这里只讲重要部分</strong></p></blockquote><ul><li>Horizontal:<ul><li>勾选此项允许水平滑动</li></ul></li><li>Vertical:<ul><li>勾选此项允许竖直滑动</li></ul></li><li>Movement Type: 决定了滚动到达边界时的行为<ul><li>Unrestricted: 内容可以无限滚动，即使超出边界也不会弹回。</li><li>Elastic: (最常用) 内容可以稍微超出边界，然后像橡皮筋一样弹回。可以设置 Elasticity (弹性系数)。</li><li>Clamped: 内容到达边界后立即停止，不能超出。</li></ul></li></ul><p><img src="https://image.nfasystem.top/img/ScrollView_Movement_Type.png" alt="alt text"></p><ul><li><strong>Elasticity:</strong> (仅当 Movement Type 为 Elastic 时可用) 弹性系数，值越大，弹回的速度越快。</li><li>Inertia:<ul><li>Inertia: 勾选此项后，当用户松开鼠标/手指时，内容会根据之前的拖动速度继续滚动一段距离，然后逐渐减速停止。</li><li>Deceleration Rate: (仅当 Inertia 勾选时可用) 减速速率，值越小，滚动停止得越慢。</li></ul></li><li>Scroll Sensitivity: 滚动灵敏度。值越大，鼠标滚轮或拖动一小段距离就能滚动更多内容。</li><li><strong>Horizontal Scrollbar(Vertical Scrollbar原理相同):</strong><ul><li>Visibility:<ul><li>Permanent: 滚动条始终可见。</li><li>Auto Hide: 只有在内容超出 Viewport 且正在滚动时才显示。</li><li>Auto Hide And Expand Viewport: 只有在内容超出 Viewport 且正在滚动时才显示，并且当滚动条隐藏时，Viewport 会自动扩展以填充滚动条留下的空间。</li></ul></li></ul></li></ul><p><img src="https://image.nfasystem.top/img/ScrollView_Visibility.png" alt="alt text"></p><ul><li><strong>On Value Changed (Scroll Rect):</strong> 这是一个事件回调。当滚动位置发生变化时，会触发这个事件。可以通过脚本监听这个事件，例如，根据滚动位置加载更多内容（无限滚动）。</li></ul><hr><h2 id="Viewport-和-Content-的-Rect-Transform-设置">Viewport 和 Content 的 Rect Transform 设置</h2><blockquote><p><strong>这是 Scroll View 正常工作的关键</strong></p></blockquote><ul><li>Viewport:<ul><li>通常，Viewport 的 Rect Transform 会被设置为与 Scroll View 的大小相同，并且锚点和位置都设置为 (0,0)。</li><li><strong>重要：</strong> Viewport 上通常会有一个 Image 组件（用于背景）和一个 Mask 组件。Mask 组件是实现内容裁剪的关键！它会确保只有 Viewport 区域内的内容才可见。</li></ul></li><li><strong>Content:</strong><ul><li><strong>锚点 (Anchors):</strong> Content 的锚点设置非常重要，它决定了内容如何对齐和扩展。<ul><li>垂直滚动： 如果你主要进行垂直滚动，Content 的锚点通常设置为 Top-Left 或 Top-Center，并且宽度设置为与 Viewport 相同，高度根据内容动态调整。</li><li>水平滚动： 如果你主要进行水平滚动，Content 的锚点通常设置为 Top-Left 或 Center-Left，并且高度设置为与 Viewport 相同，宽度根据内容动态调整。</li><li><strong>重要：</strong> 如果内容是动态生成的，可能需要使用 Vertical Layout Group 或 Horizontal Layout Group 配合 Content Size Fitter 组件来自动调整 Content 的大小。</li></ul></li></ul></li></ul><hr><h2 id="常用辅助组件-配合-Content-使用">常用辅助组件(配合 Content 使用)</h2><ul><li><strong>Vertical Layout Group / Horizontal Layout Group:</strong><ul><li>如果想让 Content 中的子元素（如多个按钮、文本行）自动按垂直或水平方向排列，并且自动调整间距，就需要用到这些组件。将对应组件添加到 Content 对象上。</li></ul></li><li><strong>Content Size Fitter:</strong><ul><li>这个组件非常有用，当 Content 对象上添加了 Layout Group 后，Content Size Fitter 可以根据 Layout Group 中子元素的总大小，自动调整 Content 的宽度或高度。</li><li><strong>Fit:</strong><ul><li>Unconstrained: 不受限制。</li><li>Min Size: 最小尺寸。</li><li>Preferred Size: 优先尺寸（根据子元素总大小）。</li></ul></li><li>通常会将 Horizontal Fit 或 Vertical Fit 设置为 Preferred Size，以确保 Content 的大小能够容纳所有子元素。</li></ul></li></ul><hr><h2 id="常见问题与调试技巧">常见问题与调试技巧</h2><ul><li><strong>内容不滚动</strong><ul><li><strong>检查 Content 的大小：</strong> 确保 Content 的宽度/高度确实大于 Viewport 的宽度/高度。如果 Content 太小，自然不需要滚动。</li><li><strong>检查 Content 的锚点和轴心：</strong> 确保它们设置正确，以便 Content 能够正确扩展。</li><li><strong>检查 Scroll Rect 的 Horizontal/Vertical 勾选：</strong> 确保允许了正确的滚动方向。</li><li><strong>检查 Viewport 上的 Mask 组件：</strong> 确保它存在且启用。</li></ul></li><li><strong>滚动条不显示</strong><ul><li><strong>检查 Scrollbar 是否拖拽到 Scroll Rect 的对应字段：</strong> 确保 Horizontal Scrollbar 和 Vertical Scrollbar 字段都关联了正确的滚动条对象</li><li><strong>检查 Scrollbar 的 Visibility 设置：</strong> 如果是 Auto Hide，只有在内容超出且正在滚动时才显示。</li><li><strong>检查 Content 的大小：</strong> 如果 Content 没有超出 Viewport，滚动条是不会显示的。</li></ul></li><li><strong>内容排列混乱</strong><ul><li><strong>使用 Layout Group：</strong> 确保 Content 对象上添加了 Vertical Layout Group 或 Horizontal Layout Group 来自动排列子元素。</li><li><strong>使用 Content Size Fitter：</strong> 确保 Content 对象上的 Content Size Fitter 设置正确，以便 Content 能够根据子元素自动调整大小。</li></ul></li></ul><hr><h2 id="编程控制-Scroll-View">编程控制 Scroll View</h2><blockquote><p>可以通过脚本来控制 Scroll View 的行为</p></blockquote><ul><li><code>scrollRect.verticalNormalizedPosition</code> / <code>scrollRect.horizontalNormalizedPosition</code>: 获取或设置当前的滚动位置。这是一个0到1之间的值，0表示最底部/最左边，1表示最顶部/最右边。<ul><li>例如，将 <code>verticalNormalizedPosition</code> 设置为0可以滚动到最底部（常用于聊天记录）。</li></ul></li><li><code>scrollRect.velocity</code>: 获取或设置当前的滚动速度。</li><li><code>scrollRect.StopMovement()</code>: 立即停止滚动。</li><li>监听 <code>On Value Changed</code> 事件:</li></ul><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">using</span> UnityEngine;</span><br><span class="line"><span class="keyword">using</span> UnityEngine.UI;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">MyScrollViewHandler</span> : <span class="title">MonoBehaviour</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">public</span> ScrollRect myScrollRect;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">Start</span>()</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (myScrollRect != <span class="literal">null</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            myScrollRect.onValueChanged.AddListener(OnScroll);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">OnScroll</span>(<span class="params">Vector2 scrollPosition</span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// scrollPosition.x 是水平滚动位置 (0-1)</span></span><br><span class="line">        <span class="comment">// scrollPosition.y 是垂直滚动位置 (0-1)</span></span><br><span class="line">        Debug.Log(<span class="string">&quot;Scroll Position: &quot;</span> + scrollPosition);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 示例：当滚动到顶部时做一些事情</span></span><br><span class="line">        <span class="keyword">if</span> (scrollPosition.y &gt;= <span class="number">0.99f</span>) <span class="comment">// 接近顶部</span></span><br><span class="line">        &#123;</span><br><span class="line">            Debug.Log(<span class="string">&quot;Reached near top!&quot;</span>);</span><br><span class="line">            <span class="comment">// 可以在这里加载更多历史数据</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 示例：当滚动到底部时做一些事情</span></span><br><span class="line">        <span class="keyword">if</span> (scrollPosition.y &lt;= <span class="number">0.01f</span>) <span class="comment">// 接近底部</span></span><br><span class="line">        &#123;</span><br><span class="line">            Debug.Log(<span class="string">&quot;Reached near bottom!&quot;</span>);</span><br><span class="line">            <span class="comment">// 可以在这里加载更多新数据</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 示例：通过代码滚动到最底部</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">ScrollToBottom</span>()</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (myScrollRect != <span class="literal">null</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            myScrollRect.verticalNormalizedPosition = <span class="number">0f</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 示例：通过代码滚动到最顶部</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">ScrollToTop</span>()</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (myScrollRect != <span class="literal">null</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            myScrollRect.verticalNormalizedPosition = <span class="number">1f</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="后期目标">后期目标</h2><ul><li>[ ] 通过代码简单仿制一个Scroll View</li></ul><p><strong>第一次编辑-2025年7月8日</strong></p>]]></content>
      
      
      <categories>
          
          <category> Unity基础&amp;复习 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Unity </tag>
            
            <tag> Scroll-View </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
